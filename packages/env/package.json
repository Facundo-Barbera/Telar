{
  "name": "@telar/env",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Environment lease scheduler — five-verb contract, sidecar config, MCP server + CLI. Standalone; Telar-optional.",
  "exports": {
    ".": "./src/index.ts"
  },
  "bin": {
    "telar-env": "./src/cli.ts"
  },
  "scripts": {
    "test": "bun test",
    "typecheck": "bunx --bun tsc --noEmit -p tsconfig.json",
    "mcp": "bun src/mcp.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.21.1",
    "yaml": "^2.9.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^26.1.0",
    "typescript": "^6.0.3"
  }
}
