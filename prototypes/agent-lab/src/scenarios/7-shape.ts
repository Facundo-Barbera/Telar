/** Scenario 7's two numbers, in one place so the generator and the scenario
 *  cannot disagree about how many turns there were or which one was gated. */
export const TURNS = 40;
export const GATED_TURN = 3;
