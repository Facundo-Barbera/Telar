# Deferred Work

Items surfaced by a workflow but deliberately not actioned at the time. Each entry records where it came from so it can be picked up with its original context.

## Deferred from: code review of 1-1-state-root-isolation-and-the-attributed-spend-ledger (2026-07-25)

_**Citation policy for this section — adopted 2026-07-25, after the THIRD drift.** Every reference below names a **file and a symbol**: a function, a const, a type field, or a test title. That is the whole fix. Precise line numbers were tried twice and were stale within the hour both times — the first draft's `weave.ts:326`/`:476` had already moved by more than 150 lines, and the re-measured set that replaced it was invalidated again when the gate edited the five resolver files, leaving all ten resolver citations pointing at comment prose. The symptom kept being repaired and the cause kept being left in place: **a line number is not a name.** It identifies a slot in a file, and any edit above it hands that slot to something else — the same defect class as a ledger key that names a slot rather than the event that filled it (see the story's Repair Round 2). Symbols survive edits; grep finds them. Where a number genuinely helps a reader navigate a long file it is written `≈:N` and is a **pointer, not a fact** — verify by symbol, never by number. Everything below was re-measured against the working tree on 2026-07-25; do not add a number here you have not just measured._

- **Unbounded in-process retention of the whole ledger** — `packages/core/src/usage-ledger.ts`. `type Fold` carries `entries: UsageEntry[]`; `foldLine` pushes every accepted row onto it and nothing ever removes one, so the fold cache holds a parsed `UsageEntry` for every line ever written, for the life of the process. `usageSummary()` walks that whole array on every call — its `ownerKind !== "session"` and trailing-7-day `ts` filters are applied *inside* the loop, so answering a 7-day question costs a scan of all history. The repair rounds added a **second** unbounded structure to the same cache: `Fold.seenKeys`, a `Set<string>` holding every non-empty `entryKey` the fold has consumed. It now has two consumers, so it cannot simply be dropped: `foldLine` uses it as THE idempotence guarantee (first occurrence wins; a repeat returns early, which also skips the `entries.push`), and `logUsage` consults it before appending as a best-effort "already billed, no new line needed" short-circuit that its own comment marks as an optimization rather than the guarantee. Bounding it by a time window is not free either — a duplicate key older than the window would fold a second time, and double-counting money is the worse direction to fail in. Row volume also grew again: the loom leg's `recordSpend` (`packages/core/src/weave.ts`, called at both settle sites inside `runWeave`) writes **one row per billable attempt** rather than one row per settled child, and the ultra leg writes one keyed row per billable agent call from the `logUsage` call inside `ultra/storage.ts`'s `onEvent` handler — both on top of the pre-existing per-chat-turn row written from `apps/web/app/api/chat/route.ts`. Repair round 2 re-cut that row's `entryKey` from the per-run ordinal to the individual settle of it (`ultra:<runId>:<ordinal>:<settleSeq>`, measured 2026-07-25 — see the story's Review Findings), which is a growth **increase** for this item, not a decrease: a resume that genuinely re-runs an ordinal now mints a new key and appends a row where the ordinal-shaped key folded it away. No rotation, pruning or `ts`-based eviction exists in `usage-ledger.ts`, and no other module opens `usage.ndjson` by path (the port-ownership rule, stated in that file's header and in `project-context.md`), so nothing else can prune it either. Not a defect at current volumes; the cost shows up when a long-lived `next dev` process meets a mature `~/.telar`. Fix when it bites: bound `entries` by the 7-day window at fold time — deciding explicitly what that window means for `seenKeys`, whose two consumers want opposite things from it — or keep rolling window aggregates instead of the array.

- **`scripts/backfill-tool-detail.ts` still hardcodes `~/.telar`** — pre-existing, and outside story 1.1's declared write set. Its module-level `TELAR_DIR` is `path.join(os.homedir(), ".telar")` and `CHATS_FILE` is `chats.json` beneath it; `main()`'s write block (≈`:244-255`) `mkdirSync`s that directory, writes one `backfill-backup-<chatId>.json` per modified chat into it, and then tmp+renames `chats.json` there. `TELAR_HOME` appears nowhere in the file. Re-measured 2026-07-25 across `apps/`, `packages/` and `scripts/`: after the patch round hardened all five in-app resolvers, `TELAR_DIR` is the **only** remaining `~/.telar` literal in the tree — every other `os.homedir()` call resolves `.claude`, `.claude-work`, `.codex`, or a Playwright cache, none of them the state root. So AC1's premise, that every persisted store honors `TELAR_HOME`, is not yet true repo-wide. Cheap to fix — the script already imports from `apps/web/lib/`, so either `store.ts`'s exported `stateRoot()` or core's exported `telarDir()` (`packages/core/src/manifest.ts`) drops straight in. Left alone here only because it is out of scope.

- **`TELAR_HOME` resolution is duplicated across five modules** — `apps/web/lib/store.ts` (`stateRoot`, exported), `apps/web/lib/permissions.ts` (`telarHome`, module-private), `apps/web/lib/session-log.ts` (`home`, module-private), `packages/core/src/looms.ts` (`telarDir`, module-private), `packages/core/src/manifest.ts` (`telarDir`, exported). Mostly pre-existing and consistent with the settled idiom; the duplication is now explicitly declared deliberate in-source, in the comment block above `manifest.ts`'s `telarDir` — "Five copies is deliberate here". Recorded because story 1.1 made it sharper: `store.ts` still resolves its state root **two different ways within one file** — `chatsFile()` and `planFile()` through its own `stateRoot()`, `usage.ndjson` through core's `telarDir()`, reached from `store.ts`'s `@telar/core` import of `usageCostBySession`/`usageTokensBySession` (and its re-export of `logUsage`/`usageSummary`) into `usage-ledger.ts`'s `usageFile()`. The two expressions are byte-identical today. If the default or the env-var name ever changes in one place, a single module would write a session's history and that session's spend into two different roots, and `getChat`'s projection would read an empty ledger — surfacing as a silent $0.00 rather than an error. Note that `store.ts` already imports `@telar/core` at runtime, so the duplication buys no boundary benefit there.
  - **Corrected 2026-07-25.** The earlier draft of this entry ended: _"both expressions use `??`, which does not catch an empty-string `TELAR_HOME` — tracked separately as a patch finding on story 1.1."_ **That is no longer true.** The patch round replaced `??` with a trim-and-resolve guard in **all five** copies. Re-verified byte-for-byte on the working tree: each of the five resolver bodies named above is now exactly `const v = process.env.TELAR_HOME?.trim();` followed by `return v ? path.resolve(v) : path.join(os.homedir(), ".telar");`, so an empty, whitespace-only or relative root can no longer redirect state into the process's cwd. What is deferred here is **only the duplication itself**, not the empty-string hole.
  - All five copies are now under test, so drift between them fails rather than passes silently. `packages/core/test/state-root.test.ts` covers `manifest.ts`'s `telarDir()` and `looms.ts`'s private `telarDir()` (observed through `loomDir()`) and pins the two against each other in `"the two core resolvers agree on the same root for the same input"`. `apps/web/lib/store.test.ts` covers `store.ts`'s `stateRoot()` in `"an empty TELAR_HOME resolves to the home default, never to the cwd"` and `"a relative TELAR_HOME resolves to an absolute path"`. `apps/web/lib/state-root.test.ts` — added by the repair round specifically to close this gap — covers `permissions.ts` and `session-log.ts` through their nearest exported callers, since neither resolver is exported: the `"permissions.ts state root (CAP-1, Finding 8)"` block drives `addRule`/`readRules`, and the `"session-log.ts state root (CAP-1, Finding 8)"` block drives `startSessionLog`/`appendSessionEvent`/`readSessionEvents`. What is **not** pinned — re-confirmed on the current tree — is that the three `apps/web` copies agree with the two core copies: the cross-check test lives in the core suite, whose own header states the web resolvers "cannot be reached from here", and no suite compares a resolver across the workspace boundary. Collapsing the five into one shared helper would make that agreement structural instead of asserted.

## Deferred from: code review of 1-2-the-event-bus-admission-control-and-the-lease-primitive (2026-07-26)

_Citations name a **file and a symbol**, per the policy adopted in the 1-1 section above. Any number is a pointer, not a fact._

- **`writeLease`'s atomic write uses a fixed temp filename, so concurrent writers to one owner directory can swap ownership or crash** — `packages/core/src/runner/lease.ts`, `writeLease`. The temp path is derived from the owner directory alone (`const tmp = file + ".tmp"`, ≈`:60`) with no pid, token or random suffix, then `io.writeFileSync(tmp, …)` is followed by `io.renameSync(tmp, file)`. Two writers to the same directory interleave badly: writer B overwrites the shared tmp before writer A renames, so **A's `renameSync` promotes B's record under A's own call** — the surviving `.runner-lease` names the wrong `pid`/`token` — and B's subsequent `renameSync` then throws `ENOENT` because the tmp file A already consumed is gone. That is exactly the scenario a lease exists to arbitrate: an old owner's `heartbeatLease` racing a new owner's post-reclaim `writeLease` on the same root. The module header's MOAT paragraph reasons only about a **stale** lease ("at worst a false-positive `failed`, never an auto-`done`") and does not cover a **torn** one that is fresh but attributed to the wrong owner — a liveness oracle answering confidently and wrongly, which `runner/liveness.ts` and `runner/recover.ts` both consume.
  - **Pre-existing, and deliberately not fixed in story 1.2.** Re-measured on the working tree at `34379cf`: `git diff 578e5b4..34379cf -- packages/core/src/runner/lease.ts` touches the write path on exactly one line, `io.mkdirSync(loomDir, …)` → `io.mkdirSync(ownerDir, …)`, which is T-C1's owner-neutral parameter rename. The tmp derivation, the `writeFileSync` and the `renameSync` are byte-identical to baseline. Story 1.2 was scoped to generalize this primitive, not to re-engineer it.
  - **Why it is worth recording now rather than leaving silent.** Story 1.2 is the commit that promotes `writeLease` from a per-loom helper to THE two-lifetime primitive (AD-16) and adds `packages/core/src/sessions.ts` as a second caller surface (`writeSessionLease`, `heartbeatSessionLease`). The race is unchanged per directory, but the number of directories and call sites exercising it just grew, and the session lifetime is the one whose co-tenant (`apps/web/lib/session-log.ts`) already writes into the same directory from a different process.
  - **Fix when it bites:** give the temp path per-writer uniqueness — `${file}.${process.pid}.${token}.tmp` is enough, and both fields are already in hand at the call — since `renameSync` within a directory is atomic once each writer owns its own source path. Note the injectable `LeaseFs` seam makes this directly testable without real I/O, which is how the failure above was reproduced during review. Cheap, local, and it does not change the record shape, the filename, or any exported signature.

## Deferred from: code review of 1-3-executable-invariants-and-the-track-a-prove-run (2026-07-26)

_Citations name a **file and a symbol**, per the policy adopted in the 1-1 section above. Any number is a pointer, not a fact. The review returned **PASS with an empty Blocking section** and 21 should-fix items; the fix pass closed 20 and partially closed 1. What is below is what it did **not** close, plus one cross-track defect the review confirmed independently._

### The cross-track defect this review confirmed (owner: Track B/C, not Track A)

- **A repo-root `bun test -t "<filter>"` run leaves a process-global `mock.module("@telar/core", …)` installed inside every core suite** — `apps/web/lib/loom-mcp.answer-blocked.test.ts`, `apps/web/lib/loom-mcp.remint.test.ts` and `apps/web/lib/ultra-mcp.test.ts`. All three install the mock at **module scope** and restore it only in `afterAll`. Without `-t`, bun loads and runs one file at a time, so each mock is restored before the next file runs and the unfiltered repo-root `bun test` is green. **With** `-t`, bun evaluates every file's module scope before running any test, so those `afterAll` hooks never fire.
  - **Which suites actually produce the symptom, measured one at a time and stated precisely** — the first draft of this diagnosis was wrong about one of the three, and being wrong about one is worse than saying nothing, because the engineer who picks it up finds no such stub in that file and discards a correct finding about the other two. `loom-mcp.answer-blocked.test.ts` and `loom-mcp.remint.test.ts` stub `saveLoom` to `() => {}` and `getLoom`/`listLooms` to fixtures, and **either one alone** reproduces the failure (`TELAR_HOME=$(mktemp -d) bun test packages/core apps/web/lib/loom-mcp.remint.test.ts -t "L4 a stale lease reclaims"` → `0 pass / 1 fail`). `ultra-mcp.test.ts` **shares the hygiene defect** — same process-global install at module scope, same `afterAll`-only restore — but its factory stubs only `compileScript`, `getProject`, `getUltraManifest`, `launchUltra`, `readUltraEvents` and `stopUltraRun`, so it does **not** stub the loom writers and the same command with it → `1 pass / 0 fail`.
  - **Pre-existing, proved two independent ways.** All three files, plus `packages/core/test/m5-reconcile-liveness.test.ts`, last changed in `84a6e7c` / `c54ae19`, long before story 1.3. And story 1.2's own `m5-reconcile-liveness.test.ts` fails identically under `bun test -t "default liveness: a stranded in-flight loom"` from the repo root, with the same `TypeError: null is not an object (evaluating 'getLoom(id).state')`.
  - **This is why story 1.3's AC4 prove-run needs a path argument.** Its one-command form is quoted as `TELAR_HOME=$(mktemp -d) bun test packages/core -t "track-a prove-run"` — still one command, still from the repo root, still one process, still producing the transcript and the `across N files` figure, and it selects exactly the five legs. `packages/core` alone loads both of story 1.3's new files under the identical `-t` filter and is green, which is what proves the defect is not in this story's write set. The literal repo-root form remains `4 pass / 1 fail` on L4, by design and disclosed.
  - **Likely fix for whoever picks it up:** move each mock installation into a `beforeAll` paired with the existing `afterAll`, or scope it to the tests that need it. `apps/web` is Track B/C's write set (story 1.1's AC6 protocol: record it, do not cross it).

### Review findings left open by the 2026-07-26 fix pass

- **S21 (partially closed) — `composesStateFile` does not recognise plain string concatenation, and its `path.join` window is bounded at 200 characters** — `packages/core/test/invariants.test.ts`, `composesStateFile`. *Risk:* a second writer that composes `` const f = `${telarDir()}` + "/" + "usage.ndjson" `` matches neither the `path.join` branch nor the `${…}/literal` branch, so INV-5a's `expect(composers).toEqual([USAGE_LEDGER])` still passes with a real second writer of the spend ledger on disk. *Why left:* the concat form cannot be matched without also matching ordinary message-building (`"we project over " + "usage.ndjson"`), and an invariant that fires on correct code gets deleted rather than fixed — which is a worse outcome than the gap. Closing it properly means matching on the assignment's right-hand side rather than on a substring window, i.e. a small expression parser, which is a redesign the review did not ask for. *What was closed instead:* INV-5d now asserts that the one module naming `.runner-lease` genuinely **composes** a path from it rather than only mentioning it, so "exactly one module" cannot be satisfied by a file that stopped doing the work.
- **Nice-to-have, all twelve, left as recorded** — `packages/core/test/invariants.test.ts` and `packages/core/test/track-a-prove-run.test.ts`. *Risk:* each is a narrow scanner gap on a shape that does not exist in the tree today — `isTestFile` not matching `foo.test.mts`; `walk()` skipping symlinks silently; `firstStatement` not skipping U+FEFF before a `"use client"` directive; `resolveLocalImport` matching case-sensitively on a case-insensitive filesystem; `exportedStringConst`/`exportedStringArray`/`GRANT_FIELD_PATTERNS.disallowedTools` each requiring one inline syntactic shape, so a safe DRY refactor of the ten-name denylist would fail INV-2b on compliant code; `toolNameLiterals` extracting only bare string literals, so `tool(ACCEPT_TOOL_NAME, …)` never enters `allToolNames`; both citation checks being whole-file substring searches, so a deleted test whose title survives in a comment still reads as live; and three assertions that add no independent signal. *Why left:* the review classified them below the should-fix bar, none makes a present assertion false, and the fix pass was scoped to the should-fix list in consequence order. Two of them were closed opportunistically because the fix pass was already editing those lines — L2's containment check now compares against `realpath(HOME)` rather than `realpath(os.tmpdir())` (which any temp directory satisfied), and the MCP inventory's order-sensitive comparison now reports a pure reorder as *"cosmetic, not a moat breach; re-pin the order"* instead of printing a diff that reads like one.
- **The two `expect`s at the end of L3's `finally`** — `packages/core/test/track-a-prove-run.test.ts`, the `finally` block inside `"L3 a loom-verify call wins a freed slot…"`. *Risk:* if the drain leaves admission non-empty **and** the `try` block already failed, the cleanup assertion replaces the original failure and hides the real cause. *Why left:* they are the drain's own proof that T-6 cannot fire, and suppressing them conditionally means catching and re-throwing around the whole leg — more machinery than the hazard justifies, and it would obscure the drain the review specifically wanted strengthened (S15, closed: `Promise.allSettled` now releases every fulfilled handle rather than short-circuiting on the first rejection).

## Deferred from: 2-1-the-sessionprofile-resolver (2026-07-26)

_Citations name a **file and a symbol**, per the policy adopted in the 1-1 section above. Any number is a pointer, not a fact; everything below was measured against the working tree on 2026-07-26 while story 2.1 was implemented._

- **A project's `manifest.guardrails` is NEVER enforced on a Codex session** — `apps/web/app/api/chat/route.ts`, the `provider === "codex"` fork versus its `else` branch. This is a genuine hole in AD-1's tool-layer half, it is **older than story 2.1**, and it is recorded rather than fixed because closing it means changing behaviour **inside** `new ReadableStream`, which story 2.1's AC5 forbids ("the existing project-session path is untouched").
  - **The measurement, stated so it can be re-derived by grep rather than trusted.** `makeGuardrailDecision` (owned by `apps/web/lib/permissions.ts`) has exactly **two** call sites in the route: one inside `canUseTool` and one inside `preToolUseGuardrail`. Neither closure is reachable on a Codex turn, because both reach the SDK only as `query()` options — `canUseTool` and `hooks: { PreToolUse: [{ hooks: [preToolUseGuardrail] }] }` — and `query()` is called only in the `else` branch. `manifest.guardrails` is read in exactly **one** place, the `disallowedTools` array in that same `else` branch. Searching the whole Codex fork (from `if (provider === "codex")` to its `} else {`, ≈`:922-1192`) for `makeGuardrailDecision`, `guardrails`, `disallowedTools` or `protectedPaths` returns **nothing**.
  - **What that means concretely.** A project that configures `guardrails.disallowedTools: ["Bash"]` or `guardrails.protectedPaths: [".env"]` in its `telar.yaml` gets both enforced on a Claude session and **neither** on a Codex session. The Codex fork's only access controls are the up-front `sandbox` and the interactive `approvalPolicy`/`onCodexApproval` card — real controls, but neither consults the project's own guardrail configuration, so a per-project protected path is silently not protected there. The failure is silent in both directions: nothing logs it, and the manifest gives the operator no signal that half its guardrail block is inert for that provider.
  - **What story 2.1 did about it instead.** It built the mechanism that makes this class of gap *report* rather than degrade silently: `ProviderCapability` (`packages/core/src/providers.ts`) publishes `tool-allow-deny-lists` and `pre-tool-use-hooks` for Claude and not for Codex, and `unmetCapabilities` (`packages/core/src/session-profile.ts`) turns a profile that needs either into the route's pre-SSE 400. That is AD-11 applied to the *profile*, and it is why an `escalation` session on a Codex account now 400s. It does **not** close this hole, because the ordinary `project` kind deliberately requires **no** capability (AC5 requires a project session to keep working on both providers), so a project session on Codex still runs with its manifest guardrails unenforced.
  - **Fix when it bites, and who owns it.** Track B, as part of story 2.2 or the Codex MCP-injection work. Two shapes are available and they are not equivalent: (a) make the Codex fork consult `makeGuardrailDecision` inside `onCodexApproval` before shaping the permission card, which enforces `disallowedTools`/`protectedPaths` at the approval seam and is the smaller change; or (b) have the `project` profile require `tool-allow-deny-lists`, which would fail Codex project sessions closed and is a **behaviour change AC5 explicitly forbids in 2.1** — do not reach for it without deciding that Codex project sessions should stop working until (a) lands.
  - **PARTIALLY CLOSED by story 2.2's AC6 (2026-07-26). Candidate (a) shipped; candidate (b) was rejected outright and must stay rejected.** `makeGuardrailDecision` now has a **third** call site, inside `onCodexApproval` in `apps/web/app/api/chat/route.ts`, driven from `sessionProfile` and `sessionProfile.cwd` rather than from `manifest` — so the profile's resolved guardrails (the manifest's, unioned with anything a profile adds) govern **both** providers, and a guardrail-denied action is declined **without** a permission card ever being shaped. Showing a human a card for something project policy already forbids invites them to approve it. Coverage: `apps/web/lib/session-profiles.test.ts` § "AC6 the profile's guardrails govern a Codex approval, not just a Claude tool call", driven with the exact input shape `onCodexApproval` builds. Candidate (b) is **forbidden**, not merely unchosen: it 400s every Codex **project** session, contradicting story 2.2's own AC3 and AC4, and two tests exist specifically to stop it — `packages/core/test/session-profile.test.ts`'s `AC4/AC5 the \`project\` kind requires NOTHING, on BOTH provider ids` and `apps/web/lib/session-profiles.test.ts`'s `a project session passes the capability gate on BOTH providers — AC5, unchanged`.
  - **THE RESIDUAL IS FOUR DISTINCT HOLES, NOT ONE — owner: story `5-5-handoff-external-sources-codex-mcp-injection-and-the-workspace-prove-run`.** A partial fix that reads as complete is worse than the gap, so each is stated separately with what it costs. _(Hole 4 was added by story 2.2's **code review**, 2026-07-26, which found the entry named three where there are four. It is recorded here rather than in a review section because this is where the gap has its one home, and because it is the hole that survives even when the card DOES fire with a real shell command — the case holes 1–3 all leave standing.)_
    1. **`onCodexApproval` only runs when Codex asks.** With `approvalPolicy: "never"` the callback is not even installed — `apps/web/lib/codex-app-server.ts`'s `onApproval` field comment says it "can be omitted entirely for approvalPolicy:'never'" — and for any action the chosen `sandbox` already permits, no approval is requested at all. So the guardrail is enforced only on the subset of actions Codex happens to surface. *This hole is about the card not firing.*
    2. **The sandbox has no per-path or per-tool granularity.** `sandboxPolicy()` maps the three presets to `readOnly` / `workspaceWrite{writableRoots:[cwd]}` / `dangerFullAccess`. A `protectedPaths` entry **inside** the workspace is therefore still unprotected on a `workspace-write` turn, regardless of what the manifest says.
    3. **A file-change approval carries no path — and this hole is live even when the card DOES fire.** Measured in `codex-app-server.ts`'s `answerApproval`: the block that populates `command` and `cwd` is guarded by `if (!isFile)`, so a `fileChange`/`applyPatch` approval reaches `onApproval` as `{ kind: "file", reason?: string }` and **nothing else** — no path, no cwd. `makeGuardrailDecision` can then check `protectedPaths` two ways and neither works: `inputPaths()` reads `PATH_KEYS` (`file_path`/`path`/…), none of which exist here, and `bashTouchesProtectedPath` word-splits `input.command`, which for a file change is the synthesized free-text sentence `` `[file change] ${reason}` ``. So `protectedPaths` enforcement on a Codex file edit degrades to *"does the model's prose reason happen to contain the protected path as a standalone word"* — which is not a check. **It survives `approvalPolicy: "untrusted"`**, the setting an operator would choose precisely *because* they expect every action to be gated. This is why story 2.2's check is gated on `req.kind === "command"`: `onCodexApproval` shapes **both** request kinds as a `"Bash"` card, so running the check on a file change would deny every file edit in a project whose guardrails merely disallow the Bash **tool** — a false positive on a request that is not a shell command at all. That false-positive guard has its own test (`THE FALSE-POSITIVE GUARD — a file-change request must NOT be denied by that same Bash entry`).
    4. **The approval's own `cwd` is captured and then never used as the resolution root, so a relative command escapes `protectedPaths` — and this hole is live even when the card DOES fire with a real shell command.** Measured in `apps/web/app/api/chat/route.ts`, `onCodexApproval`: it builds `input` with `...(req.cwd ? { cwd: req.cwd } : {})` and then calls `makeGuardrailDecision(sessionProfile, sessionProfile.cwd, "Bash", input)` — the **root argument is always the session root**. `req.cwd` reaches `makeGuardrailDecision` only as a key on `input`, and nothing there reads it: `apps/web/lib/permissions.ts`'s `PATH_KEYS` is `["file_path", "notebook_path", "path"]`, so `inputPaths` never sees it, and `bashTouchesProtectedPath` takes `root` as an **explicit parameter** rather than deriving it from the input. So `input.cwd` is inert for every guardrail check. *What it costs:* project root `/repos/demo`, `protectedPaths: [".env"]`, Codex's shell in `/repos/demo/packages/app` (which is **why** `req.cwd` is on the wire at all), command `rm -rf ../../.env`. The check resolves `../../.env` against `/repos/demo` → `/.env`, which matches nothing, so the decision is `allow` and the human is shown an ordinary card for a command that targets the protected file. *Not Codex-specific, and that is the point:* the two Claude call sites (`canUseTool`, `preToolUseGuardrail`) have the identical property and always did — a `Bash` invocation whose own working directory differs from the session root resolves against the wrong root there too. The Codex seam is where it is **sharpest**, because that callback is handed a real per-approval `cwd` on the wire and drops it. *Why it is deferred rather than fixed:* passing `req.cwd` as the root would change `makeGuardrailDecision`'s meaning at one of three call sites and diverge the three; passing it *correctly* means teaching the resolution to take a per-invocation working directory, which is a change to `apps/web/lib/permissions.ts` — a file story 2.2 was explicitly scoped to **call and not change** ("you **call** it from a second seam; you do not change it"), and which Claude-side callers would then have to be migrated onto in the same breath. *Fix when it bites:* give `makeGuardrailDecision` an optional per-invocation cwd that `isProtectedPath` resolves relative paths against, defaulting to `root` so the two Claude sites are unchanged, and thread `req.cwd` into it from `onCodexApproval`. Cheap and local; it changes one exported signature additively.
  - **There is no universal pre-tool interception point on the Codex path today**, and a complete closure needs one. Story 5.5 is named as owner because it is the only queued work that adds plumbing to the Codex path (Codex MCP injection), and this section already named "the Codex MCP-injection work" as one of two candidate owners.
  - **A clamp was considered and rejected by story 2.2.** Forcing `approvalPolicy` to at most `"on-request"` when the profile's guardrails are non-empty would close hole (1). It is **new policy**, not a retrofit: it changes Codex behaviour for projects that never asked for it, and it still would not close hole (2). Recorded as a candidate for story 5.5; deliberately not implemented in 2.2.

- **[CLOSED by story 2.2, 2026-07-26]** **Three of the four spec builders return `systemPromptAppendix: ""`, including `planner`, which story 2.1's D11 expected to be able to supply now** — `apps/web/lib/session-profiles.ts`, `buildPlannerProfile` / `buildSteererProfile` / `buildEscalationProfile`. Recorded because it is a **disclosed deviation from the story's own design table**, and because story 2.2 is the owner and would otherwise have to re-derive why.
  - **How it closed, so this entry reads as history rather than as open work.** Story 2.2 did exactly what the "Fix when 2.2 lands" line below prescribes. `apps/web/lib/session-prompts.ts` is the new home for `PLANNER_SYSTEM_PROMPT`, `STEERER_SYSTEM_PROMPT`, `ESCALATION_SYSTEM_PROMPT`, the Ultra note, `tail`, `safeRead`, `buildSteererContext` and `buildEscalationContext`, all moved out of the route byte-identically; `SessionResolutionContext` gained a required `ultraAnnotated: boolean`; and all four builders now compose their real appendix. The tripwire fired as intended — the four `""` expectations in `apps/web/lib/session-profiles.test.ts` failed the moment the values changed and were replaced with expectations re-derived from the prompt constants themselves. **One property is genuinely new and worth carrying forward:** a `steerer` or `escalation` builder is no longer PURE — it performs the per-turn live read while composing its appendix, and that read now happens PRE-STREAM, where a throw is a bare 500 instead of an SSE `error` event. `safeLiveContext` wraps the live read and nothing else, so a failure degrades to the static prompt and never to `""` (an empty appendix would silently drop the moat language that *is* those kinds). The core fold stays pure.
  - **What story 2.2 deliberately did NOT close, with a forward owner:** `mcpServers` is still omitted by all four specs (resolving to `{}`). Measured: the route's set is `{ loom, ultra, ...resolveProjectMcpServers(project) }` and is **unconditional** — it never branched on session kind, so 2.2's AC1 did not require moving it — and building it is a side effect rather than data, because `createLoomMcpServer`/`createUltraMcpServer` close over `getSessionId: () => capturedSession`, a variable the SDK's `system:init` message mutates *inside* the stream. **Owner: epic 5's project-less master profile**, which is the first kind that genuinely needs a different MCP set and therefore the first story with a reason to pay for the restructure.
  - **The measurement that forced it.** D11 reasoned that `planner`'s appendix is "a pure function of the context (the static `PLANNER_SYSTEM_PROMPT` plus `ultraAnnotationNote`), so it can be supplied now." It cannot, for a reason D11 did not measure: `PLANNER_SYSTEM_PROMPT`, `STEERER_SYSTEM_PROMPT` and `ESCALATION_SYSTEM_PROMPT` are all **module-private consts inside `apps/web/app/api/chat/route.ts`**, whose only export is `POST`. Reaching them from `lib/session-profiles.ts` requires either exporting a second symbol from a Next.js route module or hoisting the constants out of the handler — both are larger edits to `route.ts` than story 2.1's AC5 permits ("import additions, the resolve call, the capability gate") — and copying the text would create a second source of truth for moat-adjacent prompt content. `ultraAnnotationNote` is a further blocker for `planner`: it is derived from the per-turn `ultra` wire flag, which is **not** a field of `SessionResolutionContext` (D1 fixes that shape and does not include it).
  - **Why it is harmless in 2.1 and not harmless later.** Nothing consumes the resolved profile in story 2.1 except the capability gate (D8: "resolve for every request, consume only the gate"), so an empty appendix changes no behaviour today. The moment 2.2 drives `query()`'s `systemPrompt` from the profile, an empty appendix would **silently drop the planner/steerer/escalation guidance** — the exact silent degradation AD-11 exists to end, reintroduced from the other side. `steerer` and `escalation` were always going to be 2.2's, because their real values embed per-turn live reads (`buildSteererContext`, `buildEscalationContext`) that happen inside the stream body; `planner` joins them for the reason above.
  - **Fix when 2.2 lands:** hoist the three prompt constants out of `route.ts` into a `lib` module (2.2's AC1 removes the session-kind conditionals anyway, so the constants have to move regardless), add the per-turn Ultra annotation to `SessionResolutionContext`, and pin all four appendices in `apps/web/lib/session-profiles.test.ts`, which today asserts `""` for all four **on purpose** and will fail loudly the moment they change — which is the intended tripwire.

## Deferred from: code review of 2-2-retrofit-the-existing-session-kinds-onto-profiles (2026-07-26)

_Citations name a **file and a symbol**, per the policy adopted in the 1-1 section above. Any number is a pointer, not a fact. The review returned **CHANGES-REQUIRED** with one blocking finding (a test that reached the operator's real state root — fixed in the same pass, see the story's Review Fix Round) and four should-fix / nice-to-have items. What is below is what the fix pass did **not** close, each with why. The fourth hole in story 2.1's Codex-guardrail residual, above, also came out of this review and is recorded there rather than here._

### The cross-track defect this review confirmed (owner: Track C, not Track B)

> **✅ CLOSED by story 3.1 (2026-07-26).** Story 3.1 is the Track C story that next opened `session-runtime-host.tsx`, and it took the ownership explicitly rather than passing it on a sixth time — as its **AC9**, the one deliberate behaviour change in that story, disclosed as such. The fix is a **consolidation, not a second implementation**: `readPreStreamError(res)` in `apps/web/components/conversation/pre-stream-error.ts` is one shared, never-throwing helper, called from `sendTurn`'s new `else` arm, and covered by `pre-stream-error.test.ts` (hand-built `Response` objects — no DOM, no network, no server). The sentence goes to a **new optional field**, `Runtime.error?: string` in `dock-provider.tsx`, rendered as a banner between the transcript and the composer in `dock.tsx`, and cleared as the next send starts. It is deliberately **not** pushed through `liveMsgsRef` as a synthetic `{ role: "assistant" }` message: disguising a rejected turn as a model reply is a worse failure than the silence it replaced.
>
> **What is NOT closed, and stays open:** `session-view.tsx`'s own `send` still carries its own inline copy of this read and was **not** migrated onto the helper. `send` sits in story 3.1's declared "stayed" set and its AC6 is a behaviour-unchanged claim, so retrofitting it would have been a behaviour-risking edit inside the one function that claim most depends on, for a tidiness gain. One caller of a shared helper is still a shared helper; the second caller arrives free whenever a later story has `send` open for another reason. **Owner: whoever next edits `send`.**

- **The dock swallows the new pre-SSE 400, so the user's typed message vanishes with no error** — `apps/web/components/dock/session-runtime-host.tsx`, `sendTurn`. The drain is guarded by `if (res.ok && res.body) { … }`, so a 400 body is **never read**; nothing throws, the sibling `catch` never fires, and the `finally` dispatches `telar:dock-refetch`, which reloads the persisted tail. The turn was never persisted, so the message simply disappears. The session page does **not** have this defect — `apps/web/components/session/session-view.tsx`'s `send` reads `body.error` in its own `if (!res.ok || !res.body)` branch and throws, and the enclosing `catch` renders it — which is why story 2.2's claim that the change "fails **loudly** in place of failing silently" is true there and false here. Note the symbol: it is `send`, **not** `applyServerEvent` — the latter is the SSE event switch, is handed `(event, payload)` rather than a `Response`, and is reached only via `consumeSSE` **after** the `!res.ok` branch has already thrown. The whole difference between the two surfaces is that one has that branch and the other does not.
  - **Reachable, and story 2.2 is what makes it reachable.** `sendTurn` POSTs `sessionId` and **never** `role`, for any session. A docked **planner** chat qualifies: `apps/web/app/api/chats/route.ts` filters only `steerer`/`escalation` out of the session list, planner sessions are non-embedded, and `session-view.tsx` auto-docks a non-embedded session on unmount while busy (plus the manual "Minimize to dock"). The persisted `chat.role` really is `"planner"` — `apps/web/lib/loom-mcp.ts` sets `opts.link.role = "planner"` **in place** when `draft_bundle_file` lazily creates a draft loom, and the chat route persists `role: loomLink.role`. So: docked planner session + Codex account → `resolveSessionKind` → `planner` → `unmetCapabilities` → 400 → message silently lost. Before story 2.2 that same turn returned 200 (degraded).
  - **Pre-existing behaviour, newly consequential.** The swallow is shared with every other pre-SSE 400 the dock can provoke (the nine-check preamble predates this story); what story 2.2 changed is that a **resumed loom-kind chat on Codex** now reaches one. Recorded rather than fixed because `apps/web/components/**` is **Track C** and sits outside story 2.2's declared write set — the record-do-not-cross protocol that has now run five times (story 1.1's AC6 finding, 1.2's `sessions/` co-tenancy, 1.3's `mock.module` leak, 2.1's Codex guardrail gap, this).
  - **Fix when it bites:** read the body on `!res.ok` and surface `body.error` the way `session-view.tsx` already does — the shape is `{ error: string }` for all nine pre-SSE 400s, so one branch covers every one of them. Worth doing in whichever Track C story next opens `session-runtime-host.tsx`; it is a handful of lines and it converts a disappearing message into the actionable sentence the route already wrote.

### Review findings left open by the 2026-07-26 fix pass

- **No mechanical guard exists for "a test reaches the real state root", and the blocking finding was that class's third occurrence** — `packages/core/test/invariants.test.ts`. *Risk:* the control is discipline alone, and discipline failed on the story that was actively watching for it. `INV-3a`'s inventory pins **where paths are composed in production**, not **which tests execute a reader**; the `NODE_ENV=test` write-guard lives in `packages/core/src/usage-ledger.ts` and does not cover `packages/core/src/looms.ts` at all, where `ensureMigrated` is wrapped only in `try`/`catch` — which swallows errors rather than preventing writes, so a rename that succeeds is never reported. *The concrete proposal, from the review:* a new `INV-3` sibling scanning `*.test.ts` files' **code-only** text (comments and string bodies blanked, via the existing `tokenize` helper) for call sites of the reader-reaching surface — `getLoom`, `listLooms`, `readBundleFile`, `readContract`, `deriveDeliverableSignal`, and the appendix composers that default to them — failing unless the enclosing file either sets a sandboxed root in a spawned child or passes an injected reader. Anti-vacuity floor: assert the scanner finds the known-good child-process site in `apps/web/lib/session-profiles.test.ts` (positive control) and, via a runtime-assembled fixture, that it reports an un-injected call (discriminator). Production source is excluded — `buildSteererProfile` calling the real reader is correct there. *Why left:* the review classified it nice-to-have, and it is a new several-hundred-line scanner over a 2800-line invariant suite whose false-positive surface is every legitimate reader call in every suite that already sandboxes its root correctly. Building it inside a review **fix** pass is the shape of change that breaks a green gate while repairing something else. The blocking instance itself is closed structurally — the negative-compile claim in `apps/web/lib/session-prompts.test.ts` is now a type annotation on an object, which executes nothing, and its header names the exact shape that broke.
- **A resumed `escalation` chat whose client omits `role` resolves as `project` and is handed the full 20-name allow set instead of the six read-only escalation tools** — `packages/core/src/session-profile.ts`, `resolveSessionKind`. *Measured:* the escalation rung is `if (input.role === "escalation" && input.loomId)`, which gates on the **wire** role; there is no `linkRole === "escalation"` arm, and `apps/web/app/api/chat/route.ts`'s `loomLink` does persist `role: "escalation"`. *Risk today: none that is reachable.* The port is faithful — the old `isEscalationSession = role === "escalation" && !!loomLink.loomId` had the identical wire-role requirement — and the one surface that opens these sessions, `apps/web/components/looms/discuss-escalation.tsx`, always sends `role: "escalation"` together with the `loomId`. *Why left:* narrowing the rung to `linkRole` would be a **new, undisclosed behaviour change** made inside a review fix pass — it would newly 400 resumed escalation chats on Codex and newly narrow their tool grant on Claude, neither asserted nor disclosed by story 2.2. What the review actually found here was a **record** defect, and that half is fixed: story 2.2's §5.5-D12, its Debug Log and its Completion Notes said "planner, steerer **or escalation**" where only planner and steerer can take the new 400. *Fix when it bites:* whichever story next has escalation in its write set should decide deliberately whether the persisted link role should promote a resumed chat back to `escalation`, and disclose it if so.

---

## Deferred from: 3-1-carve-out-the-conversation-shell-and-its-primitives (2026-07-26)

_Citations name a **file and a symbol**, per the policy adopted in the 1-1 section above. Any number is a pointer, not a fact. Everything below was found while carving the render seam out of `session-view.tsx` and was deliberately **not crossed** — the record-do-not-cross protocol, now on its sixth run. Story 3.1 also **closed** the 2-2 Track C dock item above; see the ✅ note there._

### Recorded and not crossed (each with a named owner)

- **`SessionView` is not renamed to `ProjectSessionView`** — `apps/web/components/session/session-view.tsx`, the exported `SessionView` symbol. `UX-DR4` says *"`ProjectSessionView` (what `session-view.tsx` becomes)"*, and story 3.1 satisfied that **in substance** (the file is now the shell's first owner adapter: route, state and API stayed; the render seam left) while deliberately **not** renaming the export. *Measured:* the export has four call sites and **two are `app/**` pages** (`app/projects/[name]/sessions/[id]/page.tsx`, `app/looms/plan/[project]/page.tsx`), which Track C — "components only" — does not own; the other two are `components/looms/chat-tab.tsx` and `components/looms/discuss-escalation.tsx`. A rename also inflates the diff that a behaviour-unchanged claim (AC6) has to cover, inside the one extraction the epic's own dispatch note warns about. Note too that `epics.md` lists epic 3 as delivering `UX-DR1/2/3` — **`UX-DR4` is not in epic 3's list**, and its coverage row spreads the five adapters across 3.1/5.3/6.2/6.6. **Owner: the story that next holds both this component and `app/**` in one write set.**
- **`session-runtime-host.tsx`'s `applyLiveEvent` is a seventh transcript reducer and still is** — `apps/web/components/dock/session-runtime-host.tsx`, `applyLiveEvent`. It silently drops nine SSE event types through its `default:` case (thinking, thinking_delta, tool_result, task_status, permission, permission_result, permission_denied, plan, title, session, saved). It genuinely should die — the dock should render the same `TranscriptItem[]` the shell does, through the same registry — but killing it is a **real behaviour change to the dock's rendering**, not the six-line error path story 3.1's AC9 owned. Story 3.1 touched `sendTurn`'s failure branch and nothing else in that file. **Owner: whichever story next rebuilds the dock's transcript, most plausibly 4.2, which needs a live dock signal anyway.**
- **`handoffDismissed` is declared, set, and never read** — `apps/web/components/session/session-view.tsx`, `handoffDismissed` / `setHandoffDismissed`. Dead state left over from the loom-handoff banner that the aggregate looms pill replaced. Deleting it is a correct cleanup and still a diff line AC6 does not need, so it was recorded rather than done (story 3.1 §5.6-T1a). **Owner: any later story with this file open for another reason.**
- **`SessionView`'s prop type is written out twice, identically** — `apps/web/components/session/session-view.tsx`, the inline object types on `SessionView` and `SessionViewInner`. Collapsing them to one named type is correct and is a pure-mechanical edit; it was left for the same reason as above (story 3.1 §5.6-T1b). **Owner: as above.**
- **`components/session/session-view.tsx` still owns `estimateTranscriptTokens`, `QueueChip` and the composer's slash-command menu** — all three are transcript-adjacent but session-owned (the token estimate feeds the CTX pill; the queue's dispatch policy is welded to the turn state machine; the menu reads `usePromptInputController`). They stayed by design (story 3.1 §5.5-D7). Recorded only so a later reader does not mistake them for an oversight. **No owner; not a defect.**
- **The `loom:*` transcript rows are still adapter chrome, not item kinds** — `apps/web/components/session/session-loom.tsx`, `InlineLoomRow`, rendered through the shell's `trailing` prop rather than as registered items. The clean end-state is `loom:*` kinds, but `loom:` is **Track F's** namespace and registering another track's kinds from Track C is exactly the AD-13 breach readiness finding UX-2 warned about. **Owner: Track F (6.2).**

### An environment note, not a repo finding — CORRECTED 2026-07-26, then ✅ CLOSED 2026-07-26

> **✅ CLOSED by commit `0bd6073` (2026-07-26) — `bun run dev` serves every route again.** The fix is the second of the two candidates this note names: `apps/web/next.config.ts` now lists `shiki` in `transpilePackages`, which takes a package OFF Next's built-in server-external list, so it is bundled and never `require`d from `<distDir>/**/chunks/` at runtime — and the unreachable-install-layout problem below stops being reachable at all. The lockfile was NOT touched; the hoisting candidate was not taken.
>
> **Attribution, because it matters for reading the story record:** `0bd6073` is a **separately-orchestrated, operator-requested fix**, made AFTER story 3.1's own commits (`7dd508f`, `3416c52`) and outside its declared write set. It is **not** part of story 3.1's deliverable. Story 3.1's record now discloses it (§0's table, §10's File List, Completion Note 1 and Note 17) because the story's evidence was gathered on either side of it, not because the story owns it.
>
> **Re-verified at HEAD on 2026-07-26**, during story 3.1's review-fix round, against a **real `next dev`** on an isolated `TELAR_HOME` in a session scratchpad: `/`, both project-session routes and all six `/demo-gallery/conversation-*` configurations answer **HTTP 200**, and the full interactive driver (25 assertions) passes under dev with **zero page errors**. The `next start` substitution the first proof needed is therefore no longer forced, and the story's Debug Log (6) says so.

- **[HISTORY — the diagnosis that led to the fix above]** **`bun run dev` cannot serve ANY route in this environment, and it is not a stale cache.** The first pass of story 3.1 recorded this as a stale `apps/web/.next` Turbopack cache and said `rm -rf apps/web/.next` would clear it. **That diagnosis was wrong**, and the operator deleted 814M acting on it. Re-measured against a cache generated from scratch, the identical error returns: `Failed to load external module shiki-db8d315635eb368c: Cannot find package …`. The actual cause is the **install layout**: `shiki@3.23.0` exists only at `node_modules/.bun/shiki@3.23.0/node_modules/shiki`, linked from `node_modules/.bun/node_modules/shiki` and from `@streamdown/code`'s own tree, and is therefore **not reachable by Node resolution from `apps/web/.next/dev/server/chunks/`** — which is exactly where Turbopack's dev-mode external `require` resolves from. `next dev --webpack` is not a way out either: it fails with `UnhandledSchemeError: Reading from "node:child_process" is not handled by plugins`, because `transpilePackages: ["@telar/core"]` pulls core's `node:` imports into a bundler this repo does not configure for. Both reproduce on `/` and on gallery entries that predate epic 3, so neither is caused by story 3.1. **What still works:** `bun run build` (exit 0) and `bun run start`, which serve every route at HTTP 200 — that is how story 3.1's interactive proof was gathered. **Fix when it bites:** hoisting `shiki` so it resolves from the app root (an install-layout change, so it touches the protected lockfile), or a `serverExternalPackages` / Turbopack resolve-alias entry in `next.config.ts`. **No owner assigned — this is environment/tooling, not a track's work**, but anyone who needs a working `next dev` will hit it immediately.

### One residual from AC9's fix (owner: whoever next edits the dock's transcript)

- **The dock now ANNOUNCES a rejected turn but still drops the user's typed text** — `apps/web/components/dock/session-runtime-host.tsx`, `sendTurn` + `openTail`. Measured live, with the fix in place: the error banner is durable (still rendered at +30s), but the typed message is visible only until roughly +6s. `openTail` calls `resetLive()` on every re-arm (~3.5s while idle), which clears `liveMsgsRef`, and the next `commitLive`/refetch composes `messages` from the persisted tail alone — the turn never persisted, so the text goes. AC9's proof clauses only ever required the *sentence* to be surfaced, and it is; this note exists so nobody later reads "the dock no longer discards the user's typed message" as meaning the text is preserved or restored into the composer. **Fix when it bites:** keep the rejected user bubble in the overlay until the next successful send, or put the text back in the composer input. It sits naturally with the `applyLiveEvent` rebuild recorded above — same file, same owner.
  - **Re-measured 2026-07-26 under real `next dev`**, after the review-fix round added a second clear for `Runtime.error` (a turn started from the full session view now clears the dock's banner too — SF-3): the banner is still durable (`banner=1` at +2s, +6s, +12s, +20s, +30s) and the typed text is still gone by ~+6s. The new clear fires only when the live tail actually delivers an event, so an IDLE session keeps the sentence — which is what makes both properties true at once. **The residual is unchanged.**

### Deferred from story 3.1's REVIEW FIX ROUND (2026-07-26)

_The review returned CHANGES-REQUIRED with an empty Blocking section, ten should-fix items and eighteen nice-to-haves. The fix round closed all ten should-fix items and fifteen of the eighteen nice-to-haves. What is below is what it did **not** close, each with its finding id and why._

- **NH-11 — `Marker` hardcodes a fourth literal copy of `status.tsx`'s amber pair** — `apps/web/components/conversation/marker.tsx`, `Marker` (`border-amber-600/40 text-amber-600 dark:text-amber-400`) versus `apps/web/components/looms/status.tsx`, `TONE_ICON.attention`. *Why it was not closed:* the only import that would de-duplicate it points the CONVERSATION SHELL at the LOOMS module — `components/looms/status.tsx` is a `"use client"` domain module that pulls in `Badge`, a lucide icon set, `GodStatusKind` and `WorkUnitState`. Story 6.6's `TranscriptView` and every other downstream surface would inherit that edge from a 55-line presentational primitive, and the whole point of AD-12's shell is that it depends on no module's vocabulary. Copying the two Tailwind classes is the smaller mistake than inverting the dependency the epic exists to establish. *The real fix, for whoever owns it:* lift the tone pair into a token both can import (`lib/utils` or a `components/common/tone.ts`), which is a Track-neutral change no current story owns. *Note that `TONE_ICON.attention` carries only the TEXT half* — the border (`border-amber-600/40`) exists nowhere else — so even the naive import would not have removed the literal.
- **NH-2 (deliberately closed as DOCUMENTATION, not as a behaviour change) — `ItemViewState.render` zeroes a nested item's liveness rather than inheriting it** — `apps/web/components/conversation/conversation.tsx`, `renderItem`'s `view.render`. *What changed:* the JSDoc now states the default and why. *What did not:* the default. Inheriting the parent's flag would mark EVERY child of a live turn live, because liveness is a property of POSITION (the trailing item), which only the composite knows for its own children — and the visible cost is the DISCLOSURE DEFAULT, not a spinner: `toolsKind` derives its default from liveness (`view.isOpen("group", view.live)` in `kinds.tsx`), so every nested tool group inside a streaming turn would default OPEN rather than collapsed. (The spinner is the narrower case. `running` is `live && output === undefined && !isError`, so a FINISHED part cannot spin whatever it inherits; only an interrupted, output-less part in a non-trailing group would. **Corrected in the round-2 review: the first version of this note, and of the shipped JSDoc, stated the spinner as the general mechanism, which the code contradicts.**) Neither default is right for a caller who has not thought about it; the one that ships is the one whose failure is invisible rather than wrong-looking. *Fix when it bites:* if a third composite kind appears and gets this wrong, make `render` REQUIRE the override (`render(item, { live })`), which is a compile-time forcing function rather than a better guess. That is a contract change to `ItemViewState`, so it belongs to whichever story next opens the shell's contract with downstream registrations to migrate.
- **NH-5 residual — INV-8e cannot tell inheritance from a generic constraint** — `packages/core/test/invariants.test.ts`, `inheritanceScan`. The floor, positive control and discriminator §5.4-E requires were all added; the `\bextends\b` ambiguity was not resolved. *Why:* no file under `components/conversation/**` has a generic constraint or an interface extension today, so the guard is exact where it runs, and narrowing the predicate speculatively is how a check stops catching the thing it was written for. The residual is stated in-source beside the predicate, with the instruction to narrow it deliberately (not delete it) the day a legitimate constraint arrives.

---

## Deferred from: 4-1-completion-wake-and-session-cost-rollup (2026-07-26)

_Citations name a **file and a symbol**, per the policy adopted in the 1-1 section above. Any number is a pointer, not a fact. Everything below was found while wiring the completion wake and the session-cost rollup, and was deliberately **not crossed** — the record-do-not-cross protocol, now on its seventh run._

### Recorded and not crossed (each with a named owner)

- **`pendingUltraWakes` scans EVERY ultra run directory ever created, on every chat turn** — `packages/core/src/ultra/wake.ts`, `pendingUltraWakes`. *Measured, not estimated:* it calls `listUltraRuns()` (`packages/core/src/ultra/storage.ts`), which is `fs.readdirSync(ultraDir())` over the whole `TELAR_HOME/ultra/` tree — not scoped by session, not scoped by project — and then `getUltraManifest(id)` on each, which is a `JSON.parse` **and, for a stale `running` manifest, a `saveManifest` WRITE**. Nothing reaps that directory. Because the system-prompt appendix composes on every chat POST for every session (`apps/web/lib/session-prompts.ts`, `buildUltraWakeContext`), every turn in the app is therefore a full historical scan, with incidental self-healing writes to unrelated runs. **This cost was ACCEPTED for 4.1 rather than missed**, and it is stated in a comment above the function so the next reader meets it as a known bound: at a developer's run volumes the scan is small, and the self-heal write is the reconciliation AD-15 wants anyway. The mitigation that IS in place is the short-circuit — a call with no `sessionId` returns before touching the filesystem. *The real fix:* a `sessionId -> runIds` index under ultra's own subtree, which is a storage design this story did not need. **Owner: story 4.2** — its dock run signal needs the same session-scoped question answered from every page, so it is the first story with a reason to pay for it.
- **There is no appendix-CONTRIBUTOR registry, so a track that needs per-turn context edits Track B's files directly** — `packages/core/src/session-profile.ts`, `SessionResolutionContext`; `apps/web/lib/session-prompts.ts`; `apps/web/lib/session-profiles.ts`. Story 4.1 needed per-turn context (AC2's "the next turn already knows") and the system-prompt appendix is the only per-turn channel that exists, so it added one optional field to the context type and threaded it through three composers and three builders. That is **four disclosed widenings** where the clean end state is one seam: a registry in core that any module can contribute an appendix fragment to, resolved by the fold, so a new contributor edits nothing it does not own. *Why not built here:* designing Track B's port from inside a Track D story is a larger mistake than four disclosed lines, and 4.1 is the first module to want it — one caller is not a port. **Owner: epic 5's project-less master profile (5.3)**, which needs the same channel and is the first story with a second caller to design against. Note the field's own in-source comment already carries this forward pointer.
- **The chat route's OWN per-turn `logUsage` row still carries no `messageId`** — `apps/web/app/api/chat/route.ts`, the `logUsage({...})` call in the teardown block. `runId` is in scope there and adding it is literally one line, so it is tempting. It was deliberately **not** done: FR-UW-5 is about *a run's* spend, `ultraCostByMessage()` deliberately answers only for `ownerKind: "ultra"` rows, and changing the shape of the per-turn session row is changing the row story 1.1 spent four repair rounds stabilising — for a consumer that does not exist yet. *Consequence today:* `ultraCostByMessage(turnId)` answers "what did the runs this turn launched cost", never "what did this turn cost", and its doc comment says so. **Owner: the first story that needs a per-turn cost SPLIT on screen, most plausibly 4.2's run anchor** (`ui-contract.md` §2 puts the per-run `$`/tokens readout there).
- **A woven ROOT loom's own attempts are still never billed** — `packages/core/src/weave.ts`, `recordSpend`. Story 4.1 was asked to rule on this and **rules it OUT, deliberately**. The record is in `stories/1-1-state-root-isolation-and-the-attributed-spend-ledger.md` § *"Still open at the close of this round, deliberately"* — and **the FINDING was not in this file before now**. **⚠ CORRECTED in story 4.1's review-fix round (SF-13): this originally read "(grep confirmed: `recordSpend` appeared nowhere in it)", and the grep does not reproduce.** `git show 9a94632:_bmad-output/implementation-artifacts/deferred-work.md | grep -c recordSpend` → **1**: the 1-1 section's ledger-retention item names *"the loom leg's `recordSpend` (`packages/core/src/weave.ts`, called at both settle sites inside `runWeave`)"* while discussing row volume. The SYMBOL was here; the root-loom-unbilled FINDING was not, and the ruling below rests on the finding. Three independent reasons it is not 4.1's: (a) it is `ownerKind: "loom"` — a different owner with a different consumer (a charter's budget-left) — and no AC in 4.1 mentions loom spend; (b) `weave.ts` is not in Track D's write set and fixing it means editing the tick loop's own budget input; (c) story 1.1's own record says *"it changes what a charter's budget-left means and is the human's call"*, and an unresolved owner decision is not something a Track D story settles silently while doing something else. **Owner: whichever story next opens `weave.ts`'s spend path, most plausibly epic 6's F3 (`LR CAP-14` park/resume, which reads the same budget).**
- **`usageSummary()` / the sidebar+dashboard account windows still exclude loom and ultra rows** — `packages/core/src/usage-ledger.ts`, `usageSummary` and the `ownerKind !== "session"` filter in `foldLine`. Story 4.1 widened **exactly one** readout — `apps/web/lib/store.ts`'s `sessionSpendUsd`, the per-session display — and asserts `usageSummary()` byte-identical (`packages/core/test/usage-ledger.test.ts`, "4.1 AC5 proof 5"). Widening the account windows is an unresolved `[Review][Decision]` on story 1.1 and is the human's call. **Owner: the human, then whoever implements the decision.**
- **The wake's fast path is not installed until something ASKS about wakes in that process** — `packages/core/src/ultra/wake.ts`, `ultraWakeChannel`; `packages/core/src/ultra/storage.ts`, the publish in `run.finished.then`. The publish goes through `ultraEvents()` (the declaration only); the RECORDER is attached by `ultraWakeChannel()`, which `pendingUltraWakes`/`ackUltraWakes` call. This is deliberate: having `storage.ts` import `wake.ts` would make `storage <-> wake` a module cycle (wake reads manifests through storage), and the alternative — a registration seam in `events.ts` filled at `wake.ts`'s module scope — depends on import order in a way a direct `import "./storage"` in a test would defeat. *Consequence:* in a process where nothing has asked about wakes, a terminal run's publish reaches zero wake subscribers and `wake.json` gets no `recordedAt`. **It costs a stamp, never a wake** — pending is derived from the manifest, so the run is still reported. Verified live on the dev server: the chat route asks on every turn and `GET /api/ultra/wakes` asks on every poll, so the recorder was installed and `recordedAt` was written for both proof runs. *Fix when it bites:* if a surface ever needs the stamp to be reliable, give `events.ts` an installer seam and have `ultra/index.ts` be the thing that fills it. **Owner: whichever story first needs `recordedAt` to mean something.**
- **A wake acked pre-stream is consumed even if the turn then fails mid-stream** — `apps/web/app/api/chat/route.ts`, the `ackUltraWakes` block. Observed live during 4.1's dev proof: a chat POST whose `query()` failed (the SDK's darwin-arm64 native CLI is not installed in this checkout) still consumed both pending wakes, because the ack sits after the capability gate and before `registerChatRun` — the window §5.5-D10 fixes. That ordering is correct and the alternative is worse: acking after a successful stream re-delivers the same outcome on every abort, every disconnect and every mid-turn error, which is the double-statement AC7 exists to prevent. *Consequence — CORRECTED in story 4.1's review-fix round (SF-6), because the first version of this sentence downgraded the item using a premise the story's own Debug Log contradicts.* It said "the appendix was composed and handed to the model, so this is a lost RENDERING, not lost accounting". **How bad it is depends on where the turn died, and the observed failure was the worse end of that range.** The ack sits above `registerChatRun`, and `query()` is called far below it, so the mode actually observed — the turn dying on the missing SDK binary — meant **no model call ever happened** (§9 Debug Log (3) says exactly that) and the outcome was delivered **zero** times while the durable record says delivered. A turn that dies *after* the model has read its prompt really is a lost rendering. Read this item as covering both, and do not deprioritise it as UI polish. *The tail that IS unconditionally true:* accounting is untouched — the run's manifest and its ledger rows are unchanged and `ultra_status` still answers (AC3). *Narrowed, not closed, by the same review round:* the ack now fires only for runs the composed appendix demonstrably carries (`appendixCarriesUltraWake`), so a failed composer read or a provider that discards the system prompt no longer consumes anything (SF-3). The window between the ack and the model reading the prompt remains. **Owner: whichever story next wants at-least-once delivery rather than at-most-once; it needs a two-phase ack (claim, then confirm on stream close) and that is a route-lifecycle change, not a wake change.**

- **A throw from the TERMINAL `saveManifest` is silently swallowed, and the run is then unreachable until the process restarts** — `packages/core/src/ultra/storage.ts`, the `run.finished.then(...)` block and its trailing `.catch(() => {})`. *Measured by reading the path, found by adversarial review of story 4.1:* the terminal `saveManifest(...)` is **not** wrapped, and `saveManifest` does `JSON.stringify(m, null, 2)` over `result.result` — an Ultra script's arbitrary default-export return value. A returned `BigInt` or a circular reference makes that throw **deterministically**, and ENOSPC/EROFS/EACCES do too. The throw escapes the `.then()` into the trailing `.catch`, whose own comment says it is *"defense-in-depth only, never expected"* — but this is exactly the path that reaches it, and it discards the error with no log and no event. The run keeps a `running` manifest **and** its live registry entry (`registry()` has a `.set` and **no `.delete` anywhere in the file**), so `getUltraManifest`'s self-healing `running` → `stopped` rewrite cannot fire either: the run reads as perpetually running for the life of that process. *Consequence for story 4.1:* the completion wake is unreachable for that run until a restart — the one hole in `wake.ts`'s "delivered late, never never", and it is stated in that file's header rather than glossed. *Why not fixed here:* it is **pre-existing** (the `.then`/`.catch` shape predates this story; 4.1 only added a publish inside it), and fixing it means changing `saveManifest`'s error contract and probably giving the registry a `.delete` — both well outside a story whose mandate in this file was "add a publish and one field". **Owner: whichever story next opens ultra's terminal write path** — most plausibly 4.2, which needs Stop/Resume UI and therefore has the registry lifecycle in its write set.
- **`wake.json` is read-decide-write with no CAS, so two PROCESSES on one state root could lose an update** — `packages/core/src/ultra/wake.ts`, `saveWakeRecord` / `recordUltraWake` / `ackUltraWakes`. Within one process this is safe by construction: all three are fully synchronous with no `await`, so no interleaving is possible. Across processes it is not, and two processes on one root is a case this repo explicitly contemplates (`usage-ledger.ts`'s header names "a dev server and the packaged app appending to one root concurrently"). *The failure:* a late-landing `recordUltraWake` write computed from a stale read could carry an older `deliveredAt` forward over a concurrent `ackUltraWakes` write, making an already-stated wake pending again. *Why it is tolerable and not urgent:* the direction is the safe one — the outcome is **stated twice**, never lost — which is exactly the tolerance this module documents for the two-turns-at-once case. `apps/web`'s deployment is an in-process monolith (`project-context.md`: "App-Router-as-backend, in-process monolith"), so the window needs the packaged app and the dev server pointed at one root simultaneously. **Owner: whichever story first supports two live processes on one state root** — the same story that would need it for the ledger.

- **`packages/core/src/event-bus.ts`'s header now carries a FALSE second clause, and story 4.1 is what made it false** — `packages/core/src/event-bus.ts`, the final paragraph of the module header. It reads, verbatim and unedited at HEAD: *"This module declares NO concrete events. Which events a module publishes is owned by that module's own epic — AD-21 is precisely what makes deferring the catalogue safe. The only event names in this repo today are fixtures in event-bus.test.ts."* The first clause is still true **of that module**. The last sentence is **now false**: `packages/core/src/ultra/events.ts` is a production `declareEvents("ultra", …)` call and `ultra:run-completed` is a real event name. *Why not fixed here:* `event-bus.ts` is explicitly fenced out of story 4.1's write set — the story's §0 says *"you are its **first production caller**. You call `declareEvents`; you do not change it"* — and a one-line comment edit is still an edit to a fenced file, which is exactly the kind of drive-by the record-do-not-cross protocol exists to stop. **THE FLAG IS ONE-DIRECTIONAL, and this item claimed otherwise until story 4.1's review-fix round (SF-7).** It said the stale sentence "is flagged in `ultra/events.ts`'s own header, so a reader who arrives from either side meets it". Only one side is covered. `git diff 9a94632..HEAD -- packages/core/src/event-bus.ts` is empty, that file references `ultra/events.ts` nowhere, and its only two `ultra` occurrences are pre-existing illustrative strings (the `<module>:<past-tense-fact>` example on line 70 and the segment-shape error text on line 148). **The un-flagged side is the one that misleads**, which raises this item's urgency rather than lowering it: the reader who matters is the maintainer who opens `event-bus.ts` to declare the SECOND module catalogue — this item's own predicted owner. They read "The only event names in this repo today are fixtures in event-bus.test.ts", conclude theirs is the first production declaration, and write a module-scope `declareEvents` call — reproducing exactly the T2 trap `ultra/events.ts`'s self-healing accessor exists to solve. *Fix:* delete or amend that one sentence. **Owner: whichever story next opens `event-bus.ts` for a real reason** — most plausibly epic 5 or 6, whichever module declares the second catalogue. The fence still holds: 4.1's review round deliberately did not cross it to make a one-line comment edit.

### An environment note, not a repo finding

- **No live model turn is possible in this checkout: the Agent SDK cannot find its `darwin-arm64` native CLI.** *Measured during 4.1's dev-server proof:* `ls apps/web/node_modules/@anthropic-ai/` returns only `claude-agent-sdk`, with no native/darwin sibling package, and both an Ultra child `agent()` call and a `POST /api/chat` turn fail with `Error: Native CLI binary for darwin-arm64 not found. Reinstall @anthropic-ai/claude-agent-sdk without --omit=optional, or set options.pathToClaudeCodeExecutable.` **⚠ DIAGNOSIS CORRECTED in the review-fix round: the package is NOT missing, it is UNLINKED.** Both the story and the code review recorded it as "not installed in this checkout", and the tree says otherwise: `node_modules/.bun/@anthropic-ai+claude-agent-sdk-darwin-arm64@0.3.204/node_modules/@anthropic-ai/claude-agent-sdk-darwin-arm64/` exists and holds an executable `claude` beside its `package.json` — `stat` reports `236961904` bytes, mode `-rwxr-xr-x`, mtime `2026-07-25 18:00:12`. What is missing is a link from that store entry into a directory the SDK's resolution can reach — the same install-layout shape as the `shiki` note above, which is the closed item directly preceding this one. That matters for the remedy: this is a **linking** problem to diagnose, not necessarily a re-download. This is **pre-existing and unrelated to story 4.1** — the whole test suite is green because every suite injects a fake agent through the DI seam. *What it blocked:* the four clauses of 4.1's dev-server proof that need a real assistant turn to render (the unprompted turn's text, the no-user-bubble check across a reload, the heartbeat readout changing, and the mid-conversation case), plus the `messageId` ledger row in the dev root (no settle means no ledger row). Story 4.1's Debug Log records exactly which clauses were proved live, which were proved by test instead, and which were not proved at all. **No owner — environment/tooling.** *Fix:* `bun install` without `--omit=optional` in `apps/web`, or set `pathToClaudeCodeExecutable`, or — given the corrected diagnosis — link the store entry named above into `apps/web/node_modules/@anthropic-ai/`. **Neither the review nor the fix round did any of these**, deliberately and for the same reason: mutating `node_modules` unattended risks the green gate each had just certified, it is outside the repo's tracked state, and it is the operator's environment to decide about. *What it is still worth:* it is the single blocker between AC1's four unproved clauses and a resolved acceptance, and once it is closed everything they need is reachable in one dev-server session.

### Left behind in the DEV state root by 4.1's dev proof (not the real one)

- **`~/.telar-dev/projects.json` and `~/.telar-dev/ultra/{u-ce023fe79cb5,u-534fd3ef96f8,u-devproof-crashed,u-fixround-a,u-fixround-bad}/`** were created by story 4.1's dev-server proofs — the last two by the **review-fix round**'s re-run of the ack leg (`u-fixround-a`, a planted terminal manifest now carrying a `wake.json` with `deliveredTerminalAt`) and its B1 guard proof (`u-fixround-bad`, a `running` manifest with no `runId` key, deliberately malformed). **Note `u-fixround-bad` makes `pendingUltraWakes` throw for EVERY session against this root**, which is the whole point of it and is now harmless — both routes catch it — but it will keep the dev root's wake poll answering empty until it is deleted. None could be removed: the agent's sandbox denies deletions under `$HOME`. They are in the **dev** root, which is exactly what `apps/web`'s `dev` script isolates (`TELAR_HOME="${TELAR_HOME:-$HOME/.telar-dev}"`), and the operator's real `~/.telar` was verified byte-identical by content hash before and after **each** proof — `accounts.json` = `c7436b2a…3a22` and `usage.ndjson` = `afea3563…0170`, the same two files with the same two hashes at the close of the fix round, and still no `ultra/` directory under the real root. Recorded rather than left silent, because story 1.1's synthetic billing line in the REAL root is still there and the lesson was to say so. *Cleanup, if wanted:* `rm -rf ~/.telar-dev/ultra ~/.telar-dev/projects.json`. **Owner: the operator, at their convenience — nothing depends on it.**

---

## Deferred from: code review of story-4.1 (2026-07-26)

_Two items the unattended code review raised and deliberately did not turn into patch work. Everything else it found is a `[Review][Patch]` or `[Review][Decision]` bullet in the story's §4 Review Findings section. Citations name a file and a symbol, per the policy adopted in the 1-1 section above._

- **The wake appendix is bounded per run but not in the NUMBER of runs** — `apps/web/lib/ultra-wake.ts`, `formatUltraWakeAppendix`. `MAX_OUTCOME_CHARS` clips each run's rendered outcome at 1200 characters, which is the bound the module documents; nothing caps the length of the list itself, and the block rides **every** turn's system prompt until a turn acks it. A session left unvisited while many runs settle therefore composes an unbounded block into the next turn's context. *Why it is tolerable today:* `packages/core/src/ultra/executor.ts`'s `RUN_CONCURRENCY = 3` bounds how many runs can be in flight at once, and the ack fires on the very next turn, so the list is short in every reachable case at a developer's volumes. *Fix when it bites:* cap the list and render an "and N more" tail, the same shape any bounded readout needs. **Owner: whichever story first renders a bounded wake list — most plausibly 4.2, whose run anchor needs the same list from the same projection.**
- **INV-7's reader surface and its `INJECTED_READER` predicate do not know about story 4.1's `sessionId` / `readWake` seam** — `packages/core/test/invariants.test.ts`, `STATE_ROOT_READERS` and `INJECTED_READER`. Story 4.1 gave `projectAppendix`, `plannerAppendix` and `steererAppendix` — and through them `buildProjectProfile` / `buildPlannerProfile` — a second transitive route to the state root: `sessionId` → `ultraWakeAppendix` → `buildUltraWakeContext` → `pendingUltraWakes` → `listUltraRuns` → `getUltraManifest`, which **writes** (it re-saves a stale `running` manifest as `stopped`). **⚠ TWO CORRECTIONS to this item, made by story 4.1's review-fix round after its own adversarial pass refuted the wording it had just copied from here into INV-7's header.** (i) It said `STATE_ROOT_READERS` "names none of those symbols" and prescribed adding "the six". The list names **five** symbols, and one of them — `steererAppendix` — **is** already on the surface (so is `buildSteererProfile`), both added for the `loomId` chain. **Four** are genuinely absent: `projectAppendix`, `plannerAppendix`, `buildProjectProfile`, `buildPlannerProfile`. As written, the prescribed fix would re-add a name already present and under-count the surface. (ii) It said the hazard is latent because "every call in the tree today that carries a `sessionId` also injects `readWake`" — **false, and it credits the wrong guarantee**, which is the more damaging half: the `sessionId`-carrying calls in `session-profiles.test.ts` inject nothing and are safe because they run inside the **sandboxed child probe** (`HOME` and `TELAR_HOME` both thrown away). Injection is what covers the in-process calls, all of which are in `session-prompts.test.ts`; the one exception there calls `escalationAppendix`, which never composes the wake block and is safe by signature. `INJECTED_READER` (`/(?<![A-Za-z0-9_$.])read\s*:/`) cannot match `readWake:` and does not require it. **This is LATENT, not live** — by those two mechanisms rather than one — and `~/.telar` was verified untouched by content hash across all eight of the story's test surfaces. The hazard is that INV-7's header still tells the next implementer those calls are safe *"only because that file's `ctx()` helper never sets `loomId`"* — so adding `sessionId` to that helper reads as harmless and would silently point dozens of in-process calls at the operator's real root, with `safeLiveContext` swallowing the evidence. *Why it was not fixed in 4.1:* the story's §0 write-set row for that file reads **"Append INV-9 only"**, and hard rule 2 pre-declares the by-name blindness as an accepted property rather than a defect — the implementer honoured it with a child probe and an explicit "NOTE ON INV-7" paragraph in `session-profiles.test.ts`. *The real fix:* add the **four** absent symbols to `STATE_ROOT_READERS` (`projectAppendix`, `plannerAppendix`, `buildProjectProfile`, `buildPlannerProfile` — the other two are already there), require a `readWake:` whenever a scanned call carries a `sessionId:` (a second predicate beside `INJECTED_READER`, exercised in both directions like every other arm), and amend the header sentence to *"never sets `loomId` **and** never sets `sessionId`"*. **PARTIALLY CLOSED by review-fix round 1 (2026-07-26): the header sentence is amended** — it now names both ids and spells out the whole `sessionId → … → getUltraManifest` chain, the six unlisted symbols, and why `INJECTED_READER` cannot see `readWake:`. **The GUARD is untouched and this item stays open for it**, because widening `STATE_ROOT_READERS` and adding the second predicate is real work behind the "Append INV-9 only" fence, not a comment. `bun test packages/core/test/invariants.test.ts` → 71 pass / 0 fail after the amendment. **Owner: unchanged — whichever story next has `packages/core/test/invariants.test.ts` legitimately open, most plausibly 4.2, which will add the run-anchor item kind and therefore touches INV-8's surface anyway.**

---

## Deferred from: story-4.1 REVIEW-FIX ROUND 1 (2026-07-26)

_The review returned **CHANGES-REQUIRED**: 3 blocking, 13 should-fix, 2 nice-to-have, 2 deferred. **The fix round closed all 3 blocking and all 15 should-fix/nice-to-have items.** What is below is what it deliberately left open, each with its finding id and why. Citations name a file and a symbol, per the policy adopted in the 1-1 section above._

- **B3 (the load-bearing half) — four of AC1's proof clauses have no evidence of any kind, and the fix round could not produce it.** The unprompted assistant turn rendering, the no-user-bubble check across a page reload, the heartbeat readout changing against `manifest.spend`, and the mid-conversation case. *What the fix round DID do:* it re-ran the ack leg live at HEAD (closing SF-12), demonstrated B1's guard live with a discriminating probe, and — the substantive part — **lifted the two client-side DECISIONS out of `apps/web/components/session/session-view.tsx` into `apps/web/lib/ultra-wake.ts` (`freshUltraWakes`, `appendixCarriesUltraWake`) so tests could execute them**, which is where both of the review's client-chain defects lived and why both were found by reading rather than by running. *What it did NOT do:* render a turn. That needs a live model call, which needs the SDK native CLI (see the corrected environment note above — the package is present in the bun store and merely unlinked, which the story and the review both mis-recorded as "not installed"). §9's Debug Log (3-ter) now records **AC1 as proved by construction rather than observed**, naming every unproved clause, so an acceptance cannot silently absorb them. **Owner: the operator**, for the environment; then whoever accepts AC1, who must accept it on that stated basis or not at all.
- **B1's underlying cause is untouched — only its two call sites are guarded.** `packages/core/src/ultra/storage.ts`, `getUltraManifest`'s self-heal `saveManifest(m)`, which is an unwrapped write inside a function whose whole contract is *"never a 500 (looms.ts idiom)"*, reached from a `listUltraRuns` loop whose own `try` covers only the `readdirSync`. Reproduced twice in a sandboxed probe on a temp root: a `running` manifest with no `runId` key throws `invalid ultra runId: undefined`, and a stale `running` manifest whose heal write cannot land throws that write's errno — **both for a session that has never touched Ultra**, because the scan is not session-scoped. The fix round wrapped both callers (`apps/web/app/api/chat/route.ts`'s ack block and `apps/web/app/api/ultra/wakes/route.ts`), which is what stops the app-wide 500; it did not change `getUltraManifest`. *Why not:* `storage.ts`'s error contract is fenced out of story 4.1, and the honest fix is the same one the swallowed-terminal-`saveManifest` item above needs — so they should be done together by one owner rather than half each. *The real fix:* make the self-heal write best-effort and decide explicitly what a failed heal means to a reader. **Owner: the same as the swallowed terminal `saveManifest` — whichever story next opens ultra's terminal write path, most plausibly 4.2.**
- **SF-2 is fixed on the client only; the route still runs a wake turn for an empty mailbox if asked.** `apps/web/components/session/session-view.tsx`, the §6.D drain (fixed: a dequeued `ULTRA_WAKE_SENTINEL` is dropped when `pendingWakes` is empty) versus `apps/web/lib/ultra-wake.ts`, `isUltraWakeTrigger` (unchanged: `!!sessionId && message === ULTRA_WAKE_SENTINEL`, consulting no state). The review offered the two fixes as alternatives and the client one was taken, because it needs no change to the streaming contract. *What remains:* a client that POSTs the sentinel with nothing pending still gets a turn instructed to summarise a "COMPLETED ULTRA RUNS block" that the appendix will not contain. It cannot make the agent state an outcome that did not occur — the facts are server-composed and the ack is now gated on the appendix actually carrying the run — so the residue is a turn that talks about nothing, not a fabricated outcome. *The real fix:* a pre-stream no-op response for a recognized sentinel with `pendingUltraWakes(sessionId).length === 0`, which needs a decision about what shape a no-op takes on a route whose only success path is an SSE stream. **Owner: whichever story next opens the chat route's pre-stream response shapes — plausibly the same one that takes the two-phase ack above, since both are route-lifecycle work.**
- **`appendixCarriesUltraWake` can still be answered by a DELIBERATELY forged bullet line** — `apps/web/lib/ultra-wake.ts`, `appendixCarriesUltraWake` / `runMarker`. The route's SF-3 delivery gate asks the composed prompt "did you carry run X?" by looking for `(run X)` on a line that begins with the formatter's bullet. Line-scoping closes the ACCIDENTAL case — the fix round's own verification found that a plain `includes` let a run whose *result text* merely printed `(run u-other)` mark `u-other` as carried, and there is a named test for both that and the name-field variant. What line-scoping does not close: an Ultra script whose result contains a whole forged bullet line, `\n• x (run u-other) — done`. *Consequence:* one wake acked without being carried — delivered zero times, the case the gate exists to prevent — for an author who already controls a run and also knows a sibling run's id. *Why it is not closed here:* the only forgery-proof answer is a DATA channel from the composer back to the route, and story 4.1 rejected exactly that on the grounds that `SessionProfile` has no field for it and every appendix composer returns a plain `string` (§9 Debug Log's "A DELIBERATE SECOND READ" reasoning). That is the same seam the appendix-contributor-registry item above wants. **Owner: whichever story builds the appendix-contributor registry — epic 5's 5.3 by that item's own assignment**, since a registry that knows which fragments it composed answers this for free.
- **`apps/web/lib/use-accounts.ts:26` carries the same `react-hooks/set-state-in-effect` error, un-suppressed.** It is the file §5.3 item 8 and §5.5-D9 name as `use-ultra-wake.ts`'s template, it has carried the error since before `9a94632`, and the fix round suppressed the finding **only** in `use-ultra-wake.ts` — the file 4.1 created and therefore owns. *Why not both:* editing a file in no story's write set to make a lint baseline tidier is precisely the drive-by the record-do-not-cross protocol exists to stop. *Measured, so nobody re-derives it:* removing the hook's synchronous no-session branch does **not** clear the rule; it fires on the effect body calling a function that transitively calls setState at all. So the two files now differ by one suppression comment and nothing else. *Fix when it bites:* decide once, for the repo, whether this rule is right for the `useEffect(() => void asyncReload())` idiom — there are two instances and the answer should be the same for both. **Owner: whichever story next opens `use-accounts.ts`, or a deliberate lint-baseline pass.**
- **Two sentences in `orchestrator-run-log.md` no longer hold, and the fix round deliberately did not edit that file.** (i) Its dev-root note says `~/.telar-dev` holds *"three run directories and a `projects.json`"*; `ls ~/.telar-dev/ultra` now returns **five** (`u-534fd3ef96f8`, `u-ce023fe79cb5`, `u-devproof-crashed`, `u-fixround-a`, `u-fixround-bad`) and the root also holds `accounts.json`. **The fix round's own dev proof is what made that count stale**, so it is flagged here rather than left silent; the cleanup note above already lists all five correctly and is the authoritative record. (ii) A standing warning in the same file says *"Until fix `2.2/B1` lands, do not run `bun test` on a machine whose `~/.telar` has the legacy `runs/` layout"*, citing `apps/web/lib/session-prompts.test.ts:175` as executing a real loom read — but that file's own table records `2.2/B1` as PASS, closed structurally in commit `ecea54d`, and line 175 today is an ordinary `expect(steererAppendixNoId(...))` with no such call. The fix round ran `bun test` many times against a `~/.telar` verified byte-identical by content hash before and after, so the warning is stale in practice as well as on paper. *Why neither was edited:* `orchestrator-run-log.md` is the orchestrator's own bookkeeping, it was already dirty before this round started, and both the code review and this round excluded it from scope — silently rewriting another process's log is a worse habit than reporting it. **Owner: the orchestrator, on its next pass.**

## Deferred from: 4-2-the-real-session-ui-composer-chip-dock-signal-and-authoring-reference (2026-07-27)

### The SIX items §3 required this story to rule on, ruled on rather than inherited

Re-derived at implementation time with `awk 'NR>=129' deferred-work.md | grep -n '4\.2'` → six hits (this file's lines 135, 137, 143, 162, 163, 172). A seventh, at line 98, comes from story 3.1 and is ruled on below with them.

- **The `sessionId → runIds` index under ultra's subtree — OPEN, with an honest PARTIAL.** Story 4.2 shipped a `?sessionId=` filter **on the server** (`apps/web/app/api/ultra/route.ts`, calling `filterRunsBySession` from `@/lib/ultra-runs`). That bounds the payload and moves the filtering off the client; **it does NOT remove the directory walk**, which is what the recorded item is actually about. The index itself is a storage redesign, it is not needed to make any of this story's ACs true at a developer's run volumes, and the dock's cadence is a slow poll (8s) rather than a per-turn path. **Do not read this as closed. Owner unchanged: whichever story next finds the walk too expensive, most plausibly epic 6's fleet surfaces, which ask the same shape of question across many looms.**
- **The chat route's own `messageId`-less `logUsage` row — OPEN, and this story is NOT its owner.** The item names "4.2's run anchor" as the plausible owner because `ui-contract.md` §2 puts a spend readout there. The anchor this story shipped renders **the run's own spend, from `manifest.spend` — one run, one number, no split** — so the asymmetry the item describes never surfaces on this surface: `ultraCostByMessage()` answers "what did the runs this turn launched cost", and nothing here asks the second question. `INV-5b` pins `logUsage` to three call sites and this story added none. **Owner: the first story that puts BOTH figures beside each other in one readout.**
- **The swallowed terminal `saveManifest`, and `getUltraManifest`'s unwrapped self-heal — BOTH OPEN.** Both are about `saveManifest`'s **error contract**, and changing it means deciding what a failed heal means to every reader and giving the run registry a `.delete` it does not have. This story's `storage.ts` edit is one new reader (`listUltraAgentOrdinals`) and nothing else; folding an error-contract change into it would make a UI story the owner of ultra's durability semantics. **What this story DID do:** every route it added or edited that lists or reads runs is now wrapped exactly as `apps/web/app/api/ultra/wakes/route.ts` wraps its call — `GET /api/ultra` (which was **not** wrapped and did 500), `GET /api/ultra/[id]/agents`, `GET /api/ultra/[id]/script`. **That guard was measured firing during this story's dev proof** (see the dev-root note below), which is the strongest available evidence the underlying item is real. **Owner unchanged: whichever story next opens ultra's terminal write path deliberately.**
- **Bounding the wake appendix's run LIST — OPEN, and this story is not its owner.** The item names 4.2 because its anchor "needs the same list from the same projection". It does not: the rail's list is bounded by the `?sessionId=` filter and by the DOM, while `formatUltraWakeAppendix`'s `MAX_OUTCOME_CHARS` is a **prompt** bound with a different consumer (the model's context window). Bounding one does not bound the other, and folding a prompt-budget change into a UI story would make this story the owner of what the model reads. **4.2 rendered a list and deliberately did not bound the appendix's. Owner unchanged.**
- **INV-7's four missing reader symbols and the `readWake:` predicate — OPEN, and only the free half was done.** The item names 4.2 as owner "which will add the run-anchor item kind and therefore touches INV-8's surface anyway" — and this story does touch INV-8's surface. But the recorded fix is to `STATE_ROOT_READERS` and `INJECTED_READER`, which is **INV-7's** surface, and this story's write-set fence on that file reads "INV-8b2's scan set plus INV-10, and nothing else". **The half that was done:** every new test call in this story satisfies INV-7 by a sanctioned mechanism, and INV-7 caught one that did not — `steererAppendix({ ultraAnnotated: false, provider })` in `session-prompts.test.ts`, added by this story with no `read:` injection. It was a genuine by-name violation even though its live branch was unreachable (no `loomId`), and it was fixed by injecting rather than by exempting. **Is any of this story's calls one INV-7 cannot see? Yes, and it is named here rather than left implicit:** `apps/web/lib/ultra-runs.test.ts` and `ultra-authoring.test.ts` touch no state-root reader at all, and `session-profiles.test.ts`'s new 4.2 assertions go through `resolveSessionProfile`, which reaches `buildProjectProfile`/`buildPlannerProfile` — **two of the four symbols still absent from `STATE_ROOT_READERS`**. They are safe by that file's sandboxed-child idiom, not by the guard. **Owner unchanged.**
- **`session-runtime-host.tsx`'s `applyLiveEvent` rebuild, and the dock's dropped typed text (3.1's items) — OPEN.** This story did **not** rebuild the dock's transcript. It added two fields to `Runtime` (`ultraSummary`, `ultraFocusRunId`), one line to the head's tooltip, one badge on the head, and a query param on the tap. `applyLiveEvent` is untouched. **Owner re-stated: whichever story next rebuilds the dock's transcript.**

### Recorded and not crossed (each with a named owner)

- **INV-8b2 still cannot follow an identifier into a top-level function — and THREE MORE SHAPES defeat it, none of them documented before this story.** `packages/core/test/invariants.test.ts`, `kindRendererSlices` / `KIND_DONOR_RELS`. The extractor brace-matches from a `: ItemKind<…> = {` declaration, so **four** shapes defeat it and **three of the four leave the invariant GREEN** (its anti-vacuity floor was satisfied by `agentBucketKind` alone and its `toContain` still held):
  1. `render: someTopLevelFn` — a slice IS produced and holds no hook-call text. Documented in-source before this story; **still open**.
  2. `const k: ItemKind = { … }` — the pattern requires the generic `<`, so a bare annotation yields **no slice at all**. Undocumented before this story; **still open**.
  3. `const k = { … } satisfies ItemKind<P>` — no `: ItemKind<` annotation exists, so likewise **no slice**. Undocumented before this story; **still open**.
  4. A file outside the scan set, which was **exactly one file** (`session-view.tsx`). **CLOSED for the ultra anchor only**, by widening `KIND_DONOR_RELS` to two rels with a per-file floor, a failure message naming every rel in the set, a two-direction discriminator (INV-8b2b) and a whole-file `ambientContextScan` pass (the import arm is structurally inert on a slice). It is **not** closed for a future kind registered in a third file.
  All four are now **executably documented** by `INV-8b2c`, which asserts what the extractor does for each shape separately — so the residual can no longer be described wrongly, and the day someone closes one, that test fails and tells them to update this record. *The real fix for 1–3:* follow the identifier into the named function and scan its body, and accept `satisfies`/bare-annotation forms in the DECL pattern. **Owner: whichever story next registers an item kind, or whichever next has `invariants.test.ts` legitimately open.**
- **The `human-facing` delivery class is still exercised by no production event, after this story.** `packages/core/src/event-bus.ts` and ultra's catalogue. `ARCHITECTURE-SPINE.md` assigns "UW CAP-6 dock signal | event bus, human-facing class → dock aggregator", and story 4.2 deliberately did **not** build it that way: the dock is a **client** and cannot subscribe to an in-process bus, so something must poll regardless; `INV-9a` pins ultra's catalogue as the exact set `["ultra:run-completed"]`, so a second name is a contract change that buys nothing this story needs; and NFR-X-15 already fixes the client pattern as poll-plus-refetch, with `LoomNotifications` as the shipped precedent. **This story added no declared event.** **Owner: epic 5's workspace, whose "never initiates contact" is the case that class exists to describe.**
- **The tone tokens are still not lifted anywhere Track-neutral, and there is now a THIRD copy.** Story 3.1's `NH-11` declined to import `components/looms/status.tsx` into a presentational primitive and recorded the right fix as lifting the tone pair somewhere shared. Story 4.2 followed that precedent rather than opening a new edge — `components/session/**` still has **zero** imports from `components/looms/**` — and therefore duplicated two Tailwind expressions into `apps/web/components/session/ultra-anchor.tsx` as `ULTRA_TONE_CLASS`. `ultra-rail.tsx` imports them from there rather than making a fourth copy, so the count inside this story is one. **Owner: whichever story lifts the tone tokens; this file's `ultra-anchor.tsx` copy joins 3.1's item rather than starting a new one.**
- **`spliceRunAnchors` takes a THIRD optional argument the story's §5.5-D3a signature does not name.** `apps/web/lib/ultra-runs.ts`. D3a pins `spliceRunAnchors(items, runs)`, and D8 separately requires that "a run whose manifest has not arrived yet renders the anchor in a pending form rather than dropping it". With only the two pinned arguments those cannot both hold: the map is keyed by runId and the splice is the only thing that DISCOVERS a runId, so an unknown launch has no payload to render. The third argument is an optional `pending?: (runId) => UltraAnchorPayload` factory supplied in the same wrapping call, so `session-view.tsx` still spends exactly one expression. Both arities are tested. **Not a defect and not deferred work — recorded because a reviewer comparing the shipped signature to D3a will otherwise read it as drift.**
- **`DockRunSummary` carries a `state` field D3a does not name.** Same file. AC7 proof 4 requires the head to render `name · state · spend`, and D3a's `{ text, spend, focusRunId? }` has nowhere for the state. It is set only in the single-run case (where it is always `"running"`, since the summarizer considers only live runs) and is `undefined` for N>1, where there is no one state to name. **Recorded for the same reason as above.**
- **`filterRunsBySession` is generic over `{ sessionId?: string }` rather than over `RunSnapshot`.** Same file. D3a's signature names `readonly RunSnapshot[]`, but the whole point of C1 is that **the route** — which filters `UltraManifest[]` — is a three-line caller of the same tested function. A generic constrained to the one field the rule reads satisfies both callers with one rule. **Recorded, not deferred.**

### The T12 ruling (a permission event and a pinned Workflows view)

`session-view.tsx` forces `setActiveTab("main")` on every `"permission"` event. **Story 4.2 changed nothing about that, deliberately, and the decision is that it cannot reach the Workflows view.** The selected run lives in its own `openRunId` state (§5.5-D1 item 10) precisely so it is not `activeTab`: `activeTab` is simultaneously the rail's `activeId` **and** the transcript's bucket selector, so a runId in it would silently change what the transcript renders. A permission event therefore returns the transcript to Main and leaves the expanded run card expanded, because the Workflows section lives inside the rail's own column rather than behind a tab. **This was reasoned from the code and NOT observed at the dev server** — no model turn is possible in this checkout, so no permission event could be provoked (see the environment note). **If a later story finds it does reach the view, the fix is a scope item, not a bug in this ruling.**

### An environment note, not a repo finding — STILL OPEN, still unowned

- **No live model turn is possible in this checkout, and story 4.2 re-measured it from the running server.** `ls apps/web/node_modules/@anthropic-ai/` → `claude-agent-sdk` **alone**; the native package sits unlinked in the bun store at `node_modules/.bun/@anthropic-ai+claude-agent-sdk-darwin-arm64@0.3.204/`. This story did not only re-run that `ls` — it **launched a real run through `POST /api/ultra` on the dev server and captured the engine's own words on `events.ndjson`**: `log: "scout could not run: Native CLI binary for darwin-arm64 not found. Reinstall @anthropic-ai/claude-agent-sdk without --omit=optional, or set options.pathToClaudeCodeExecutable."` **Consequence for 4.2's dev proof:** no chip → `ultra` tool call, no `mcp__ultra__ultra` tool part, therefore no anchor splice and no wake TURN could be observed. Everything not requiring a model WAS observed (see the story's Debug Log). Mutating `node_modules` is the operator's call, not an agent's. **Owner: none — this needs a human.**
- **`GET /api/chats` returns ZERO chats in the dev root, which is what makes the dock signal's docking half unobservable.** No model turn has ever been possible against this root, so no chat was ever persisted. `ultra-dock-signal.tsx` correctly **skips** a session id with no matching chat row rather than docking it with a placeholder title (AC7 proof 2), so with zero chats it correctly docks nothing — the code path was exercised and took its designed branch, but the *positive* case (a head appearing for an undocked session) could not be reached. **Same owner as above: it unblocks when a model turn does.**

### Left behind in the DEV state root by 4.2's dev proof (not the real one)

- **`u-fixround-bad` was QUARANTINED, not deleted, and it was actively poisoning the root.** `~/.telar-dev/ultra/u-fixround-bad` held `{ "sessionId": "nobody", "state": "running", "startedAt": …, "updatedAt": … }` — no `runId`, no `meta`, no `spend`. **Measured live:** with it in place, `GET /api/ultra` returned `{"runs":[]}` **while five run directories sat on disk**, because `listUltraRuns()` threw and this story's new guard degraded to the empty list; `GET /api/ultra/wakes?sessionId=devproof-session-1` returned `{"pending":[],"live":0}` for the same reason. Both returned **HTTP 200 with a silently empty body** — which is exactly the behaviour the guard is for, and exactly why the underlying `saveManifest` item above is real. It was **moved** (not removed) to `…/scratchpad/quarantine/u-fixround-bad` so the operator can restore it; after the move the list route returned all four remaining runs with their server-rendered names. **Nothing under the real `~/.telar` was read or written at any point.**
- **Three new run directories were added to `~/.telar-dev/ultra` by this story's dev proof** — `u-d8d86a7a5c49` ("dev proof sweep"), `u-5f0194385c42` ("concurrent alpha") and `u-610965beb3d5` ("concurrent beta"), all `done`, all `spend 0` (no agent ever billed, because none could run). They join the four already there. **Owner: none — they are harmless fixtures, and the next dev proof will meet seven.** `orchestrator-run-log.md`'s "five run directories" sentence is stale again; this story did not edit that file.

---

## Deferred from: code review of story 4-2-the-real-session-ui-composer-chip-dock-signal-and-authoring-reference (2026-07-27)

Round 1 of `bmad-code-review` over `e093a98..3a85c6d`. Three parallel layers produced 35 findings; all 35 were put to an independent adversarial verifier instructed to refute them, and 17 survived. Fifteen are `[Review][Decision]`/`[Review][Patch]` items and live in that story's `### Review Findings`. The two below are the ones ruled **real but not this change's to fix**.

- **Two raw NUL bytes sit in `apps/web/lib/server/git-tab.ts`, and the run's occurrence tally does not count them.** Offsets **2261** and **26024** — a `const NUL = "<the byte>"` on line 88 and a `.replace(/<the byte>/g, "")` regex literal on line 861, both legitimate `git … -z` separator handling and both valid TypeScript. **Pre-existing: commit `d87ad57`, outside story 4.2's write set**, and 4.2 correctly did not cross into it. It was found while auditing 4.2's own close-out NUL scan, and it reproduces the exact harm story 4.2 §9 documents: `grep 'const' apps/web/lib/server/git-tab.ts` prints `Binary file … matches` instead of its 190 matching lines. **Consequence for the record:** the run has been counting NUL occurrences (1.1's `usage-ledger.ts`, 1.2's `admission.test.ts`, 4.2's story file, 4.2's `ultra-rail.tsx` = "the fourth"), and that tally is a count of *occurrences caught*, not of *NUL bytes in the tree* — the tree holds two more that no story's scan covered, because every scan so far has been scoped to the files that story touched. **The fix** is the rule §9 arrived at: write the separator as the six-character escape, never as the byte. **A repo-wide scan** (`git ls-tree -r --name-only HEAD` piped through a byte count) finds these in one pass and is worth adding to the gate. **Owner: whichever story next opens `git-tab.ts`, or whichever first adds a repo-wide byte scan to the gate.**

- **`GET /api/ultra/[id]/agents` re-reads and re-parses every agent transcript in full, plus the whole events journal, on every 4s poll.** `apps/web/app/api/ultra/[id]/agents/route.ts`, `GET` — `readUltraEvents(id, 0)` plus `listUltraAgentOrdinals(id).map((ordinal) => readUltraAgentTranscript(id, ordinal))`. `readUltraAgentTranscript` (`packages/core/src/ultra/storage.ts`) is a whole-file `readFileSync` + per-line `JSON.parse` with **no offset, no cap and no cache**, so the handler's cost is O(all bytes every agent in the run has ever streamed) while its answer is a fixed 200-code-point snippet plus a `settled` flag per ordinal. `use-ultra-runs.ts`'s index effect fires it every `POLL_MS = 4000` per live run. **Not user-visible today and deliberately not filed as a defect:** a verifier measured the realistic worst case (60 agents, 30 turns, payloads pinned at `TOOL_OUTPUT_CAP`) at 16–30 ms per 4 s under Bun — roughly a 0.5% duty cycle — and every run under `~/.telar-dev/ultra/` is 16 KB. It is also not the dominant cost: the **pre-existing** SSE tail (`app/api/ultra/[id]/events/route.ts`, added in `84a6e7c` and untouched by 4.2) already does a full read of the same `events.ndjson` every 400 ms per live run. **The residual is that the work is unbounded in principle** — `LIFETIME_BACKSTOP` allows 1000 agents per run — while the answer is fixed-size; a byte-offset or last-N-lines read makes it O(new bytes), the same shape `usage-ledger.ts` already uses. **Owner: whichever story first runs an Ultra script at a scale where this bites, or whichever next opens ultra's read path for performance.**

## Deferred from: the round-1 FIX PASS of story 4-2-the-real-session-ui-composer-chip-dock-signal-and-authoring-reference (2026-07-27)

Three items, all found while closing the review's ten. Each is out of that pass's write set or out of its scope; none is a defect it introduced.

- **The repo-wide NUL scan the review's deferred item proposed was RUN, and it confirms the finding.** `git ls-tree -r --name-only HEAD` piped through `tr -dc '\0' | wc -c` over every tracked file finds exactly three: `apps/web/lib/server/git-tab.ts` (**2 bytes** — the pre-existing item above), `apps/desktop/build/icon.icns` (3) and `apps/web/app/favicon.ico` (27). The last two are genuine binaries and are not findings. **So `git-tab.ts` is the only source file in the tree carrying a raw NUL, the deferred item's "the tally undercounts" is now measured rather than argued, and the scan that measures it is one line.** Nothing about the owner changes; what changes is that the scan exists and takes about a second. **Owner: unchanged — whichever story next opens `git-tab.ts`, or whichever first adds this line to the gate.**

- **`session-view.tsx`'s own auto-dock mints its `initial` by UTF-16 code unit, the same bug NH-1 fixed one file over.** `apps/web/components/session/session-view.tsx`, the unmount auto-dock cleanup: `initial: (s.title.trim()[0] ?? s.project.trim()[0] ?? "·").toUpperCase()`. NH-1 fixed the copy in `ultra-dock-signal.tsx` (`Array.from(...)`), because that line is one story 4.2 wrote. **This one is pre-existing, and out of the fix round's reach.** `git diff -U0 -- apps/web/components/session/session-view.tsx | grep '^@@'` gives the round **two** regions in that file — **2148–2159** (B1's pending factory, `runSnapshot(null, null, [], runId)` and its comment) and **3206–3230** (SF-2's `&& !escalation` term and its comment) — and the astral-surrogate line is at **1140**, in neither. Changing it would be an uninstructed edit to a line outside the story's write set, which §3's declined-cleanup discipline forbids. *(This sentence first said "the only hunks … at 3197–3221" — one region where there are two, at a range nine lines low, because it was measured before B1's second hunk landed in the same file and never re-taken. Nine is exactly what that hunk inserts. Caught by an adversarial pass over the fix round, and worth leaving visible: it is the defect class the round exists to fix, committed inside the round's own record.)* The harm is identical and ordinary: a chat titled from a first message beginning with an emoji renders half a surrogate pair as the dock head's glyph. §5.6-T19 item 9 is the repo rule. **Owner: whichever story next legitimately opens `session-view.tsx`'s dock code, or a sweep that fixes both call sites at once.**

- **The two `agents/` and events reads the B1 fix adds for TERMINAL runs inherit the unbounded-read residual filed above.** `apps/web/lib/use-ultra-runs.ts`. Closing B1 meant reading a finished run's journal and agent index — the surface could not otherwise state `N done` for a run it never watched — so a session with N historical runs now costs **N** `EventSource` opens and **N** `GET /api/ultra/[id]/agents` requests **once per page load**, where before it cost zero and lied. Each individual read is the same one the item above measured at 16–30 ms worst case, each is one-shot rather than per-cadence (`hydrated` and `indexed` refs), and the whole set is bounded by that session's own run count. **It is disclosed rather than deferred as a defect** because the cost is real, is new, and scales with a number nothing caps: a session that accumulates hundreds of runs pays for all of them at mount. The fix is the same offset-read the item above names, plus — if it ever bites — hydrating lazily from the rail's own expansion rather than at mount. **Owner: the same story that takes the item above.**
