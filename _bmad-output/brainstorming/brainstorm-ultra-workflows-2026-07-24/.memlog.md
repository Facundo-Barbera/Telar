---
topic: implementing ultracode (Ultra workflows) in telar
goal: grounding session before migrating normal daily sessions into telar: map the feature's current state (U1-U5 core+tools done, U6 real UI missing), settle final touches and gaps — quick, in chat
mode: partner
status: complete
updated: 2026-07-24T01:06
---

- (direction by user) quick in-chat session: map the ultra/workflows feature + final touches; workflows are the first feature needed to migrate normal daily sessions into telar
- repo state researched: U1-U4 core done (packages/core/src/ultra: sandbox, executor, journal+ordinal resume, runner, storage), U5 tools done (ultra-mcp.ts, chat route wiring, 6 API routes), U6 real UI missing (only demo-gallery mockup session-ultra.tsx exists); out of U5 cut: authoring-reference skill file + session-cost rollup; open Qs from plan: schema-less agent(), completion delivery mechanism, codex gating
- (direction by user) zoom out first: coach should state what the feature is supposed to DO, not the implementation map
- (decision) read confirmed; flow = map the feature, surface gaps, list what's missing to implement
- (insight by coach) verified in repo: completion delivery is POLLING ONLY (ultra_status), no wake mechanism when a detached run finishes — biggest parity gap vs CC harness which re-invokes the model on completion
- (insight by coach) verified: child posture (fixed ULTRA_CHILD_TOOLS + restrictTools) and startup reconciliation (self-healing reads) ARE implemented; zero ultra wiring in real session UI (session-view, subagent-rail, composer all clean of it)
- (question by user) doesn't get the sandbox part — doesn't think he designed it originally
- (direction by user) user is tired: coach takes over the session (babysit mode), simple language; end goal is simply 'ultracode on Telar' — coach settles the calls with sensible defaults
- (decision) sandbox stays as-is (determinism bans are load-bearing for resume; hygiene part is reference parity, not a real security layer)
- (decision) coach settles open forks with defaults: (1) completion delivery is THE missing design piece — when a run finishes, Telar wakes the main agent with the result (synthetic completion event -> fresh assistant turn summarizing it; if user is mid-conversation the event just lands in context on the next turn), mirroring CC task notifications; polling stays as fallback
- (decision) keep schema-less agent() (reference parity); ship Claude-first, Codex lights up when the driver seam lands; run one real end-to-end prove-run on a sandbox project before calling the feature done
- (decision) build order: 1) completion wake mechanism, 2) U6 UI wiring (gallery mockup contract -> real SSE: anchor, rail Workflows section, stop/resume buttons), 3) authoring-reference skill file, 4) polish (composer chip, session-cost rollup)
- (insight) synthesis: engine is done and strong; the feature fails today at the two human touchpoints — seeing a run (no UI) and hearing it finish (no wake). 'Ultracode on Telar' = close those two, teach the author agent well (skill file), prove it once for real. Everything else is polish.
