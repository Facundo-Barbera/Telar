---
topic: loom system design — from vibecoded to deliberate
goal: give the loom system more form and thought, drawing inspiration from Claude Code ultracode workflows, Codex Ultras, and bmad-loop (BMad Next) — looms only, telar's own ultras are a separate workstream
mode: partner
status: complete
updated: 2026-07-18T13:55
---

- (direction) coach should accompany decisions: creative + assertive with options, but user makes the calls
- (decision) technique batch confirmed: Job to Be Done -> Trait Transfer -> Assumption Reversal -> Kill the Crown Jewel
- (technique) started Job to Be Done
- (insight by user) bmad gives great planning+execution but is bad at testing; user has never managed to close a bmad project — closing the loop is the unmet need
- (insight by user) harnesses are great at doing but weak at final delivery — they require constant steering
- (direction by user) telar origin story: standardize CC+Codex connectors in one UI first, then build automation pieces on top; loom is the first automation piece
- (idea by user) loom = e2e development executor with integrated surface testing: Playwright-enabled, evidence provided to the user
- (idea by user) loom should reason about how the implementation will FEEL, not just what was asked — specialized agents+rules, tuned for web dev
- (insight by user) the job: if I want a feature implemented I want to trust a loom to get it done — trust and autonomy over steering
- (idea by user) accept screen = evidence + proof + synthesized narrative: what happened, what the implementation was (if any)
- (idea by user) multi-tasking is part of the job: run many work items concurrently through looms (e.g. 7 bmad epics at once — epics as example, not rule)
- (idea by coach) if fleets are the norm, the accept surface becomes an inbox of proven work — evidence must compress to a skimmable delivery card: claim, proof, risk flags
- (idea by coach) job statement v1: when I know what I want built, I hire a loom to carry it from intent to proven-working feature — judgment, not steering (+ coach reframe: the deliverable is evidence, code is a side effect)
- (idea by user) boundary: never send a 'simple task' (quickly doable) through a loom — below that threshold it's chat/ultra/by-hand
- (insight by user) PARKED FLAW: loom's planning/intake step (spec generation) is weaker than bmad's epic definition — likely source of downstream problems; revisit later
- (idea by user) pipeline utility: send looms to build what's already planned while the user plans the next batch in parallel
- (insight by coach) JTBD synthesis: the loom's job is to keep the human in the planner seat — planning and delivery overlap; human does judgment only; loom ceremony only pays above a task-size threshold
- (technique) started Trait Transfer (anchors: bmad/bmad-loop, ultracode workflows, Codex Ultras)
- (insight by user) bmad trait named: planning yields broken-down pieces (not one monolithic spec) AND each piece comes from a task-specific agent; after planning, automation takes over on its own — 'essentially where we want to get at'
- (idea by coach) graft: telar's EXIT is pluralistic (multi-lens critic panel) but its ENTRANCE is monolithic (single draftCharter pass) — staff the loom's intake with specialist seats so the entrance matches the exit
- (idea by user) loom intake = dynamic graph grown over time by an agent: opens with a 'what are we doing' conversation, then grows horizontally (parallel nodes: PRD planning, context gathering, brainstorm) and vertically (next layer e.g. sprint planning after layer completes)
- (idea by user) graph completion gates execution — 'you actually spend time preparing' before the loom runs
- (idea by user) progress preservation: skip planning nodes whose artifacts already exist and need no additions (e.g. PRD already created)
- (idea by user) path form is free — never hard-coded; the first agent chooses the shape of the planning path, user can steer anytime; all agent-driven like bmad, possibly via MCP server
- (insight by user) correction: the pre-loom graph is NOT a weave — a weave is already-planned by the orchestrator; this system comes BEFORE the loom (similar outputs, not the same thing)
- (insight by user) bmad rot problem: docs go obsolete over time, epic numbers grow forever, you drag bad things along — real documentation shouldn't accumulate, it should ADAPT; first agent asks: does this request update the form? the objective? the architecture?
- (idea by coach) name the pattern: reconciliation, not accumulation — the first agent DIFFS new intent against the current knowledge model; nodes spawn only where drift exists (this is why PRD-skip falls out naturally and why the shape can't be hard-coded)
- (idea by coach) close the knowledge loop: when a loom completes, its evidence/synthesis patches the map back — delivery itself keeps documentation current; rot dies when the executor is also the documenter
- (question) OPEN INVESTIGATION: what regions does the living map/model contain? Can't be named yet — research bmad and other methods for what a living project model should hold
- (insight by user) telar owns the platform (unlike being a user of bmad's skills): the method can be native — adapt steps to fit our automations, choose which parts go where, create our own agents, design for modification and growth later
- (idea by coach) method-as-platform: bmad is a method you follow by discipline; telar can make its method STRUCTURAL and self-enforcing (like the accept moat) — planning seats as a growable registry, method enforced by code not convention
- (technique) Trait Transfer: moving to anchor 2 — ultracode / dynamic workflows
- (insight by user) ultracode traits: (1) the deterministic+LLM-driven mixture design — 'honestly really good', (2) PARALLELIZATION — the most-loved trait, tackling multiple tasks at once, (3) planning-first before the fan-out
- (idea by coach) graft: parallelism is a PLANNING OUTPUT, not a runtime feature — you get the parallelism you planned for; make the loom's planner produce independence explicitly (interference analysis: which subgoals share files/surfaces) so weave width is designed, not accidental; same analysis at fleet level -> lanes + landing queue for concurrent looms
- (idea by user) width = team orchestration: an agent on its own does a lot, but an agent orchestrating a team should do MORE — multiple tasks developing simultaneously as the norm
- (technique) Trait Transfer: moving to anchor 3 — Codex Ultras
- (insight by user) the key trait of ultras/workflows/loops = CONTEXT PRESERVATION: subdivide instead of bombing one agent with tasks; the problem shifted from user-facing to 'make sure your agent has everything they need'
- (insight by user) reframe: the loom is not only failing in design, it's failing in PREPARATION (connects to the parked planning-intake flaw)
- (insight by user) bmad-loop (tmux-driven) is almost what telar wants but it's SERIAL (story by story); ultras/workflows are DYNAMIC — agent decides upfront which tasks can run in parallel
- (idea by user) original loom vision: plan a bunch of things, execute in one go, encourage parallelization where possible + dependency charts (agents can declare 'before this you need this') — DAG execution
- (idea by user) thread vision: a full TEAM behind a feature, scalable — thread as sub-orchestrator defining its own flow; never landed (today: one agent + subagents following pre-written steps)
- (question) THE CHOICE surfaced: during active development, pre-defined steps (bmad-like) vs orchestrator-defined steps (ultra-like)?
- (idea by coach) dissolve the binary: DYNAMIC AT PLAN-TIME, DETERMINISTIC AT RUN-TIME — the thread COMPILES its flow: authors a validated DAG artifact (parallel lanes, dependency edges, per-node context manifest), engine executes it deterministically; re-plan is an explicit bounded audited event; current pre-written steps become the fallback template for simple/low-confidence tasks (adaptive ceremony)
- (decision by user) CORE IDEA: redesign looms entirely — problem definition must come from the USER, before; today a single agent drafts charter+spec and infers too much (fine for quick workflows; looms are not for quick workflows)
- (idea by user) graph-based decision making at the start is the best bet: telar provides agents proper planning tools (bmad-like); user defines task(s) -> agent suggests a flow -> accept / modify on-the-go / deny and go straight to development
- (idea by user) after user-facing tasks end, orchestrator takes SOLE responsibility for vision->reality: deterministic-guided but fully AI-driven; core initiative = created -> proven -> delivered; thread main agent (sub-orchestrator) defines HOW to implement its given task
- (decision by user) IMPERATIVE role wall: orchestrators and sub-orchestrators NEVER code, never touch code directly, get their own separate role in the UI — hard developer/orchestrator distinction
- (idea by coach) power-by-denial symmetry: telar now has three structural walls — verifiers can't write, conductors can't code, agents can't accept; the loom is a role system defined by what each role is DENIED
- (technique) started Assumption Reversal (against both the current implementation and the new core idea)
- (direction by user) requested a visual mockup (artifact) of the preparation graph, styled with elements from the actual app, to confirm we share the same mental model — before starting Assumption Reversal
- (note) published preparation-graph mockup artifact (telar-styled: map drift panel, opening conversation, drift-spawned layer 1 with skipped PRD node, flow-compile layer 2, readiness gate w/ accept-modify-deny, orchestrator handoff, write-back loop) — awaiting user's model-match verdict
- (direction by user) mockup correction: same-sized blocks, graph flows LEFT-TO-RIGHT, nodes connected with lines so multi-dependencies and branching from a single node are visible — a true DAG view
- (note) mockup v2 republished (same URL): uniform 148px nodes in columns, bezier dependency edges with arrowheads, dashed dormant lane through skipped PRD, indigo write-back arc loom->map, gate detail panel below the board
- (decision by user) NOT on board with automatic write-back loop: once in a loom, it's a separate thing; map modifications must happen via a system inside a working loom or a NEW loom — inspired by bmad-loop escalation, only triggers when necessary
- (idea by coach) separate the SIGNAL from the WRITE: (A) drift ledger — on accept a loom declares a map-impact note, queued, next intake consumes it (bmad-loop deferred-work pattern); (B) reconciliation loom — when drift crosses a threshold, propose a dedicated small loom to update the map, with its own accept ('map updates are work; work goes through looms; nobody writes the map silently'); (C) lazy diff safety net — next preparation's intake detects reality-vs-map drift itself; compose A+C always, B as escalation
- (note) mockup v3 republished: loopback arc removed; caption states map updates are a separate system, not an automatic loop
- (decision by user) adopted the layered map-update model: A drift ledger (declare on accept) + C lazy intake diff always on, B reconciliation loom as threshold-triggered escalation — replaces automatic write-back
- (note) preparation-graph.html saved into the session workspace to accompany the brainstorm artifacts
- (decision by user) clarify assumption #2: a loom is NOT necessarily for a single task — multi-task batches are core to the vision; guard against future agents reading 'loom = one task' from fresh context
- (direction by user) verification process deliberately NOT covered this session — it's its own rabbit hole; schedule a dedicated brainstorming session for it
- (technique) Assumption Reversal: flipping seeded assumptions (verification flips parked for the dedicated session)
- (insight by user) flip #1 refined: streaming evidence already partly exists BUT current loom 'escalates on everything' — mid-flight checking must be a USER CHOICE (pull), never an obligation; escalation (push) reserved for dire situations only
- (insight by user) flip #4: the inversion IS the current state — quick spec, scattered files, incomplete tasks from lack of context; looms lack the context to run e2e
- (idea by user) dedicate part of the preamble graph to VERIFICATION-READINESS: an agent that ensures verification CAN occur (reliable local dev server standup, replicable across project types beyond web); a seed of this exists in the system — expand it into a first-class preamble node
- (idea by coach) evidence is a WINDOW, not a DOORBELL: observability always-on and pull-based; push interruptions reserved for dire (bmad-loop CRITICAL shape) — at fleet scale this is the difference between an inbox and notification hell
- (idea by coach) verification-readiness node emits a VERIFICATION RECIPE artifact (boot command, ports, seeds, probe script) stored as a MAP region — learned once per project, diffed by intake like any region, inherited by every future loom; fail-closed: no lab, no experiment
- (insight by user) bedrock named: 'getting this done' is the loom's AXIOM, not an assumption — flipped, a loom shouldn't exist; the flip test separates axioms (flip = nonexistence) from assumptions (flip = ideas)
- (technique) started Kill the Crown Jewel — deleting the human-accept moat
- (insight by user) crown jewel exercise: the only replacement for the accept moat = a genuinely good agent that totally understands the product vision and the user's request — one that could do the user's job and give the security of merging without damaging the project
- (insight by coach) what the jewel is made of: the accept moment is where VISION enters the system — verifiers check contracts, the human checks vision; correctness and evidence are already delegated, judgment is not
- (idea by coach) VISION CRITIC seat: judge deliveries against the map's objective/form regions and put an advisory vision-alignment pre-verdict on the delivery card — the road toward the 'agent that does my job' runs through externalizing judgment into the map; later maybe a graduated-autonomy dial per loom class (never a silent bypass)
- (decision by user) converge now; UI/UX rebuild and verification system each get their own future session, seeded from this memlog
- (decision by user) affinity clusters confirmed: (1) preparation graph, (2) living map, (3) execution orchestrator+threads, (4) accept experience, (5) method & identity — through-line: loom as three-act system (prepare / execute / judge) with the human in the planner seat
- (decision by user) MoSCoW locked — MUST: preparation graph MVP, map v1 + lazy diff, verification-readiness node + recipe, role-wall carving, escalation audit (dire vs uncomfortable); SHOULD: flow compile, delivery-card inbox, drift ledger, seat registry; COULD: vision critic, fleet lanes + landing queue, early-kill UI, adaptive ceremony; WON'T THIS TIME: autonomy dial, verification deep design (own session), UI/UX rebuild (own session), map-region taxonomy (research first)
- (idea by user) SYNTHESIS CATCH — loom state: pause/resume is missing entirely; credits/usage run out often, looms must be able to park and resume; not a feature today — added to MUST (durable execution state)
- (insight by coach) closing connections: (1) one fractal pattern everywhere — declare -> validate -> execute deterministically -> reconcile lazily (preparation graph, flow compile, map updates); (2) minute-five 'feel' and the vision critic are the same idea at entrance and exit, both living in the map; (3) context preservation materialized as the DAG node's context manifest; (4) simple-task boundary + deny-and-go-direct + fallback templates = one adaptive-ceremony dial; (5) pause/resume is enabled BY the design law — only explicit deterministic state can resume (ultracode journal/resume + bmad-loop pause-and-rearm as anchors; multi-account resume as a telar-native twist); (6) every cluster serves the planner seat
