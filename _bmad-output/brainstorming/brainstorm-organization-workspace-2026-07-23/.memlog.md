---
topic: an organization/workspace module for telar
goal: extract and give shape to the fuzzy idea: a workspace that helps Facundo stay organized across many projects (work + school), capture meeting snippets before they're lost, using agentic tools — figure out what the module IS before how to build it
mode: partner
status: complete
updated: 2026-07-23T11:41
---

- (direction by user) Creative Partner mode, coach picks techniques; goal is extraction — finish pulling the idea out, not feature-listing
- (decision) technique batch: Laddering -> Backcasting -> Morphological Analysis -> Worst Possible Idea
- (technique) started Laddering
- (direction by user) reframe Laddering around the gap: 'what will this bring you that you don't have today?'
- (idea by user) a work-focused todo list you can talk to — not a daily-life todo app; items get assigned to projects
- (idea by user) a system that tells you what you're MISSING — notes, meetings, etc. — proactive gap detection, not just storage
- (insight by user) notifications are a dead channel for him — 'instantly or never', no late reaction; so the system must be pull-based (he asks, it answers), push is worthless
- (idea by user) morning-resume conversation: 'where did I stop last night? what's up today?' — agent knows active projects, agents left working, what got done, what's pending that day
- (idea by user) extends to school: could help track/write homework assignments
- (insight by coach) 'agents I left working' makes this Telar-native: the workspace already knows half the story (looms, sessions, overnight agent work) — a generic org app can never answer 'where did I stop'
- (insight by user) the real cost is warm-up: re-entering a project untouched for days often takes longer than the work itself; value = getting into something quicker
- (idea by coach) reframe: not a todo app but a context bank — snippets/todos/meetings/agent-activity are deposits, 'where am I at' is the withdrawal; success metric = time from sitting down to first productive keystroke
- (insight by user) BEDROCK: the need is calm, not speed — not stressing when priorities force a project on hold; put down and pick up work without anxiety
- (insight by user) agent speed inverted the bottleneck: months-long features now take hours, so more tasks in flight; more tasks demand more organization across projects — too many things to track, hard to give them an order
- (insight by user) the module connects the real world + product vision to the coding part ('the dirtiness of multi-tasking'); docs without order are just text in a repo; Jira-style methods assume pre-agent pace
- (technique) started Backcasting
- (idea by user) sit-down overview: priorities, organization, maybe a queue — then start everything in parallel from it
- (idea by user) 'expert agents': per-project context holders that triage work — simple task -> start a session with the right context; complex task -> plan a loom; meeting -> here are the notes
- (question by user) the fork: one master agent orchestrating experts, talking to each expert directly, or a more manual UI? worry = context window
- (insight by user) compaction/context-cutting fits here: only a bounded slice of context defines 'the current', unlike long textual conversations
- (idea by coach) resolve the fork by altitude: experts WRITE durable state digests, master is a thin READER of digests (receptionist, not manager) — master answers the cross-project sit-down question, experts serve in-project work; context fear dissolves because state lives on disk, not in the conversation
- (decision by user) front door = a single chat: the master agent with the overall view
- (insight by user) the calm mechanism is asymmetry: what stresses him ('everything I need to do') is already-known ground for the master — 'I already knew this, take this approach', backed by knowledge + expert agents
- (insight by user) today's capture: Raycast floating notes — markdown, always-on-top until dismissed, recallable — but notes sometimes get lost; also Granola meeting recordings and talking to agents
- (idea by user) Telar Notes: native floating-notes primitive (Raycast-style) that Telar is aware of — can't integrate Raycast, so build our own
- (idea by user) 'fix my notes' (AI to me): quick notes are written without context — the system must repair them and bring them to the right place; even just assigning the right project is a huge step
- (idea by coach) design law for capture: capture stays raw (zero ceremony, he's mid-meeting), understanding is DEFERRED — a post-capture enrichment pass routes fragments through the project's expert agent (who can decompress 'the auth thing' where a generic AI can't); ambiguous leftovers resolved in the morning chat, never via pings
- (idea by user) rich todos: a todo can hold entire files — even HTML mockups; hold those in Telar instead of the repo's /docs; a todo can be simple but doesn't have to be
- (idea by coach) todo = work packet that RIPENS: fragment -> fixed todo -> accumulates context (mockups, meeting excerpts, expert analysis) -> at execution time it IS the loom/session briefing; the BMad story-file concept made native to Telar with a lifecycle; docs ordered by intent (attached to work items), not by location (/docs)
- (idea by user) 'bed mode': Telar performs organization tasks autonomously in the background (overnight); the scope of that autonomy is the open design question
- (insight by user) not every task is a work packet — simple one-line todos coexist; richness is a spectrum, not a requirement
- (idea by user) foreign structures: some projects aren't his — someone else defines tasks (issues pointing to files, handoffs, mockups in the repo); Telar must make that structure part of itself, not impose its own
- (idea by coach) bed-mode boundary = the accept moat applied to organization: agents prepare (file, draft, ripen, sync) but never commit (start/complete work); native projects = Telar is source of truth, mirrored projects = Telar holds a view with pointers back; the expert agent doubles as translator of each project's methodology
- (technique) started Morphological Analysis
- (insight by user) experts are ACTUAL agents, not passive context stores: the master chat calls them at will, like sub-agents — except inverted: sub-agents inherit the caller's project scope, but the master has NO project; each expert is scoped to its own project
- (idea by coach) two shapes for expert agents: (a) persistent residents, (b) ephemeral agents rehydrated from the project digest per call — (b) fits Telar's state-on-disk law: the 'expert' = project context + standard recipe, spawned fresh, illusion of residency without long-lived processes; (c) hybrid: resident during bed mode, ephemeral by day
- (decision by user) experts are EPHEMERAL: spawned per call, rehydrated from on-disk project digest — always-alive agents are bad for usage and context keeping
- (idea by user) capture options: screenshots, clipboard, voice memos, email forwarding, GitHub issues, Telegram messages (+ Telar Notes, Granola, agent chats = 9 channels)
- (insight by coach) capture splits into PUSH (he sends: notes, memos, telegram, email) vs WATCH (system observes: github issues, agent chats, calendar) — watch channels are what make 'I already knew this' possible without his effort
- (idea by coach) calendar as the EXPECTATION source: gap detection needs to know what SHOULD exist — 'meeting at 3pm' + no note afterwards = the original 'what notes am I missing' flag, derived not asked
- (insight by user) pushback/reframe: in his model the calendar is GENERATED inside the app — an output concept, not an input
- (idea by coach) two distinct calendars may coexist: the external real-world calendar as watch-input (only if he actually keeps one) vs Telar's generated agenda as OUTPUT — the queue projected onto time; the generated agenda is itself an Ordering option
- (insight by user) meetings mostly live in Google accounts (surfaced via Apple Calendar) = watchable input exists; but many meetings are SPOKEN, never calendarized
- (idea by coach) two expectation sources: watch Google Calendar + MINE time commitments from captures ('we'll sync Thursday' in a note/transcript) — bed mode harvests spoken commitments and proposes them into the generated calendar
- (question by user) Ordering stays OPEN: instinct = mixture of conversation + existing constraints (deadlines, office/school hours that shift over time); no dedicated times today; many approaches possible, none convincing yet
- (idea by coach) ordering without clocks: split PRIORITY from SCHEDULE — context lanes (office/school/free, coarse and shifting) each holding an ordered STACK; master answers 'what's next?' conversationally instead of pre-timing the day; a stack can't slip, a timed plan can (slippage = guilt = anti-bedrock)
- (decision by user) Ordering shape: deadlines saved as DATA (not schedule) incl. flexible SELF-deadlines (much of his work has no external time limit); plan = stack/queue; live reprioritization via 'what's next?' conversation — both modes coexist
- (idea by coach) self-deadlines get a RENEGOTIATION RITUAL, not alarms: sliding is allowed, SILENT sliding is the leak — master as witness in the morning chat ('you told yourself Friday, third slide — re-set or drop it honestly?')
- (direction by user) wants UI mockups at some point, to see how the module integrates into Telar's existing UI — capture as a next step after this session
- (technique) combination game didn't land, dropped; started Worst Possible Idea
- (insight by user) worst version: wake up to 100 agent-derived tasks from a simple series of todos — bombardment defeats the purpose; plus pinging and upfront demands
- (insight by coach) CONSERVATION LAW derived from the horror: the module must compress attention, never multiply it — agents may fan out INSIDE a packet, but the queue's item count only grows when reality grows (real meeting, real request), never because an agent got creative; bed mode's output is a digest bounded by his attention
- (idea by user) the BRAIN DUMP: dump everything in one go into the conversation — multiple things from multiple projects, 'a call for organization'; master parses, splits, and routes it all
- (idea by coach) dump and briefing are bookends: briefing = system->him (state compressed OUT), dump = him->system (mind uncompressed IN); master answers with a RECEIPT — '6 items, 3 projects, filed these, 2 unplaceable: what's the pdf thing?' — compression demonstrated live
- (decision by user) session closed toward UI mockups: build them in the app's builtin demo-gallery (real components, real style) so placement + look can be judged and modified in situ
- (insight) SYNTHESIS: the module is a context bank for calm multi-tasking — capture raw (push+watch channels incl. Telar Notes, brain dump), understanding deferred (expert-routed enrichment, bed mode), items span one-liners to ripening work packets, ordering = deadlines-as-data + lanes/stacks + 'what's next' conversation, front door = single project-less master chat over ephemeral per-project experts; two laws: agents prepare never commit, agents compress never multiply
- (direction by user) session reopened: mockups are not the end — still brainstorming; now digging technical: how the system stands up
- (decision by user) mockup-pass decisions: desk = RIGHT rail (session-sidebar pattern), dismiss drains to queue never deletes; queue = dense grouped list, board retired; lanes are DYNAMIC data with structural provenance (master may propose splits, human accepts); sub-tasks live inside items (conservation valve); batches = select N -> weave as one loom series
- (decision by user) tasks are a Telar-wide substrate: sessions read/create/modify via workspace tools, provenance records the surface; master reads external sources via workspace-scoped MCP servers — reference not inventory, nothing becomes a task unless user says so
- (question by user) how do we stand up MCP servers for the workspace? project MCP servers exist and are shared across harnesses (CC/Codex load differently), but the master has NO project and NO working directory for the harness
- (idea by coach) the missing primitive isn't MCP config, it's A HOME: TELAR_HOME/workspace/ as a synthetic project dir — harness cwd, manifest with mcpServers, per-harness config materialization all carry over unchanged; the master's 'repo' IS the context bank (task store as files it can natively read)
- (idea by coach) packets map to directories: workspace/packets/<id>/ with packet.yaml + attachments — 'todos that hold entire files' becomes literal filesystem; harness file tools = the master's v1 read/write path, formal workspace MCP toolset comes later for cross-surface access
- (direction by user) correction: MCP is NOT loaded via JSON files — Telar injects servers directly at launch, single configuration across harnesses; coach's materialize-into-dir reasoning was wrong. Real open question: can the harnesses run without a working directory at all? -> investigate
- (note) research fan-out launched: (1) Claude Code CLI/SDK without a meaningful cwd + programmatic MCP injection, (2) Codex CLI git-repo/wd requirements + MCP config injection, (3) telar codebase: how sessions launch, where cwd comes from, what assumes a project exists
- (insight) research(telar): project coupling lives in the CHAT ROUTE, not the engine — engine.ts agent() takes cwd as an optional param (process.cwd() fallback, zero manifest dependency); the one hard gate is route.ts getProject(project).manifest -> workspace=manifest.root feeding cwd/guardrails/settingSources
- (insight) research(telar): PRE-EXISTING GAP — Codex sessions never receive project MCP servers; resolveProjectMcpServers is wired only on the Claude branch (SDK mcpServers + strictMcpConfig:true); runCodexTurn has no mcpServers plumbing at all
- (insight) research(telar): project-less chat is NEARLY FREE — store.ts Chat.project already optional, permissions string-keyed (synthetic '__master__' works), session-log keyed by sessionId only, loom/ultra MCP servers only dereference project lazily inside mutating tools; blockers = upfront gate + synthetic cwd + default guardrails + settingSources:[]
- (insight) research(CC): wd is INCIDENTAL not hard — empty non-git dir empirically fine; no 'no-cwd' mode exists (process cwd always), SDK cwd optional w/ process.cwd() default; settingSources:[] zeroes ambient config discovery; MCP fully programmatic (mcpServers + strictMcpConfig, telar already uses this); trust dialog skipped in non-interactive/SDK mode
- (insight) research(CC): session history + auto-memory are keyed PER-DIRECTORY (~/.claude/projects/<encoded-path>) — a STABLE master cwd accrues one continuous history/memory bucket, throwaway scratch dirs fragment it; never use /Users/facundo itself as cwd (trust never persists there)
- (insight) research(Codex): no no-cwd mode; -C/--cd sets working root (dir must EXIST); empty non-git dir verified working once --skip-git-repo-check (SDK: skipGitRepoCheck) is set; sandbox workspace-write boundary = working root + --add-dir, purely path-based
- (insight) research(Codex): per-invocation MCP injection VERIFIED two ways with zero global config: -c mcp_servers.<name>.<field> dotted overrides (SDK: config object -> repeated --config flags) and CODEX_HOME pointed at a synthetic dir; --ignore-user-config for config-file-free runs — telar's Codex MCP gap is closable with exactly this
- (insight) SYNTHESIS: neither harness has a wd-less mode but neither needs a meaningful wd; both run fine in an empty dir (CC natively, Codex w/ skip flag). Both key per-directory state (CC history+auto-memory buckets, Codex trust + sandbox writableRoots) -> ONE STABLE synthetic home (TELAR_HOME/workspace) beats throwaway scratch dirs. Master session shape = skip project gate, cwd=workspace home, settingSources:[], guardrails default, MCP injected (wire Codex via -c overrides, closing the pre-existing gap)
- (note) UI mockup artifacts live in the app's demo gallery: apps/web/lib/demo-gallery/workspace/** (fixtures, shared, home, queue, packet, session) registered as 4 entries — workspace-home (master chat + desk rail + linear read), workspace-queue (dense grouped list, dynamic lanes, sub-tasks, batch action bar), workspace-packet (ripening work packet), workspace-in-session (tasks substrate from a project session); view at /demo-gallery
- (insight) FINAL SYNTHESIS: a context bank for calm multi-tasking — capture raw + hand-fed (understanding deferred to agents), items from one-liners to ripening packet-directories, dynamic lanes + stacks (no clocks, no schedule to slip), single project-less master chat over ephemeral per-project experts, desk rail for persistent agent-filed items, tasks as a Telar-wide substrate every surface reads/writes, external sources readable via workspace MCP (reference not inventory); three laws: agents prepare never commit, agents compress never multiply, pull never push; feasibility verified: master = new session shape at stable TELAR_HOME/workspace home, one route gate to skip, MCP injectable on both harnesses (Codex gap closable via -c overrides)
