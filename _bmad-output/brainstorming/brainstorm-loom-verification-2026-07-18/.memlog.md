---
topic: the verification system for telar's redesigned looms
goal: design the verification side of the loom — per-project-type verification recipes, the verification-readiness preamble node, reliable local dev-server standup, streaming/mid-run evidence vs final ALL-verify, and what makes surface evidence trustworthy; feeds a spec afterward, concrete outcomes over vibes
mode: partner
status: complete
updated: 2026-07-22T14:29
---

- (direction by user) Creative Partner mode: coach facilitates and contributes, is creative and assertive with options, but user makes the calls
- (decision) technique batch confirmed: First Principles Thinking -> Morphological Analysis -> Devil's Advocate Courtroom -> Decision Tree Mapping
- (technique) started First Principles Thinking
- (insight by user) verification-as-concept is settled (observe vs pre-planned expectation); the session's real problem is the HOW — setting up the verification system
- (insight by user) agents are the part that works (playwright, commands); the hard part is background services — databases, node/python servers — hard to configure, no one-fits-all
- (insight by user) telar's core missing primitive: background processes. CC and Codex attach processes to their shells; telar lacks this at session level (feature gap) and loom level (no way to reproduce service state or know 'this belongs to this')
- (direction by user) user requested an investigation pass: drive agents to research the background-process/service-standup problem before continuing first-principles
- (note) research fan-out launched: (1) prior-art service declaration schemas, (2) CC/Codex background-process primitives, (3) telar codebase seeds for process standup
- (insight) research(1): convergent minimal declaration set = command, cwd, env, readiness signal, CONDITIONED depends_on (service_healthy-style), stop signal+grace, restart policy, ownership token — full report in research-1-service-declaration-schemas.md
- (insight) research(1): 9 distinct readiness mechanisms exist (http, exec, tcp, grpc, notify-socket, watchdog, fixed-delay, log-regex, socket-activation); k8s uniquely splits startup vs readiness vs liveness probes
- (insight) research(1): ownership property independent of mechanism = 'declaration + one token -> enumerate and tear down everything this run created, touching nothing from other runs'; tokens vary: compose project name, overmind socket, pitchfork dir-binding, k8s ownerReferences
- (insight) research(1): 'no one-fits-all' partially refuted — the FIELD SET converges hard across 9 tools; what varies is readiness mechanism and ownership token, both enumerable
- (insight) research(3): telar already has a near-complete lane/supervisor stack (run-server.ts startLane: ports, templated env, portInject, readyCheck, dependsOn; supervisor.ts: restart-only-on-death, crash-loop breaker) — but reachable only from privileged verify/setup code, never from agent tools; full report in research-3-telar-codebase-seeds.md
- (insight) research(3): verification-readiness seed is split: requirements.ts (read-only detection at scoping) + setup-agent.ts (authors servers.yaml, validate-and-teardown); the proactive preparing-phase hook (weave.ts runSetup) is DEAD in production — only reactive repair-time mediation runs it
- (insight) research(3): no agent-facing background-process tool exists; a builder's 'bun run dev &' is an untracked orphan — no lease, no registry, no teardown hook
- (insight) research(3): the ownership shape already exists once: runner lease (~/.telar/looms/<id>/.runner-lease {pid,token,ts}, heartbeat, stale-reclaim) — generalize it for per-owner service leases; natural home ~/.telar/looms/<id>/services/<name>.json; NO per-chat-session dir exists at all
- (insight) research(3): session reframe candidate — not 'design a process system' but 'unbundle the existing lane primitive: add persisted ownership leases, expose an agent-facing tool surface, wire the proactive preparing hook'
- (insight) research(2): CC has two lifecycle levels (in-session bg commands: id, cursored output, stop-by-id; whole-session bg agents: supervisor daemon) — matches telar's session/loom split; Codex has only a coarse unified-exec PTY tier (kill-all only, no live stream); full report in research-2-cc-codex-background-processes.md
- (insight) research(2): verified convergent failure mode across CC+Codex: no ownership tagging of spawned process trees -> orphans reparented to PID 1, duplicate dev servers on port conflict, timeout kills wrapper not children, stale status
- (insight) research(2): under Codex sandbox, network is off by default in workspace-write and the opt-out is silently broken on macOS — port reachability is untrustworthy without an explicit probe
- (idea by coach) agents never spawn services at all: they DECLARE them via a tool; telar's supervisor (existing lane stack) spawns, owns, leases, probes, and tears down — ownership by construction, uniform across CC/Codex, orphans impossible
- (idea by coach) model the tool surface on CC's typed event shape (start/progress/notification + stop-by-id), not Codex's raw PTY polling
- (decision by user) 'agents declare, telar spawns' ACCEPTED — conditions: must feel as smooth as Claude Code's background flow; processes must surface in the UI for both sessions and looms
- (question by user) loom process topology: global or local? loom=branch (declared), currently worktree per thread — spawn a service per worktree for verification, or unify on the branch? how to see a thread's progress? make worktrees optional?
- (idea by user) leaning: single server per loom branch, updated when a thread's worktree merges; worktrees optional-but-preferred for threads (collision avoidance); accepted consequence: server-requiring tests can't run inside a worktree — 'not bad'
- (insight by user) instinct from own practice: one running server per line of development — mirror how a human dev works locally
- (idea by coach) yes-and: server-per-branch becomes an EVIDENCE LADDER — worktree level: cheap serviceless evidence (tests/typecheck/lint); landing onto loom branch: serial, each landing triggers lane verify (surface evidence, unambiguous blame — the just-landed thread owns any failure); final ALL-verify: cold frozen lane, evidence-grade
- (idea by coach) the loom lane doubles as the user's WINDOW: a live URL of the merged-so-far state — 'windows not doorbells' made physical; want thread progress? open the loom's URL
- (idea by coach) 'preview this thread' escape hatch: human-triggered ephemeral lane on a thread's worktree, torn down after — thread visibility without permanent per-thread servers
- (idea by coach) smoothness = idempotent ensure: ensure_service(name) reuses a live lease instead of spawning a duplicate — kills the duplicate-dev-server failure mode (CC issue #9780) by design
- (question) open fork: persistent warm lane whose checkout moves only at landing boundaries (declared state-reset policy per verify round) vs fresh frozen lane per landing; coach rec: warm lane for streaming evidence + window, cold frozen lane reserved for final ALL-verify
- (insight by user) warm-lane concern: updates that need cache cleaning; DB constraint is REAL: supabase CLI stack is too heavy to run per loom (RAM, weird dev) — one heavy infra stack max, never N
- (idea by coach) service TIERS in the recipe: scope: project|loom|ephemeral — heavy infra (supabase/postgres) shared at project scope under a refcounted lease; app server per-loom warm; ephemeral for previews and frozen verifies
- (idea by coach) isolation moves from process layer to data layer: tenant-per-loom via Postgres TEMPLATE database (cheap file-level copy; manifest.templateDb seed already exists); app gets {infra.postgres.tenant_url} injected
- (idea by coach) warm-lane cache problem: declared update strategy per service — hmr|restart|clean-restart chosen by DIFF against declared trigger paths (package.json, next.config.*) — deterministic refresh, no vibes
- (idea by coach) supabase wrinkle: gateway services (auth/PostgREST/storage) point at ONE db — so isolation strategy must be declarable per service: tenant-db | schema | mutex (serialize verifies against shared stack) | none
- (idea by coach) final ALL-verify recomposed: COLD app checkout + SHARED warm infra + FRESH tenant data from template — evidence purity where it binds, without re-paying the supabase boot
- (direction by user) empirical grounding pass: survey ~/Projects fleet ('sit in my chair — set up looms for these'); orchestrator (Focaltec) and ozom-gv are most active — deep-dive those, census the rest
- (insight) research(4) census A: majority of fleet is happy-path (single dev cmd, one port, no DB); outliers: bixkuOS (3 coordinated local processes + env-derivation script + MISSING seed.sql) and Focaltec portal (private CodeArtifact creds wall); OAuth walls (inboxdigest Gmail) are un-automatable — recipes need 'requires pre-provisioned X' fail-closed class
- (insight) research(4): doc/code drift confirmed in the wild (inboxdigest README says SQLite+Claude CLI, code says remote Supabase+OpenRouter) — recipes must derive from code+env, never trust READMEs; also runtime-mismatch trap (texassite: vite dev never boots the serverless fn — needs vercel dev)
- (insight) research(5) census Ozom: port collisions are FLEET-level, not just loom-level — 4 projects sit on stock supabase ports (54321-9), 3 on :8080; at most one such stack can run at a time; telar needs fleet-wide port planning
- (insight) research(5): new requirement classes from reality: install-phase gates (GITHUB_TOKEN for private registries — fails before boot), machine-global tool deps (portless proxy daemon), generated migrations (db:compose must run before reset), mixed lockfiles (PM detection ambiguous)
- (insight) research(5) DANGER class: ozom-linden's documented dev path points at the LIVE PROD Supabase — recipes must classify data stores disposable vs shared-production and REFUSE surface verification against production, fail closed
- (insight) research(5): 698-migration db reset (ozom-rent) validates template-DB stamping (pay reset once, copy cheap); only 2 of 8 Ozom projects have real test suites — surface verification is the ONLY verification for most of the fleet
- (insight) research(6): ozom-gv is the gold standard — kickstart.ts IS a verification recipe in shell form (idempotent, non-interactive, preflights docker/supabase, writes .env.local from supabase status, seeds, creates owner); bun run verify = full 3-layer gate (unit+pgTAP+playwright e2e w/ webServer)
- (insight) research(6): the WORKTREE-SECRETS WALL — ozom-gv's supabase/.env.keys is gitignored, non-reconstructible, missing from all 20+ existing worktrees; fresh-worktree standup fails at supabase start. NEW schema field: carry (gitignored-but-required files to propagate into lab checkouts) — telar-specific, absent from all prior art
- (insight) research(6): orchestrator = polyglot stress test — 5 services/3 languages, portless+SUDO wrapper in every dev script (lab must derive direct wrapper-free commands), documented seed path BROKEN (validate-and-teardown would catch), existing telar.yaml gate never runs the 130+ python tests, no /health on main API
- (insight) research(6): degraded modes must be declared ('without secret X: data synthetic / features Y off' — contract narrows explicitly); project-owned verify entrypoints (bun run verify, playwright suites) should be INGESTED as gates with the lane owning the server, not duplicated
- (question by user) reframe request: with all context loaded, describe non-technically what you'd EXPECT when running a loom (the experience, not the machinery)
- (decision by user) promise ranked most critical: #2 (only knock when dire) — worst failure = SILENT STALL: gone for hours believing the project is advancing while something broke and nothing moved
- (insight) the scarce resource isn't spend, it's WALL-CLOCK TIME UNDER FALSE CONFIDENCE — hours lost believing work advances; silence must therefore be a guarantee, not an absence of noise
- (idea by coach) the escalation razor: dire := the loom no longer has a viable path to advance without the human (stall, park, credential wall, crash-loop, credit exhaustion) -> PUSH; everything else (failed test entering repair, flaky boot being retried) -> window only
- (idea by coach) two liveness layers: supervisor watches PROCESS liveness (restart-on-death, crash-loop breaker — exists); orchestrator watches PROGRESS liveness — a heartbeat of advancement (evidence rungs, landings, verify rounds); flatline for N minutes = dire even if every process is green
- (insight by user) scope check on phone pushes: there's currently no way to SOLVE anything from the phone — telar is a local, no-auth desktop cockpit
- (decision) right-sized: pushes are AWARENESS-ONLY — the knock kills false confidence (you know it stopped and why; it resumes when you're back); acting on it stays at the desktop. Remote answer/accept = explicitly a future workstream (needs auth+networking telar deliberately lacks today)
- (direction by user) FLAGGED FOR LATER: phone answering may be feasible after all — not an app but a companion WEBSITE (Vercel-hosted) to authorize/answer things remotely; capture as a future finding, not this spec
- (note) coach caveat logged with it: companion implies a relay (local telar publishes pending questions, pulls signed answers — local stays sole executor); scope v1 to answering blocked questions, NOT remote accept — the human-accept moat deserves its own design pass before any remote accept exists
- (technique) started Morphological Analysis (recipe schema grid: parameters x options x real fleet columns)
- (idea by coach) grid v1 built (schema-grid.md): 16 parameters with closed option sets, held against all 5 fleet archetypes — no column broke the schema; untried combos surfaced: test-gate+tenant-db (CI pattern local), cli-harness+lane, artifact-assert+server-lane, ephemeral+carry
- (question) three open cells put to user: A) playwright webServer ownership (rec: lane owns, suite reuses via PORT+reuseExistingServer); B) fleet port strategy for stock-port supabase (rec: serialize v1, remap later); C) recipe home (rec: map region as source of truth compiling to servers.yaml projection)
- (idea by coach) Playwright is a verification gate, not an infrastructure owner: the lane boots and leases the server, while the suite receives PORT and reuses the existing service; nested webServer startup is rejected or adapted.
- (question by user) scenario stress: 3 projects with a loom each + a 4th loom on an extra dev path of one project — too many supabase stacks? avoid multiple looms per project? does the session attach? what when looms' migrations collide?
- (insight) scenario exposed a hidden assumption: 'lab lives as long as the loom' — false; the evidence ladder already made verification EVENT-based (landings, final), so heavy infra can be leased just-in-time per verify round and released
- (idea by coach) just-in-time heavy leases + a fleet-level HEAVY-INFRA SEMAPHORE (default 1-2 concurrent stacks, queue beyond) — unifies with stock-port serialization; RAM fear dissolves via time-sharing
- (idea by coach) same-project looms SHARE the project stack (refcount); migration divergence: plain-PG projects -> tenant-db with each loom's branch migrations applied to its own tenant; gateway-mode (supabase API) -> mutex + reset-on-acquire, made cheap by TEMPLATE CACHE keyed by migration-set hash
- (idea by coach) the TRUST WALL: sessions MAY adopt a user-started foreign stack (convenience, attach as owner); looms NEVER — evidence comes only from telar-owned labs; adoption is allowed for working, forbidden for proving
- (question by user) development of the scenario: when 2 looms work one project — same branch or not? if not, how does verification occur; if yes, how is that even possible?
- (idea by coach) answer shape: looms NEVER share a branch (branch-per-loom is constitutional); verification has TWO proof layers — each loom proves ITS branch concurrently in its own borrowed lab; the fleet LANDING QUEUE proves combinations serially (rebase onto moved main -> re-verify same contract -> merge)
- (idea by coach) ordering rec (new cell D): ACCEPT-THEN-LAND — human judges the branch evidence; landing (rebase+re-verify) is mechanical afterward; a failed landing re-verify re-opens repair and knocks only if repair exhausts; alternative land-then-accept makes the queue stall on human latency
- (idea by coach) stacked looms as an explicit declared relationship (loom B based on loom A's branch — the 'extra dev path' case): B verifies against A+delta, landing order constraint B-after-A; never implicit
- (question by user) how can 2 branches exist materialized on the same repo locally without worktrees?
- (decision) resolution: you can't — one working dir = one checked-out branch; concurrent same-project looms REQUIRE separate materializations. Revised worktree doctrine: user's primary checkout is sacred (sessions only, never looms); worktree-per-LOOM is MANDATORY (it's where the loom branch lives, the warm lane runs, landings update, carry files are planted); worktree-per-THREAD stays optional-but-preferred (single serial thread may work directly in the loom's worktree)
- (decision by user) TWO-ALTITUDE VERIFICATION locked: thread level = bmad-review-style (code review + checks, serviceless, always runs); loom level = 'is this what the user requested?' — full lab standup, agents get a live playwright playground, evidence as output; HARD RULE: only one loom verification per repo at a time (per-repo verification mutex)
- (insight by user) this unsticks the bmad failure (threads never reaching approval) and enables full implementation testing — the loop finally closes
- (idea by coach) consequence: WINDOW-ON-DEMAND for heavy projects — opening the window IS a pull that borrows the stack (stands the lane while you look, releases after); infra-free projects keep always-warm windows
- (idea by coach) seed Q3 resolved by the two altitudes: streaming/mid-run evidence = thread-level rungs (review findings, tests, gates — live and cheap); surface evidence = verification events (loom verify, landing re-verify — both count under the per-repo mutex)
- (decision by user) cell D resolved: judge the PRODUCT the loom came out with — after its own verify/repair loop converges (the loom finds bugs and re-sends itself to work); final judgment on the ready product 'so I can see it in work'; landing is mechanical after accept (accept-then-land)
- (decision by user) rejection is a BOOMERANG, not a bin: on non-approval the user can send the loom back with feedback to finish what it started — loom keeps its branch, worktree, and recipe; resumes cheap
- (note) spec nuance: post-accept landing — clean rebase+contract-pass lands silently; if landing repair must modify code, surface a delta note on the done card; contract-fail re-opens repair and knocks only when exhausted
- (note) mid-session visual checkpoint produced (session-summary.html): five promises, physical picture (sacred checkout / loom worktrees / borrowed infra + mutex), two-altitude flow with boomerang, 16-param schema, decision list, open items
- (technique) started Devil's Advocate Courtroom — the evidence on trial (seed Q4: what makes surface evidence trustworthy)
- (decision by user) courtroom verdicts: all three countermeasures approved — (1) lab GRADES, release-grade accept-gating verify ('excellent, hadn't thought of that'); (2) evidence ledger: harness-captured artifacts + narrative must cite them, uncited = marked unverified; (3) attempt history on the card ('of course, we keep a history') + flaky flag
- (decision) grade placement (assented as proposed): landings verify at dev grade for speed; the accept-gating ALL-verify and the post-accept landing re-verify run at release grade
- (technique) started Decision Tree Mapping — readiness-node failure branches (seed Q2)
- (decision by user) fork A resolved: every loom start spends a few tokens comparing current project state to the last run's (the intake diff); unchanged -> trust the recipe, drift -> full re-prove by booting. 'A few tokens to verify state vs last run is not a bad deal'
- (decision by user) fork B resolved: degradation is a PREP-TIME concern — the readiness node surfaces it before the gate, so the user decides with full knowledge before any spend; it shouldn't surface mid-run. In the rare mid-run case: escalation ladder — ask the ORCHESTRATOR first (mediation), then the user only if unresolved
- (insight) fork B answer preserves two standing invariants for free: 'children escalate to mediation, never to humans' and the dire razor (user is knocked only when no other path remains)
- (decision by user) loom priority order for MVP recipe coverage: 1) ozom-gv, 2) orchestrator, 3) TELAR ITSELF (dogfooding — looms building telar under a sandboxed TELAR_HOME)
- (insight) SYNTHESIS: the whole architecture is Facundo's local workflow formalized — 'one server per line of development' became worktree-per-loom + warm lane + per-repo verification mutex; the loom works the way its user works
- (insight) SYNTHESIS: evidence is one subsystem wearing three hats — proof for the card (accept), proof-of-life for the heartbeat (stall detection/promise 2), and forensics for the courtroom (ledger+provenance); designing it once serves all three
- (insight) SYNTHESIS: the empirical fleet pass paid the biggest dividend — carry (the one novel schema field) and the kickstart.ts discovery (the readiness node already hand-written) both came only from surveying the real projects; chosen direction: unbundle telar's existing lane stack, add leases + tool surface + wire the preparing hook
