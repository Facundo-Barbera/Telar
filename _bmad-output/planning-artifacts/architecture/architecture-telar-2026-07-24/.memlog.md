---
scope: Initiative spine over three SPEC packages landing on the telar monorepo: SPEC-loom-redesign, SPEC-organization-workspace, SPEC-ultra-workflows. Brownfield — packages/core + apps/web (Next 16 canary) + apps/desktop.
purpose: Fix the invariants that keep the three features from diverging where they overlap (session surface, TELAR_HOME state root, cross-module handovers, agent capability walls). Deliverables: spine + walkthrough deck + work-split view + fuller solution design.
altitude: initiative → features
updated: 2026-07-24T21:42
---

- (constraint) ADOPTED from project-context.md: @telar/core is server-side only; client components import types only. Runtime core code lives only in Route Handlers, Server Components, instrumentation.ts. Not re-decidable.
- (constraint) ADOPTED: App-Router-as-backend, in-process monolith. All server surface is app/api/**/route.ts. No pages/api, no Server Actions, no middleware.ts. No auth layer (local single-user cockpit).
- (constraint) ADOPTED: TELAR_HOME is the entire state root; all engine state filesystem-backed; atomic writes only (.tmp -> renameSync). zod schemas for persisted entities owned by @telar/core.
- (constraint) ADOPTED (highest-priority invariant): Human-Accept Moat — ready->done is human-only, no agent-callable accept tool anywhere, enforced twice (core by construction + web PreToolUse hook). Verifier stack is capability-walled: verifier.ts/verify-thread.ts/critic.ts/panel.ts hold no write/edit tools.
- (constraint) ADOPTED design law: deterministic control flow in code, intelligence in the leaves. tick/decide/rollupWeave/decideRepairContinuation are pure over ints, sets, injected clocks; LLM calls isolated behind schema-forced agent() with explicit tool allow/deny.
- (constraint) ADOPTED: no global client-state library; useState/useEffect + fetch + window 'telar:refresh' event + poll. SSE hand-rolled (raw ReadableStream of event:/data: frames, lib/sse.ts consumeSSE; GET tails via native EventSource). No useChat, no EventSource polyfill.
- (constraint) ADOPTED stack (brownfield, verified in repo): Bun workspaces monorepo; packages/core @telar/core TS ^6.0.3 ESM no-build; apps/web Next ^16.3.0-canary.80 App Router + React 19.2.4 + Tailwind v4 + shadcn on @base-ui/react + ai ^7; apps/desktop Electron ^43 mac/arm64 only. bun test only tooling. No CI.
- (constraint) ADOPTED from SPEC-loom-redesign map-and-storage.md: MapStore adapter with two backends (in-repo dir | TELAR_HOME dir); lockless collision protocol pin -> propose -> serial land -> rebase-adapt -> human rules semantic contradictions; map is a regenerable projection; one file per region; regions declared by loaded methodology not the engine.
- (constraint) ADOPTED storage trees, each decided independently by its own SPEC: looms -> TELAR_HOME/projects/<id>/looms/<loom-id>/ (canonical per-loom root, incl. service leases; legacy flat ~/.telar/looms/<id>/ read-only until drained); workspace -> TELAR_HOME/workspace/{home,lanes.yaml,packets/<item-id>/packet.yaml}; ultra -> TELAR_HOME/ultra/<runId>/.
- (constraint) ADOPTED: cross-surface access goes through in-process MCP servers (lib/loom-mcp.ts, lib/ultra-mcp.ts, + new workspace MCP), mounted in the chat route with strictMcpConfig:true, inheriting the PreToolUse guardrail. Sessions never get raw filesystem access to another module's store.
- (assumption) Known gap named by SPEC-loom-redesign brownfield: there is NO per-chat-session directory under TELAR_HOME, so session-owned processes have nowhere to lease from — blocks CAP-11 'one primitive, two lifetimes'.
- (assumption) Known gap named by SPEC-organization-workspace brownfield: Codex sessions receive no MCP servers today (runCodexTurn has zero MCP plumbing); resolveProjectMcpServers is wired only in the Claude branch. Required work for a Codex-backed master.
- (event) Scope set: initiative spine over all three SPECs. Mode: coaching path. Deliverables: spine + walkthrough deck + work-split view + fuller solution design.
- (direction) Coaching round 1 opened on paradigm.
- (decision) PARADIGM (user-chosen, coaching round 1): three laws, one per layer. (1) Control — functional core, imperative shell: core decisions pure over ints/sets/injected clocks, effects at edges, non-determinism only in agent() leaves. (2) Boundaries — ports & adapters: every cross-module crossing is a typed port; MCP servers driving, MapStore/provider-harness/service-lane driven; no module reaches into another module's store. (3) State — log + lazy projection ONLY where declared (ultra journal, drift ledger, evidence); workspace packets, manifests and lane files are single-owner documents. Binds: all three SPECs. Prevents: a builder hitting an uncovered case and inferring the wrong model — esp. event-sourcing the workspace store, or reaching across a store boundary because 'it's all one process'. Rationale: the four real collision zones found in scan are boundary-shaped, so boundaries must be a first-class law, not a sub-law; law 1 ratifies the existing project-context.md design law at zero migration cost; law 3 is scoped explicitly because the log/projection claim is only true for some artifact classes.
- (direction) Rejected paradigm framings (do not re-litigate): single 'functional core' name (demotes boundaries, which is where these three features actually collide); pure hexagonal (reframes settled project-context.md design law as a rewrite); no-paradigm rules-only (leaves uncovered cases with no fallback model).
- (direction) Coaching round 2 opened: TELAR_HOME state ownership — session tree + lease primitive, schema evolution, cross-tree references, persisted-format convention.
- (decision) STATE OWNERSHIP: one owner per TELAR_HOME subtree, declared. Owners: projects/<id>/looms/<loom-id>/ -> looms module; workspace/ -> organization-workspace module; ultra/<runId>/ -> ultra module; NEW sessions/<sessionId>/ -> session module. No module reads or writes another's subtree by path; access is through the owning module's port only (law 2). Prevents: three features each inventing a layout and a fourth reaching in by path because 'it's all one process'.
- (decision) SESSION TREE + LEASE (user-chosen, round 2): add TELAR_HOME/sessions/<sessionId>/ owned by the session module. Generalize runner/lease.ts into ONE lease primitive ({pid, token, ts}, atomic temp+rename, heartbeat, stale-reclaim) used by BOTH loom-owned services (under the per-loom root) and session-owned services (under the session root). Lifetime differs — loom-owned dies at land, session-owned dies at session close — the record shape and reclaim semantics are identical. Binds: loom CAP-11/CAP-12, ultra manifest sessionId link. Prevents: two stale-reclaim implementations drifting, which is how a false-positive 'done' gets issued. Delivers CAP-11's 'one primitive, two lifetimes' literally.
- (decision) PERSISTED FORMAT follows artifact class (architect call, confirmable): human-editable -> YAML (lanes.yaml, packet.yaml); machine single-doc -> JSON (manifest.json, decision-graph.json); append-only stream -> NDJSON (journal.jsonl, events.ndjson). All three SPECs already picked this independently; stated so a fourth module does not invent a fourth answer. All writes atomic (.tmp -> renameSync). zod schemas owned by @telar/core.
- (decision) SCHEMA EVOLUTION (user-chosen, round 2): tolerant readers (zod fills defaults, unknown fields survive) + regenerate what is regenerable. An explicit schema-version field and a real migrate-on-read go ONLY on stores holding unrecoverable human input — workspace packet.yaml above all (holds 'raw' verbatim, never overwritten). Regenerable stores (living map regions, projections, evidence views) are rebuilt by a reconciliation loom rather than migrated. Prevents: losing months of the user's own captured notes to a breaking change, without paying migration ceremony on every store.
- (decision) CROSS-TREE REFERENCES (user-chosen, round 2): weak refs. A stored cross-module ref is an id plus enough denormalized label to render without a lookup; live resolution always goes through the owning module's port, never a path read. A dangling ref renders as a tombstone and NEVER throws. Binds: item.tracking -> loom id (workspace CAP-11), ultra manifest -> sessionId+messageId, loom premise <- packet. Prevents: inbound-dependency webs (cascade deletes) that law 2 forbids, and stale denormalized snapshots that would break 'row stays until the loom lands AND the human accepts'. Survives the planned legacy-loom drain by construction.
- (direction) Coaching round 3 opened: the two 'one file, three features' collisions — app/api/chat/route.ts chokepoint and the session-view.tsx / Conversation shell carve-out, plus the contested right rail.
- (event) round 2 closed
- (decision) CHAT ROUTE SHAPE (user-chosen, round 3): a typed SessionProfile is resolved BEFORE the route body — {cwd, guardrails, settingSources, mcpServers[], toolPolicy, systemPromptAppendix}. Project session, master session, loom-node session and steerer are profiles, not if-branches. Workspace's 'skip the project gate' becomes a profile supplying a synthetic cwd (TELAR_HOME/workspace/home) rather than a special case in the handler. New surfaces add a profile. Binds: all three SPECs. Prevents: three features accreting conditional branches in one 103KB handler (the planner/steerer branches already show the accretion).
- (decision) MOAT UNDER PROFILES (user-chosen, round 3): the PreToolUse guardrail is wired into the pipeline OUTSIDE the profile and runs for every session regardless of profile. A profile's toolPolicy is intersect-only — it can deny more, never grant more — enforced by the type (deny lists and allow-narrowing only; no field can re-enable an accept path). Prevents: the Human-Accept Moat degrading from structural to configurable, and a mis-authored profile silently omitting it. Keeps project-context.md's non-negotiable invariant non-negotiable.
- (direction) Rejected (round 3, do not re-litigate): separate routes per session kind — would duplicate PreToolUse moat enforcement into every path, giving the moat three chances to be forgotten.
- (constraint) ADOPTED from conversation-component.md (companion of BOTH loom-redesign and organization-workspace): the Conversation family is three layers — primitives, the Conversation shell (transcript via item-kind registry + composer slot + right-rail slot + header slot, config over inheritance, owns scrolling/auto-follow/streaming, owns NO data fetching and NO session semantics), and owner adapters (ProjectSessionView, MasterChat, NodeConversation, LoomSessionView, TranscriptView). 'One slot, many rails' already answers who owns the right rail — subagent rail, Desk, chat-history rail and evidence rail are all the same slot. 'Session' stays a domain word; the UI concept is Conversation.
- (decision) SHELL SEQUENCING (user-chosen, round 3): this spine FREEZES the Conversation shell contract (four slots + item-kind registry). An epic-0 story does the pure-render carve-out and migrates production session-view.tsx onto it. The three features build owner adapters against the frozen contract CONCURRENTLY, proven in the demo gallery until epic 0 lands; only final wiring waits. Prevents: a three-way merge conflict on a 151KB file, and a sixth hand-built copy of the chat surface — the exact failure the teardown doc was written to stop.
- (decision) ITEM-KIND REGISTRY NAMESPACING (architect call): registered transcript item kinds are namespaced by owning module (ultra:run-anchor, loom:gate-card, workspace:receipt). Prevents: three features registering a colliding bare kind id in one shared registry.
- (direction) Coaching round 4 opened: async event delivery + agent wake vs human notification, harness capability parity (Claude/Codex), and the operational envelope (restart/crash doctrine, environments).
- (event) round 3 closed
- (decision) EVENT DELIVERY (user-chosen, round 4): one typed in-process event bus in core as the single publish path; adapters subscribe — SSE tails (UI), a session-wake injector (agent), the dock aggregator. Every event carries a REQUIRED delivery class: agent-facing (may synthesize an assistant turn — ultra completion CAP-1, loom escalation) or human-facing (renders on a surface the user arrives at, never pushes — workspace, dock badges). Prevents: the apparent contradiction between ultra CAP-1's 'unprompted assistant turn' and workspace CAP-1's 'never initiates contact' being resolved differently by each builder. Makes 'notifications are a dead channel' structural rather than a remembered habit.
- (direction) Rejected (round 4): filesystem-watch-derived events — inverts law 2 (a watcher reads every module's subtree by path) and fs-watch semantics differ between the dev server and the packaged Electron app.
- (decision) HARNESS CAPABILITY PARITY (user-chosen, round 4): a SessionProfile declares its required harness capabilities; the provider port publishes what it supports; an unmet requirement is a hard error BEFORE the stream opens — same posture as the chat route's existing pre-SSE 400. No silent degradation. Binds: the Codex MCP gap (organization-workspace brownfield) becomes ordinary work rather than a correctness prerequisite. Prevents: a Codex-backed master coming up with no workspace MCP and confidently answering 'where did I stop' from nothing.
- (decision) RESTART/CRASH DOCTRINE (user-chosen, round 4): no in-process work survives a server restart and nothing auto-restarts. On next read a store reconciles a stale 'running' into a terminal-but-resumable state and offers explicit Resume. Generalizes ultra's existing manifest reconciliation and the lease rule ('a stale lease can at worst produce a false-positive failed, never an auto-done') into one doctrine. Prevents: a fourth module inventing a fifth restart behaviour, and auto-resumption advancing work with no human in the loop (which would cut against the accept moat).
- (direction) Rejected (round 4): supervisor re-adoption of in-flight work on boot — better for hours-long looms but auto-advances work without a human; would need its own guard rails. Revisit condition: if dev-server reloads are measured to be losing expensive loom builds regularly.
- (direction) Coaching round 5 opened: cross-feature resource governance (concurrency on one laptop), spend accounting, and how these invariants stay true with no CI.
- (event) round 4 closed
- (version) Verified in repo 2026-07-24: engine.ts MAX_CONCURRENT=4 is a PROCESS-WIDE gate every schema'd agent() call joins; ultra/executor.ts stacks a SEPARATE run-local semaphore cap 3 on top; schemas.ts Budget.maxAgents default 12 per charter (budget.ts DEFAULT_MAX_AGENTS=12); vcs.ts holds a module-level async worktree mutex serializing worktree add/remove and branch-create.
- (decision) ADMISSION CONTROL (user-chosen, round 5): the process-wide gate becomes a typed admission controller with classes — interactive-session, loom-build, loom-verify, ultra — each with a reserved minimum and a weighted share of ONE configurable ceiling. Interactive holds a guaranteed slot so the cockpit is never dead while the fleet runs. Charter.budget.maxAgents becomes a per-loom clamp WITHIN its class, not a competing number. Prevents: the loom SPEC's '7+ concurrent looms is the norm' colliding invisibly with a hardcoded MAX_CONCURRENT=4, an ultra fan-out stalling the fleet, and a fleet starving the user's own session.
- (decision) SPEND (user-chosen, round 5): one append-only spend ledger. Every agent call writes one attributed record (owner-kind + owner-id + provider + amount). The session per-turn usage display, ultra's manifest spend (CAP-5) and the loom charter's budget-left are all PROJECTIONS over that log, not three independent counters. Law 3 applied where it genuinely holds. Prevents: three counters that can disagree, the USD-vs-tokens cost-language split being re-solved per feature, and 'what did today cost across everything' being unanswerable.
- (decision) INVARIANT ENFORCEMENT (user-chosen, round 5): the load-bearing rules become executable assertions in bun test, run in the existing manual pre-commit trio (bun test / bun run lint / bunx tsc --noEmit). At minimum: no MCP surface exposes an accept tool; verifier/verify-thread/critic/panel are granted no write tools; no module reads another module's TELAR_HOME subtree by path; client components import no core runtime. Mirrors the moat's 'enforced twice' ethos. CI was offered and not taken — enforcement stays manual-trigger.
- (direction) Rejected (round 5): adding CI (declined as scope beyond the three SPECs); per-feature spend counters; per-class pools with no global ceiling; stacked independent concurrency gates.
- (event) round 5 closed — coaching complete, moving to distill
- (event) Reviewer Gate run inline (user's global config forbids unrequested subagents). lint_spine.py: 0 findings, before and after fixes. Three lenses: rubric walker, reality-check, adversarial-seams. Full reviews in reviews/.
- (decision) AD-18 CORRECTED by reality-check lens: the repo ALREADY ships ~/.telar/usage.ndjson (logUsage/UsageEntry in apps/web/lib/store.ts, with account/model/sessionId/costUsd + usageSummary + plan-usage.json). AD-18 originally invented a new spend.ndjson — a rule written to stop three counters disagreeing would have created a fourth. Now: extend usage.ndjson with owner-kind + owner-id; no new file.
- (assumption) OPEN ITEM bound to AD-18's story: apps/web/lib/store.ts:8 hardcodes path.join(os.homedir(),'.telar') and does NOT honor TELAR_HOME, unlike manifest.ts, looms.ts, session-log.ts, permissions.ts and vcs.ts. So chats.json, usage.ndjson and plan-usage.json do not isolate under ~/.telar-dev. Pre-existing defect; becomes load-bearing once spend is built on usage.ndjson, because dev runs would write into production state. Must be fixed first.
- (decision) AD-20 ADDED (adversarial lens): shared runtime services are the sole writer of their own state. AD-5 assigns owners to SUBTREES and so never reached root-level state belonging to no module (usage ledger, bus subscriptions, admission accounting) — two modules could each append their own record shape while both technically obeying AD-5.
- (decision) AD-21 ADDED (adversarial lens): a module's published event names and payload shapes are part of its port contract, as binding as tool signatures; cross-module subscription only to declared events. Closes the seam where looms renames loom:landed during unrelated work and workspace queue rows silently never leave the list (OW CAP-11).
- (decision) AD-12 AMENDED: a registered item-kind renderer is a pure function of (item payload, shell-provided view state) and reads nothing from ambient context — otherwise ultra's context-reading anchor and loom's payload-reading gate card cannot coexist in one transcript, and TranscriptView can render neither.
- (decision) AD-15 AMENDED: because the doctrine guarantees reconciliation and never rollback, any repo- or map-mutating operation (landing, folding, delta application) must be idempotent and resumable from its own journal — else a restart mid-land leaves a half-folded repo that Resume re-runs on top of.
- (decision) AD-17 AMENDED: admission governs agent() concurrency ONLY. Long-lived processes (labs, declared services, borrowed infra) take no slot; governed by the lease (AD-16) and the per-repo worktree mutex. Closes the seam where LR CAP-11's epic counts a lab standup as occupancy and LR CAP-13's does not.
- (decision) AD-5 AMENDED: sessions/<sessionId>/ holds session-scoped RUNTIME state (leases, ephemera) and does not replace the existing chats.json store, which keeps its root-level home. Prevents one builder migrating chat history into the new tree while another leaves it.
- (decision) CONVENTIONS: logging/diagnosis row added (rubric found the observability dimension silent) — a module's durable trace is its own NDJSON stream under its subtree, readable with tail/jq with the app down; console output is development-only and never the record; LR CAP-15 progress-liveness reads those streams rather than a separate telemetry path.
- (event) spine finalized — status: final, 21 ADs, all 43 capabilities mapped (LR 1-24, OW 1-13, UW 1-6)
- (event) Deliverables complete: ARCHITECTURE-SPINE.md (build substrate), WORK-SPLIT.md (6 tracks, disjoint write sets, 4 real serialization points), SOLUTION-DESIGN.md (rationale the spine omits), ARCHITECTURE-DECK.html (10-slide self-contained walkthrough), reviews/ (3 lenses).
- (version) CORRECTION verified in code: apps/web/app/api/chat/route.ts:1171 calls the SDK query() DIRECTLY — interactive chat sessions never enter engine.agent(), so they never joined MAX_CONCURRENT. AD-17's original claim that 'a fleet can starve your own chat session' via this gate was WRONG, and a reserved interactive-session slot would have been meaningless. Interactive is out of band by design; classes are loom-build, loom-verify, ultra, other.
- (decision) AD-17 AMENDED + IMPLEMENTED. Verification gets PRECEDENCE, not a held-open slot. A hard reservation was built first, then rejected on evidence: it made a pure-Ultra workload peak at 3 of 4 (breaking test/ultra-runner.test.ts's peak===4 assertion) and permanently burns 25% of a 4-slot machine to insure against a wait that is already bounded. Final design: no barging past a queue + a freed slot goes to entitled waiters in class-priority order (loom-verify first) before any borrower, plus a work-conserving borrow pass so no slot idles with work queued. Verify therefore waits at most one in-flight call, never a whole fan-out.
- (decision) SHIPPED: packages/core/src/admission.ts — pure policy (entitlement/availableFor/admissionCheck: no I/O, no clock, all args) + a queue. Ceiling from TELAR_MAX_AGENTS, default 4 (unchanged from the historical engine.ts value, so adopting the controller is not itself a throughput change). weight 3/2/2/1, floor 1/1/1/0, priority [loom-verify, loom-build, ultra, other]. Exported from index.ts including admissionSnapshot() for observability.
- (decision) BUG FIXED (pre-existing, found while reading engine.ts:89-99): the old semaphore could exceed its own ceiling. A woken waiter did active++ without re-checking, so an arrival landing between release() and that resumption over-committed the pool. The new controller takes the slot BEFORE waking the waiter and queues all arrivals once anyone is waiting. Covered by a regression test.
- (decision) budget.ts fanoutClamp gained an optional processCeiling term + capByProcess + binding 'process'. Optional so every existing call site keeps exact behaviour; tie-break order pool -> budget -> process preserves prior reporting. This is what lets a Charter stop promising 12 agents under a ceiling of 4.
- (event) Verification: 1317 pass / 0 fail across 99 core test files (22 new in test/admission.test.ts); core bunx tsc --noEmit clean; web bunx tsc --noEmit clean. Web 'bun run lint' fails with 77415 pre-existing problems — scoped entirely to apps/web (1176 paths under .next-desktop build output), zero references to packages/core, so untouched by this change.
- (assumption) REMAINING (recorded in spine Deferred): executor.ts's 5 agent() call sites still default to class 'other' rather than 'loom-build' — safe but means build work does not yet compete as its class; tagging needs each site read in a 155KB file and a mis-tag silently changes scheduling. And processCeiling is not yet passed by tick.ts/executor.ts, since doing so changes scheduler output and wants its own test sweep.
- (direction) USER CORRECTION: the ask 'can we fix the max agent thing' meant WRITE THE SPEC FILE, not implement. Implementation reverted (git checkout of 6 core files + rm admission.ts/admission.test.ts); tree verified back to MAX_CONCURRENT=4 at engine.ts:89. Working implementation preserved in the session scratchpad at admission-impl/ (admission.ts, admission.test.ts, core-changes.patch) so nothing is lost if it is wanted later.
- (decision) Track A now has its own spec package: _bmad-output/specs/spec-runtime-foundations/ (SPEC.md + admission.md + brownfield.md + stories.yaml), chosen over folding into spec-loom-redesign because these are shared invariants no single feature SPEC owns and all three depend on them. CAP-1..CAP-6 map to WORK-SPLIT Track A units A1..A6; 7 stories with CAP-1 ordered strictly before CAP-2.
- (decision) admission.md records the REJECTED hard-reservation design explicitly, with the two reasons it was rejected (breaks ultra-runner.test.ts peak===4; burns 25% of a 4-slot machine), so a dev pass does not re-derive it. Also records the pre-existing barging bug and the interactive-out-of-band correction.
- (event) Stale implementation claims corrected across the architecture artifacts after the revert: AD-17 no longer names packages/core/src/admission.ts as existing ('becomes' not 'is'); Deferred entries no longer say the mechanism 'is built'; SOLUTION-DESIGN.md's interactive-always-has-a-slot and reserved-minimums passages replaced with the two corrections; WORK-SPLIT Track A table now cites the spec's CAPs and story ids; deck slide 8 redrawn (no interactive-session class, precedence not reservation).
