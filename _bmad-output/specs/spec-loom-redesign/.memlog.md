---
topic: Loom redesign — the three-act loom (prepare/execute/judge), its verification lab model, living map + artifact storage, and the six UX surfaces
updated: 2026-07-24T13:23
---

- (direction) Facundo: one new spec for looms, distilled from all four brainstorm sessions whose name carries "loom" (system, verification, ux-ui, artifact-storage), with organization-workspace treated as mildly related.
- (decision) One spec + companions rather than a split by domain — one kernel, one stable CAP-N space across spine/verification/UX/storage, so downstream never reconciles three contracts.
- (decision) Organization-workspace enters only at the loom-birth seam (queue batch-weave, ripened-packet handover, detach receipt); the workspace module itself gets its own spec later, so its six unresolved questions stay out of this kernel.
- (decision) Contract covers the full redesign, not an MVP slice: everything locked across the four sessions is the contract; July-18 MoSCoW Could/Won't items become non-goals unless a later session superseded them.
- (decision) Slug = loom-redesign, matching the July-18 source title and signalling brownfield over the loom engine already running in production.
- (note) Sources read: the four loom brainstorm-intent.md files + schema-grid.md + organization-workspace intent. next-session-ui-ux.md and next-session-verification.md are spent briefs (both sessions ran) — process metadata, not sources.
- (decision) The July-18 MoSCoW "Won't (this time)" entries for verification deep design and UI/UX rebuild are spent: both sessions ran (2026-07-22, 2026-07-23) and their outcomes are in this contract. Chronology supersedes.
- (decision) Three Could items were promoted by later sessions and enter the contract: fleet lanes + landing queue (verification made serial landing constitutional), streaming-evidence early-kill (park/kill verbs landed in the UX 2 cockpit header), adaptive ceremony templates (superseded by "ONE design at any size" + the ceremony dial).
- (decision) Vision critic promoted from Could to in-contract but advisory-only — UX 3's final delivery card renders its verdict in the courtroom section; it never gates Accept. Logged as an assumption since July-18 called it Could.
- (decision) The Conversation-component extraction is HOW, not WHAT — it lands as a constraint plus the adopted conversation-component.md companion, never as a capability (Spec Law rule 2).
- (capability) CAP-1 Loom birth and detach — session hands over premise + optional context; the chat keeps only a detach marker and a one-line mono receipt. Same grammar from the workspace queue batch-weave and a ripened packet. Below the simple-task boundary nothing detaches at all.
- (capability) CAP-2 Decision graph — a true branching DAG that converges into the gate; every node is a real session that must happen or be explicitly skipped; nodes spawn only where the intake diff finds drift.
- (capability) CAP-3 Living map + lazy intake diff — adaptive project model in fine-grained region files, diffed against main/default-branch head at prep and again at land; adapts, never accumulates.
- (capability) CAP-4 Verification-readiness node + recipe — proves the lab stands up before any build spend, emits the per-project recipe as a map region, declares degraded modes at prep time. Fail-closed: no lab, no experiment.
- (capability) CAP-5 Readiness gate — the entrance moat: Accept / Modify on the go / Deny straight to development, with the degraded-mode ack gating Accept.
- (capability) CAP-6 Approval-gated advance — advance_node, weave_batch and the ack-gated Accept are one protocol shape: proposal, explicit human approval, then the effect. The only way an agent moves a node forward.
- (capability) CAP-7 Specialist seat registry — graph nodes are staffed by named seats from a growable registry; fixed seats first.
- (capability) CAP-8 Orchestrator ownership + role wall — after the gate the orchestrator owns created-to-delivered; conductors never touch code and carry their own role in the UI.
- (capability) CAP-9 Flow compile — a thread's main agent authors a schema-validated DAG (parallel lanes, dependency edges, per-node context manifest) that is then executed deterministically; recompile is explicit, bounded and audited; pre-written steps survive as the low-confidence fallback template.
- (capability) CAP-10 Branch and worktree isolation — branch-per-loom constitutional, worktree-per-loom mandatory, worktree-per-thread optional-but-preferred, the user's primary checkout untouched, stacked looms declared explicitly.
- (capability) CAP-11 Declared services + supervisor-owned labs — agents declare via ensure_service and never spawn; telar's supervisor spawns, owns, probes, restarts and tears down under a per-owner lease; one primitive serves both sessions and looms; the UI reads the registry.
- (capability) CAP-12 Borrowed heavy infra — per-service scope project/loom/ephemeral, refcounted project stacks, a fleet-wide heavy-infra semaphore, and just-in-time leasing per verify round rather than for the loom's lifetime.
- (capability) CAP-13 Two-altitude verification — thread altitude (does the code hold: review + checks, serviceless, streaming, cheap, always) and loom altitude (is this what you asked: full lab, live Playwright, capability-walled, gated by the per-repo mutex).
- (capability) CAP-14 Pause, park and resume — durable execution state; a loom parks on usage/credit exhaustion or user pause and resumes later, possibly under a different account or provider.
- (capability) CAP-15 Progress liveness + dire-razor escalation — the supervisor watches process liveness, the orchestrator watches progress liveness; a flatline is dire even when every process is green. Push only when the loom has no viable path without the human.
- (capability) CAP-16 Evidence subsystem — one subsystem wearing three hats (accept proof, heartbeat proof-of-life, courtroom forensics): provenance-stamped ledger, cited narrative, full attempt history with flaky flags, dev/release lab grades, reality manifest.
- (capability) CAP-17 Delivery card — shelf row for fleet-scale skim, taste-first open card led by the live product on its frozen final-verify lane, courtroom below the glass, one shared VerdictBar per delivery.
- (capability) CAP-18 Accept-then-land + landing queue — the human judges branch evidence first; landing is mechanical afterward, serial, rebased and re-verified at release grade, silent on a clean pass.
- (capability) CAP-19 Boomerang — rejection sends the loom back with feedback keeping its branch, worktree and recipe, so it resumes cheap. Never a bin.
- (capability) CAP-20 Vision critic — an advisory seat judging a delivery against the map's objective and form regions; renders on the card, never blocks Accept.
- (capability) CAP-21 Map write-back — drift ledger declared on accept, proposals as the only write path, serial landing as the only commit path, mandatory rebase-adapt when head moved, semantic contradictions surfaced to the human on the accept card.
- (capability) CAP-22 MapStore + artifact storage split — loom-run artifacts always in TELAR_HOME, project knowledge in-repo by default as per-region markdown, behind one adapter with a per-project reversible dial.
- (capability) CAP-23 Home / fleet triage — a graft onto the existing dashboard: Needs-you top-left splitting into a delivery shelf and parked questions verbatim, running rows carrying act chip and evidence age, done-today receipt, hot projects with map drift.
- (capability) CAP-24 Loom cockpit — persistent Prepare/Execute/Judge act tabs with park and kill in the header, the sealed prep DAG as a frozen receipt, the conductor pinned over thread rows, thread drill into the compiled flow and live transcripts, and the WINDOW / LAB / CHATS header trio.
- (constraint) The four structural walls are the constitution and never bend: verifiers cannot write, conductors cannot code, agents cannot accept, and nobody writes the map silently.
- (constraint) ONE design for looms at any size — no small/medium/large UI split. Below the simple-task boundary work stays a plain session with no loom and no graph; the moment it becomes a loom it takes the full shape. Ceremony scales; the branch, evidence and human-accept walls never do.
- (constraint) No file locks anywhere on the map — looms run for hours and locks would starve the fleet. Propose-then-land-serially is what makes locks unnecessary.
- (constraint) Only human-meaningful markdown lands in-repo, one file per map region; machine artifacts (DAG JSON, evidence, HTML) always stay in TELAR_HOME referenced by loom id.
- (constraint) Only one loom verification per repo at a time (per-repo verification mutex); heavy infra is never duplicated for concurrent verifies of the same repo.
- (constraint) Trust wall: sessions may adopt a user-hand-started foreign stack, looms never may. Adoption is allowed for working, forbidden for proving.
- (constraint) Data class production means refuse surface-verify, fail closed — a surveyed project's documented dev path pointed at live production Supabase.
- (constraint) Degradation is a prep-time concern surfaced before the gate so the user decides with full knowledge before any spend; it never surfaces mid-run.
- (constraint) Mid-run escalation asks the orchestrator first (mediation), the user only if unresolved; children escalate to mediation, never straight to humans. Pushes are awareness-only — acting stays at the desktop.
- (constraint) The lane owns the server: project-owned e2e suites reuse it via PORT + reuseExistingServer rather than launching a nested webServer.
- (constraint) The recipe is a map region that compiles to servers.yaml plus prepare/carry/verify sections — the region is source of truth, the compiled files are what the lane consumes.
- (constraint) v1 serializes fleet access to stock/colliding ports rather than rewriting committed port config.
- (constraint) Evidence must cite artifacts; uncited claims render as unverified. Every verify attempt stays on the card's history and flaky boots/tests are flagged, never silently retried into invisibility.
- (constraint) Landings verify at dev grade for speed; the accept-gating ALL-verify and the post-accept landing re-verify run at release grade.
- (constraint) The decision graph is never rendered inside the originating session's UI — it belongs entirely to the loom's own pages.
- (constraint) Tone law: no suggestion-text or doctrine captions anywhere in the UI; surfaces show state and data only.
- (constraint) Hand-edits to in-repo map files are allowed and treated as good — the lazy intake diff absorbs them as drift, the same as code drift.
- (constraint) New conversational surfaces are born on the extracted Conversation shell; the carve-out of session-view.tsx cuts at the render seam only and the shell owns no data fetching or session semantics.
- (constraint) One fractal pattern runs everywhere: declare, validate, execute deterministically, reconcile lazily. Storage, flow compile and map write-back all fall out of it rather than each being a fresh decision.
- (decision) Companions: lifecycle.md (three-act spine + states + walls + diagram), verification.md (lab model), recipe-schema.md (the 16-parameter grid), map-and-storage.md (regions, MapStore, collision protocol), ux-surfaces.md (the six surfaces + shared design language), brownfield.md (as-built engine). Adopted: recipe.schema.yaml, conversation-component.md, project-context.md.
- (event) Kernel + companion set derived from the four session intents; entering self-validate.
- (assumption) The three acts replace and absorb the existing production loom lifecycle (queued/scoping/charter-review/preparing/running/verifying/ready/done) rather than running beside it; the dead preparing-phase runSetup hook is the natural slot for the readiness node.
- (assumption) Demo-gallery entries under apps/web/lib/demo-gallery are design source of truth, not production code — production still runs the current dashboard and session-view.tsx.
- (assumption) recipe.schema.yaml in the verification brainstorm folder is the machine-readable draft the recipe map region compiles from; adopted as-is rather than re-derived in this spec.
- (assumption) Vision critic is in-contract because UX 3's final card renders it; the July-18 MoSCoW had it as Could, so its promotion is inferred from chronology rather than a direct user call.
- (question) Workstream grouping of Execute's thread rows was a new UX-session idea flagged unvalidated — persistent grouping, flat list, or filter chips?
- (question) The preview-this-thread escape hatch was identified as a cockpit gap and never designed. Where does it live?
- (question) Does v1 ship any fleet-level surface for landing-queue state, stacked looms, or a project/region map, or does the dashboard graft stand alone?
- (question) A loom may carry multiple tasks sized by its accept moment, but only the workspace queue-weave path demonstrates it — what do the gate and cockpit show for a multi-task loom?
- (question) Final map-region taxonomy is unresolved: which regions exist beyond objective, architecture, surfaces and verification, and what does stale mean per region?
- (question) carry propagates gitignored secret files into worktrees, lab checkouts and ephemeral previews, and evidence captures screenshots of seeded data — no retention, scrubbing or teardown policy is declared for either. Needs a human call before build.
- (question) What does the workspace queue show once a batch-woven loom series is actually running? Unaddressed in the workspace session and owned by this seam.
- (question) The workspace task/packet store format is undecided upstream, and the batch-weave and packet-handover paths read from it. Which format does the seam target?
- (event) Pass 1 coherence: PASS. All 24 capabilities carry intent + success; constraints all rule something out; 10 non-goals; success signal is a demonstrable end-to-end run plus the telar-on-telar dogfood. Three intents (CAP-9, CAP-11, CAP-22) name a mechanism — flow DAG, ensure_service, MapStore — judged contract vocabulary rather than implementation prescription, so rule 2 holds.
- (event) Pass 2 preservation: PASS with one repair. Interference analysis over shared files/surfaces shaping decomposition width was dropped from the first render and is now restored to CAP-9 success.
- (note) Deliberate drop: the loom-system intent's hedge that the seat registry might be "possibly exposed via MCP" is speculation, not a decision — not preserved.
- (note) Scope exclusion, not a drop: the organization-workspace intent's bedrock need, context-bank framing, six laws, agents model, capture model and harness-feasibility research are load-bearing for a future workspace spec but out of scope here by the user's seam-only call. They stay in the source doc.
- (note) Wrapper-only content not preserved: session narrative, technique names, per-session "how to walk it" instructions, and the two spent next-session briefs.
- (decision) Preview-this-thread is resolved as NOT a gap and NOT in scope. Verification memlog 34-41 is explicit: the user decided a single server per loom branch ("one running server per line of development"), and the answer to "how do I see a thread's progress" is "open the loom's URL". The per-thread ephemeral lane was tagged (idea by coach) and never received a (decision by user), unlike the accepted items around it.
- (constraint) One window per loom, never per thread. The loom's warm lane is the single live URL and thread progress is read through it; heavy infra is one stack at a time, never N (the Supabase CLI stack is too heavy to run per loom). The ephemeral scope stays what it was created for — frozen final verifies and preview lanes as infrastructure — not a per-thread preview surface.
- (constraint) Window on demand for heavy projects: opening the window IS the pull that borrows the stack, standing the lane while the user looks and releasing after; infra-free projects keep always-warm windows. (Verification memlog 83, under-weighted in the first render.)
- (note) Correction of my own framing, not the UX's: the UX intent listed the preview hatch under open items as "a cockpit gap ... never designed", and I promoted that word "gap" into a spec open question. An unaccepted coach idea is not an owed feature. The UX lane did not design it and was not wrong.
- (event) Open question count drops from 8 to 7; the preview-this-thread question is closed by source evidence rather than by a new decision.
- (decision) Secrets and evidence governed as teardown-scoped + data-class inherited: carry plants files at 0600 and scrubs at teardown, never copying them into evidence, the map or a transcript, failing closed if a scrub cannot complete; evidence inherits the recipe data class (disposable retains, shared-dev is flagged on the reality manifest and never lands in-repo, production already refused) and is reaped with its loom.
- (decision) v1 ships a minimum landing-queue strip in Home's right column (queued count, currently landing, landed today) — enough that an accepted-but-unlanded loom is never invisible, without reopening the fleet surface the UX session deferred.
- (decision) Execute's thread rows are ALWAYS grouped, single group included — no flat-versus-grouped mode switch. Group identity is derived from the compiled flow's declared lanes, not a model-authored workstream label, so a group always denotes real interference structure.
- (decision) A loom's premise is a list of N>=1 intents: the gate renders one contract group per intent, Judge lights them per intent, the delivery card claims N deliveries under one verdict, and Accept stays a single moment — the accept moment is still what sizes the loom.
- (event) Four open questions closed by user decision (secrets, landing-queue visibility, thread grouping, multi-task looms); one closed by source evidence (preview-this-thread). Three remain.
- (insight) Worktree storage cost is not git — worktrees share the object database. The cost is node_modules, untracked and required per worktree for thread-altitude checks/tests, at roughly 0.5-2GB each. At 7 looms x ~4 checkouts that is tens of GB, and core manages node_modules for worktrees nowhere today.
- (decision) Thread worktrees reap at land, the moment their work folds into the loom branch, so peak disk tracks threads running now rather than threads ever run. Accepted trade: a landed thread's exact working state is gone and re-derived from the branch.
- (constraint) The loom worktree cannot be reaped at delivery — boomerang keeps branch, worktree and recipe so a rejected loom resumes cheap, and a parked loom may resume days later. It survives until accept and landing complete.
- (decision) Thread worktrees get dependencies by APFS copy-on-write clone of the loom worktree's install (telar is mac/arm64 only, so clonefile is a safe assumption), falling back to prepare.install where unavailable. Shared-symlink node_modules rejected: postinstall scripts, native binaries and bun's hoisted store make shared mutable state across parallel threads a source of confusing cross-thread failures.
- (decision) Worktree cleanup stays best-effort as it is today, with one exception: a worktree that held carry files and cannot be removed raises a dire escalation naming the path, rather than swallowing the failure and leaving secrets on disk. This resolves the collision between the new carry-scrub constraint and the existing catch-and-swallow removeWorktree idiom.
- (note) Existing cleanup machinery inherits unchanged: removeWorktree called from build-fanout.ts, verify-thread.ts and executor.ts, plus reapOrphanWorktrees from dispatcher.ts as the crash safety net, keyed on the telar-wt- prefix so it never touches the user's main checkout or their own worktrees.
- (decision) Multi-loom per project stays supported; the worktree lifecycle and isolation constraints now settled make it tractable, so no v1 serialization to one loom per project.
- (decision) Window concurrency per project is a function of the recipe's isolation parameter, not a fixed number: mutex (Supabase-gateway, reset-on-acquire) permits exactly one live window per project and forbids a window coexisting with a loom verification there; tenant-db and schema permit one window per loom; a DB-less project keeps always-warm windows per loom. Requests beyond the permitted count queue and name their holder, never displacing a live window or a running verify.
- (note) Provenance: this rule is DERIVED, not recorded. The verification memlog scopes both mutex statements to verifies (line 44 "serialize verifies against shared stack", line 81 "only one loom verification per repo") and line 83's window-on-demand never says what happens when a second window is requested. The ingredients were all present; the rule was not.
- (decision) Batch weave is not a loom-count decision. The selected work packets SEED the preparation graph: the graph initializes from that task set instead of from an opening intake conversation, and grows from there. This resolves the "one loom" (UX session) versus "one loom series" (workspace memlog) wording conflict — neither was a shape decision, the shape falls out of preparation.
- (decision) Both workspace birth paths take a typed handover (premise list + context references), never a read of the workspace store, so the workspace module's persistence layout stays its own to decide. Location is already settled upstream as TELAR_HOME/workspace; the packets/<id>/packet.yaml layout was a coach idea never user-confirmed and is not adopted here.
- (event) Two more open questions closed. Remaining: the v1 map-region set, and what the workspace queue shows during a running batch loom.
- (decision) Tasks a loom has picked up stay in the workspace list and stay editable; an edit posts to that loom's always-open steering channel and the entry marks that it was sent, leaving the orchestrator to decide what to do with it. The loom's copy of the brief stays frozen and what it actually built is settled at the delivery card. Entries drain to done when accept lands.
- (decision) Locking rejected on two grounds. Simplicity: steering reuses machinery that already ships (mcp__loom__steer_loom, POST /api/looms/[id]/steer, the contracted always-open steering channel), whereas a lock is new state whose release path must cover park, crash, kill and boomerang — a loom parked on credit exhaustion would hold the user's notes for days. Doctrine: the workspace's premise is zero-ceremony capture, and "agents prepare, never commit" constrains agents, not the human — locking the user out of their own notes inverts the law it appears to serve.
- (note) Vocabulary correction: "woven item" was my coinage, not session language. The sources say "weave as one loom" (button) and weave_batch (tool); the spec now says "tasks a loom has picked up".
- (event) One open question remains: the v1 map-region set.
- (direction) Facundo: don't hardcode the graph or the regions — load methodologies as data, treat them as plugins eventually, let the user modify them; at the end of the day they produce artifacts for the loom.
- (decision) CAP-7 becomes Methodology as data, superseding the narrower "specialist seat registry": seats, artifacts and the map regions they land in are declared data rather than engine code. This makes live the loom-system line I had dropped as speculation — "specialist seats forming a growable registry, possibly exposed via MCP" — and extends the already-locked "shape is agent-chosen, never hard-coded".
- (decision) v1 ships exactly one built-in methodology, BMAD-derived, expressed through the data mechanism rather than hardcoded; no authoring UI and no second methodology this round. User overrides ride the customize.toml + _bmad/custom/<name>.toml pattern already proven in this repo rather than a new format.
- (decision) v1 built-in region set: objective, architecture, form, surfaces, verification, conventions. form is included because the vision critic is specced to judge against "the map's objective/form regions"; conventions folds telar's existing project-context.md into the map so hand-edits to it are absorbed as drift; surfaces is retained from the artifact-storage session's named file list.
- (decision) Epics and stories are deliberately NOT a map region. They are work breakdown rather than durable project truth, and that role is already played by the decision graph and compiled flow DAGs which live as run artifacts under TELAR_HOME. Making them durable would import the epic-sediment failure the artifact-storage session set out to fix.
- (constraint) A methodology declares the catalog, never the graph — it states what artifacts and regions MAY exist and who staffs them; drift alone decides which nodes actually run. Otherwise a twelve-artifact method rebuilds epic-sediment inside the loom.
- (constraint) Methodologies never declare how anything is written. They name artifacts, seats and regions; the propose-then-serial-land protocol sits below the methodology layer and is unreachable from it, so wall 4 survives a user-modified methodology.
- (constraint) The region set is per-project; changing a project's methodology is a reconciliation-loom rebuild of a regenerable projection, never a schema migration.
- (decision) The deferred map-region taxonomy research is moot as posed and becomes a non-goal: regions are whatever the loaded methodology declares, so there is no universal set to discover.
- (question) What "stale" means per region remains open, and it is the behavioural half: checkable drift (does the recipe still boot, does the structure still match) can spawn a node on its own, while judged drift (objective, form) likely has to surface as a question at intake rather than silently growing the graph.
- (event) Re-validate after the walkthrough: coherence PASS (24 capabilities intact, all carry intent + success; CAP-7 renamed to Methodology as data with no stale cross-references; 26 constraints, 12 non-goals, 5 assumptions, 1 open question). Preservation PASS — no source claim lost by the changes; four capability entries and two companions were extended rather than replaced.
- (decision) Drift detection stays free: the intake session judges it by comparing intent against the map and the actual repo, rather than following a per-region rule table. A region declaration says what the region holds, not how its staleness is proven. This restores the loom-system doctrine verbatim — "intake diffs intent against the map; planning nodes spawn only where drift exists" — which I had been about to over-specify into a schema field.
- (insight) The freedom is safe because the readiness gate is downstream of it: the human rules on the whole spawned graph before any build spend, so an unnecessary node costs a glance rather than money. The checkable-versus-judged split remains a real observation the session can exploit, but never a constraint it must obey.
- (event) Open Questions section removed — zero remain. Every question the four sessions left open is now closed, by user decision or by source evidence.
- (question) Facundo: with several looms referencing files generated in the decision graph, what happens if a loom modifies them mid-run — or are files isolated per loom? His model: a master copy pulled into the loom at definition. His worry: when does it become the reality?
- (note) Mostly already decided. The artifact-storage session pins the map content-hash at prep (that IS the master copy pulled at definition), forbids mid-run writes to shared docs (wall 4), and lands proposals serially at accept. So "when does it become the reality" answers as: at accept, through the same moat as the code — documents and code promote together. Graph-generated artifacts are not shared at all; they are single-owner engine state under looms/<loom-id>/.
- (decision) A loom's pin does not move for its lifetime: threads read the pinned map as stable background reference, and the loom's own prep decisions reach them through the gate-frozen contract and per-node context manifests rather than by mutating the loom's view of the map. Keeps the map background truth instead of a mutable scratchpad and makes the rebase base at land time unambiguous.
- (note) Correction of my own framing: I had offered the pinned-copy option as "almost certainly wrong" on the assumption that build threads read the PRD as their build spec. They do not — they build from the gate-frozen contract, and each agent sees its context manifest. The user's choice is right and my objection was based on a wrong model of where a thread's spec comes from.
- (decision) Another loom's open, unlanded proposals are reachable but never handed over: a preparation agent may query the ledger for proposals touching a region it is working on, and nothing is injected into its context by default. A loom plans against accepted reality, may look at what is coming, and never builds on it — so a boomeranged loom leaves nothing built on a fiction. This is pull-never-push applied to cross-loom context, consistent with the no-context-bombing manifest doctrine.
- (event) Read semantics section added to map-and-storage.md. Spec still carries zero open questions.
- (direction) Story breakdown requested. Flat ids within this spec, epic names in titles only — epic numbering is deferred because three specs are being planned together and the global numbering is assigned later.
- (decision) Sequencing: foundation, lab, prepare, execute, judge, surfaces. Driven by two facts — execution already works today so Acts 2-3 are largely upgrades while Act 1 and the lab are net-new, and the Conversation shell must precede every new chat surface (gate room, node chats, steering), which pulls it into foundation.
- (decision) Checkpoint policy: spec_checkpoint on stories that invent something new; done_checkpoint at each group boundary so review lands on a coherent slice rather than a fragment.
- (decision) Story 1 Service declaration + per-owner leases — CAP-11; unbundles run-server/supervisor from the verify-only path, generalizes runner/lease.ts, adds the missing per-session dir under TELAR_HOME.
- (decision) Story 2 MapStore, region files, ledger, methodology-as-data — CAP-22, CAP-7 and the storage half of CAP-3. Substrate only; no intake or write-back yet.
- (decision) Story 3 Conversation shell carve-out — the Conversation constraint and conversation-component.md; render-seam extraction plus Marker and ApprovalCard, proven in the demo gallery before any new surface is born on it.
- (decision) Story 4 Recipe schema + readiness node — CAP-4, wired into the dead preparing-phase runSetup hook, fail-closed, degraded modes declared at prep time.
- (decision) Story 5 Heavy infra scopes, refcount, semaphore, data isolation — CAP-12, including the window-concurrency rule keyed on isolation.
- (decision) Story 6 Worktree and branch doctrine + lifecycle — CAP-10; reap-at-land, APFS clonefile deps, telar-wt- prefix guard kept, fail-closed only where carry files resisted removal.
- (decision) Story 7 Two-altitude verification + per-repo mutex — CAP-13; thread rungs double as the progress heartbeat.
- (decision) Story 8 Decision graph + approval-gated advance — CAP-2 and CAP-6.
- (decision) Story 9 Intake + lazy drift diff — the intake half of CAP-3; pin the map hash for the loom's life, drift detection is session judgment not a rule table.
- (decision) Story 10 Readiness gate room — CAP-5.
- (decision) Story 11 Birth, detach and the workspace seam — CAP-1; typed handover, not a store read.
- (decision) Story 12 Orchestrator ownership + role-wall carving — CAP-8; includes auditing existing dispatcher/weave paths for write-tool leakage.
- (decision) Story 13 Flow compile — CAP-9; interference analysis shapes width, recompile explicit and audited, templates survive as fallback.
- (decision) Story 14 Park and resume durability — CAP-14.
- (decision) Story 15 Progress liveness + dire-razor escalation audit — CAP-15.
- (decision) Story 16 Evidence subsystem — CAP-16; ledger, citations, grades, attempt history, reality manifest.
- (decision) Story 17 Delivery card — CAP-17.
- (decision) Story 18 Accept-then-land + landing queue — CAP-18.
- (decision) Story 19 Boomerang — CAP-19.
- (decision) Story 20 Map write-back + collision protocol — CAP-21; includes reachable-not-injected access to other looms' open proposals.
- (decision) Story 21 Vision critic — CAP-20, advisory only.
- (decision) Story 22 Loom cockpit — CAP-24.
- (decision) Story 23 Home / fleet triage — CAP-23.
- (decision) Story 24 Prove-run — the SPEC success signal end to end, plus the recursive telar-builds-telar run under a sandboxed TELAR_HOME.
- (event) stories.yaml validity check PASS against all four schema rules: 24 entries parse with required fields, ids unique, prefix-free, no status field, ids quoted strings of digits only. 17 spec_checkpoints (the net-new stories), 6 done_checkpoints at group boundaries (3, 7, 11, 15, 21, 24).
- (event) Source brainstorm docs reconciled: resolution markers added inline to the open-item lists of the loom-system, verification, ux-ui, artifact-storage and organization-workspace intents, each pointing at SPEC-loom-redesign. Session prose left as the historical record.
- (insight) Discovered mid-reconciliation that SPEC-organization-workspace now exists and had already closed all six workspace open questions, including the packet store format (lanes.yaml + packets/<id>/packet.yaml). My first edit to that brainstorm doc claimed the format was still open and was wrong; corrected.
- (decision) Aligned the queue-departure wording to SPEC-organization-workspace CAP-11: entries leave the list only once the loom is both accepted AND landed, rather than the looser "drain to done when accept lands".
- (note) The typed-handover decision is independently confirmed by SPEC-organization-workspace, which requires cross-surface access to go through the workspace MCP server and never raw file tools — a loom reading packets/ directly would violate it. Rationale updated to cite that rather than "format undecided upstream".
- (question) Cross-spec divergence to settle: SPEC-organization-workspace CAP-11 says selected items "weave as one loom series", while this spec records Facundo's later clarification that the packets seed the preparation graph and the shape falls out of preparation. Two live specs, two shapes.
- (question) Cross-spec seam ownership: the queue-edit-posts-to-steering behaviour is recorded here, but SPEC-organization-workspace scopes itself out of loom internals ("hands off premise + context and stops at the detach boundary"). Which spec should carry it is unsettled.
