---
topic: Runtime foundations — the shared substrate under the three feature SPECs (loom-redesign, organization-workspace, ultra-workflows)
updated: 2026-07-25T11:35
---

- (direction) Back-fill: this spec folder was hand-authored by the 2026-07-24 architecture run (SPEC.md + admission.md + brownfield.md + stories.yaml, all 22:39) with no memlog — the only one of four specs never derived through bmad-spec. Entries below reconstruct its decision-of-record from those files before any new change is applied.
- (direction) Sources: ARCHITECTURE-SPINE.md (AD-1..AD-21), WORK-SPLIT.md (tracks A-F), SOLUTION-DESIGN.md — all under planning-artifacts/architecture/architecture-telar-2026-07-24/
- (direction) Scope: the substrate three feature SPECs all land on — one process, one state root, one session surface. Deliberately unglamorous, deliberately first; nothing here is a user-visible capability. Gathered where it belongs rather than misattributed to whichever feature touches it first.
- (decision) Two findings from reading code drove the package: the concurrency ceiling is a fiction (engine.ts MAX_CONCURRENT=4 module-private vs Charter budget.maxAgents 12 vs Ultra's own cap 3), and the spend ledger already exists (usage.ndjson / logUsage / UsageEntry in apps/web/lib/store.ts)
- (decision) Ordering CAP-1 before CAP-2 is not negotiable: extending the usage ledger while store.ts ignores TELAR_HOME would write dev spend into production state
- (capability) CAP-1 TELAR_HOME honored everywhere: store.ts resolves via process.env.TELAR_HOME ?? path.join(os.homedir(), '.telar') matching the five modules that already do; asserted by test with an empty temp dir; no behavior change when unset. AD-18
- (capability) CAP-2 One attributed spend ledger: UsageEntry gains owner-kind + owner-id; NO new ledger file; session per-turn usage, Ultra manifest spend and charter budget-left all become projections over usage.ndjson; readers tolerate pre-field records (AD-7); cost language is a property of the projection. AD-18, AD-20
- (capability) CAP-3 Typed event bus with required delivery class: one in-process publish path in packages/core; adapters subscribe (SSE tails, session-wake injector, dock aggregator); agent-facing may synthesize an assistant turn, human-facing never pushes; class is a required field the type system demands; published names/payloads are a declared contract. AD-14, AD-21
- (capability) CAP-4 Admission control for agent() concurrency: configurable ceiling TELAR_MAX_AGENTS default 4, classes loom-build/loom-verify/ultra/other with weights and floors, verification holds precedence not a held-open slot, no barging, work-conserving borrow pass, pure policy half, admissionSnapshot() observability export, fanoutClamp gains optional processCeiling. Full contract in admission.md. AD-17
- (capability) CAP-5 One lease primitive, two lifetimes: runner/lease.ts's record ({pid,token,ts}, atomic temp+rename, heartbeat, stale-reclaim) generalized to serve session-owned and loom-owned processes; TELAR_HOME/sessions/<sessionId>/ created, holding session-scoped runtime state only, not absorbing chats.json; a stale lease can at worst produce a false-positive failed, never an auto-done. AD-16, AD-5
- (capability) CAP-6 Load-bearing invariants are executable: bun test assertions for no-accept-tool, verifier stack holds no write tools, no cross-subtree path reads, no core runtime in client components, no direct writes to a shared runtime service's state; each fails loudly naming the invariant it defends. AD-19
- (decision) Admission design settled in admission.md: a hard reservation for loom-verify was specified, BUILT, and then rejected on evidence — it broke ultra-runner.test.ts's peak===4 assertion and permanently burns 25% of a 4-slot machine. Precedence-not-reservation replaces it. Do not re-derive the rejected design.
- (decision) Admission scope fixed: interactive chat sessions are out of band by design (they call query() directly from the chat route, never engine.agent()), so no interactive-session class and no reserved slot; long-lived processes take no slot; Ultra's run-local cap of 3 stays as a separate per-run limit
- (decision) A reference implementation of admission.md was written during the architecture run and deliberately reverted so it lands as reviewed work; preserved at reference/admission-impl/ (22 tests, verified 1317 pass / 0 fail across 99 core test files). It is reference, not authority — admission.md is.
- (decision) Companions: admission.md + brownfield.md spec-authored; ../../project-context.md and the ARCHITECTURE-SPINE adopted
- (direction) Update run 2026-07-25, driven by implementation-readiness-report-2026-07-24.md. Findings in scope: CV-1/CV-2 (Track B unstoried, OW story 6 specifies the AD-9 anti-pattern), EQ-1 (lease primitive built twice), RA-1 (SPEC missing Constraints/Non-goals/Success signal/Assumptions).
- (note) RA-1 root cause identified: this spec never went through bmad-spec's template, which is exactly why it carries only Why/Capabilities/Out-of-scope. The four missing sections were never dropped — they were never authored.
- (constraint) Back-filled from brownfield.md 'Boundaries that bind this work': @telar/core is server-side only, client imports types only; all server surface is app/api/**/route.ts; zod schemas for persisted entities owned by @telar/core; deterministic control flow in code with intelligence in the leaves; one owner per TELAR_HOME subtree; the Human-Accept Moat and Verifier capability wall are non-negotiable and CAP-6 makes them executable, never relaxes them
- (constraint) Back-filled: bun test / bun run lint / bunx tsc --noEmit run manually, no CI. bun run lint currently reports ~77k pre-existing problems in apps/web, most under .next-desktop/ build output — not a clean baseline and not this SPEC's to fix
- (constraint) Back-filled: reuse never rebuild — the usage ledger, the lease primitive, the atomic-write idiom and the TELAR_HOME resolution expression all already exist; copy the settled expression rather than inventing a variant
- (decision by user) OWNER RULING (CV-1): Track B's SessionProfile resolver is absorbed into this SPEC as CAP-7 rather than getting its own package or going to organization-workspace. Rationale accepted: A and B are both no-UI substrate unblocked from day one, and this SPEC's stated purpose is to gather cross-cutting invariants where no single feature owns them. WORK-SPLIT keeps A and B as separate TRACKS with disjoint write sets (packages/core vs the chat route) — one spec carrying two tracks is fine, parallelism is a track property not a spec property.
- (capability) CAP-7 Session config is a resolved profile, not a branch: a typed SessionProfile {cwd, guardrails, settingSources, mcpServers[], toolPolicy, requiredCapabilities, systemPromptAppendix} resolved before the route body. Project session, master session, loom-node session and steerer are all profiles. A new surface adds a profile; it does not add an if. The PreToolUse guardrail is wired OUTSIDE the profile and runs for every session regardless of profile; toolPolicy is intersect-only, enforced by its type. requiredCapabilities are checked against what the provider port publishes and an unmet one is a hard error before the stream opens. AD-9, AD-10, AD-11
- (decision) CV-2 resolved by CAP-7: organization-workspace story 6 currently reads 'lift the chat route's project gate', which is the conditional branch AD-9 forbids. With the resolver specified here, the project-less master becomes a profile supplying cwd=TELAR_HOME/workspace/home and settingSources=[] — no special case in the handler. The OW-side story rewrite is a separate bmad-spec run on that spec folder; this SPEC supplies the mechanism it must be rewritten against.
- (decision) CV-4 reconciled: the profile RESOLVER is critical path (it blocks tracks E and F); Codex MCP INJECTION is optional for v1 because a Claude-backed master is unblocked without it. Those are two different pieces of Track B, and conflating them is what made WORK-SPLIT, SPEC-organization-workspace and its story 13 read as three different priorities. The injection work stays storied where it already is (organization-workspace story 13) and is deliberately NOT re-claimed here — re-claiming it would repeat the EQ-1 duplication this same run exists to fix.
- (constraint) EQ-1 fix: the lease primitive and TELAR_HOME/sessions/<sessionId>/ are built exactly ONCE, here, under CAP-5. Every consumer — including the loom's per-owner service-lease registry — composes over that primitive and re-implements no stale-reclaim. AD-16 exists to prevent two stale-reclaim implementations drifting, which is the path by which a false done gets issued, and a false done is a breach of the human-accept moat.
- (note) EQ-1 is only half-fixable from this spec folder: SPEC-loom-redesign story 1 still says it 'generalizes runner/lease.ts' and 'close that gap here' about the sessions directory. bmad-spec is single-writer per spec folder, so that edit needs its own run on spec-loom-redesign. This SPEC now makes the ownership claim unambiguous so that run has something to point at.
- (decision by user) OWNER RULING (RA-1): success signal is the technical gate PLUS a consumable demonstration — under TELAR_HOME=<temp>, one scripted run shows the bus waking a subscriber on an agent-facing event and provably not pushing on a human-facing one, a spend record attributing to an owner and reading back through a projection, a loom-verify call winning a freed slot ahead of queued loom-build with the snapshot saying why, a stale lease reclaiming to failed never done, and an unmet requiredCapability failing before the stream opens — with ~/.telar untouched and CAP-6 assertions green, plus full core suite green and core AND web tsc clean. Chosen over the bare technical gate (a checklist, not a demonstration) and over gating on Track D's Ultra run (inverts the dependency — A cannot be gated on its own consumer; that proof belongs at WORK-SPLIT's phase-2 gate where it already lives).
- (constraint) Non-negotiables this SPEC serves and never relaxes: the Human-Accept Moat (no agent-callable accept tool anywhere, ready->done is human-only) and the Verifier capability wall. CAP-6 makes them executable; CAP-7's toolPolicy is intersect-only precisely so making session config data cannot turn a structural invariant into a configurable one.
- (constraint) Separate routes per session kind were rejected: a moat enforced in three places has three chances to be forgotten. One route, one resolver, one guardrail outside the profile.
- (constraint) No new persisted state shapes: CAP-2 extends the existing usage.ndjson, CAP-5 generalizes the existing lease record. Creating a second spend file or a second lease shape is the failure each capability exists to prevent.
- (constraint) Nothing in this SPEC is user-visible. Every capability is substrate two or more features would otherwise build twice, differently. If a capability here starts growing a surface, it has escaped its package.
- (decision) Non-goals promoted from the hand-authored 'Out of scope' section, unchanged in substance: tuning the admission ceiling or class weights against a real fleet; wiring processCeiling into tick.ts and executor.ts live clamps; tagging executor.ts agent() call sites with loom-build; adding CI; persisting bus events.
- (decision) Non-goals added this run: Codex MCP injection (storied in organization-workspace, optional for v1); the SessionProfile field set beyond the named core (surfaces add fields they need, per the spine's own deferral); rewriting the loom's service-lease registry (it composes over CAP-5, it is not re-specified here); any UI for admission state — admissionSnapshot() is an export, and whether a surface renders it is that surface's spec to decide.
- (assumption) The five modules that already resolve TELAR_HOME correctly (manifest.ts, looms.ts, session-log.ts, permissions.ts, vcs.ts) are the reference; store.ts is the sole outlier. Verified against the working tree 2026-07-24; if a sixth outlier appears, CAP-1 covers it.
- (assumption) admissionSnapshot()'s shape (policy, occupancy, waiting-by-class, in-flight, queued) is taken from admission.md as-is; no consumer has yet specified what it needs, so the export is defined by what the controller can honestly report rather than by a surface's requirements.
- (assumption) CAP-7's profile list — project session, master session, loom-node session, steerer — is the set the spine names. Retrofitting the three existing session kinds onto profiles is in scope; discovering a fifth kind is not, and would be that surface's own work.
- (question) Does retrofitting the existing project session onto CAP-7's resolver land in the same story as the resolver itself, or as a follow-on? The spine says node conversations and steerers become profiles 'retroactively cleaning up what is already there', but does not say when.
- (event) Self-validate pass 1 (coherence, Spec Law 1-6+8): PASS. Rule 1 — all 7 capabilities carry intent+success. Rule 2 — intents state WHAT; the one place naming a concrete shape (CAP-7's SessionProfile field list) is a contract from AD-9, not implementation prescription, and the mechanism detail sits in success where it is testable. Rule 3 — every constraint rules something out; the lease-exclusivity and one-route constraints each forbid a specific alternative that was live before this run. Rule 4 — 9 non-goals, up from 5. Rule 5 — success signal is now a demonstrable scripted run across six ports, replacing nothing (there was none). Rule 6 — CAP-1..6 preserved unchanged, CAP-7 is the next unused id, none reused. Rule 8 — Why gained one paragraph (the third finding) and lost none; no decoration added.
- (event) Self-validate pass 2 (preservation): PASS. Hand-authored SPEC.md fully preserved — all six original capabilities verbatim in substance, the CAP-1-before-CAP-2 ordering rule, both original findings, and all five original out-of-scope items carried into Non-goals unchanged. admission.md and reference/admission-impl/ untouched and still cited. brownfield.md extended, nothing removed. New content traces to AD-9/AD-10/AD-11, SOLUTION-DESIGN's profile rationale, WORK-SPLIT's Track B ownership row, and the readiness report's CV-1/CV-2/CV-4/EQ-1/RA-1. Wrapper-only content dropped: none — this was an additive derive over a hand-authored artifact, so the preservation risk ran the other way and was checked in that direction.
- (event) Spec derive complete: 7 capabilities, 9 constraints, 9 non-goals, 1 success signal, 3 assumptions, 1 open question. Companions unchanged (admission.md, brownfield.md spec-authored; project-context.md and ARCHITECTURE-SPINE.md adopted).
- (decision by user) OWNER RULING (open question resolved): retrofitting the existing session kinds onto CAP-7's resolver is a FOLLOW-ON story, not part of the resolver story. The resolver lands additively — new profiles resolve through it while the existing project-session path is untouched — and a second story moves the live chat path over. Splits an additive change from one that touches production traffic, and gives the risky half its own gate. Open Questions section is now empty and is omitted from SPEC.md.
- (direction) Story Breakdown run 2026-07-25 over the updated spec. Existing stories 1-6 keep their owner-set checkpoints; two CAP-7 stories are inserted and the prove-run is re-cut against the new six-port success signal.
- (decision by user) Story order agreed: 1 TELAR_HOME, 2 ledger, 3 admission, 4 bus, 5 lease, 6 SessionProfile resolver, 7 profile retrofit, 8 invariant assertions, 9 prove-run. Assertions move from position 6 to 8 so CAP-6 can assert AD-10's intersect-only toolPolicy alongside the five original invariants rather than shipping and immediately amending. Stories 6-7 are Track B and are not blocked by 1-5 — list position is execution order for a serial dispatcher, not a dependency claim.
- (decision by user) Story 6 agreed: SessionProfile resolver (CAP-7), spec_checkpoint=true, done_checkpoint=true — owner chose both because AD-10 calls this the move that could turn a structural invariant into a configurable one, the intersect-only toolPolicy must hold at the type level, and tracks E and F both build owner profiles on top so a wrong shape propagates twice
- (decision by user) Story 7 agreed: profile retrofit of the existing session kinds (CAP-7 follow-on), done_checkpoint=true only — the shape was settled at story 6, so the residual risk is live-chat-path regression, which a spec review cannot see and a done gate can
- (decision by user) Story 5 dispatch note extended by owner ruling: names the EQ-1 collision explicitly — this is the only implementation, the loom's service-lease registry composes over it, SPEC-loom-redesign story 1's competing claim is being retired, and a second stale-reclaim is a stop signal
- (decision) Story 9 (prove-run) re-cut against the new success signal: the six-port scripted demonstration under TELAR_HOME=<temp> replaces the old technical-gate-only description; done_checkpoint=true retained
- (event) stories.yaml schema check: PASS — 9 entries, all required fields present, ids "1"-"9" unique, quoted, charset-clean and prefix-free, no status field anywhere, checkpoints only where the owner set them, list order = execution order. No stories/ files exist yet, so all ids remain unpinned and renumberable.
- (event) Story Breakdown complete: 9 stories (was 7). Inserted story 6 (SessionProfile resolver, both checkpoints) and story 7 (profile retrofit, done checkpoint); assertions moved 6->8 and gained the toolPolicy-widening assertion; prove-run re-cut as story 9 against the six-port success signal; story 5's dispatch note now names the SPEC-loom-redesign story 1 collision.
- (event) Session complete. Outstanding cross-spec work this run could not do: SPEC-loom-redesign story 1 still claims to generalize runner/lease.ts and create the sessions directory (EQ-1 half-fix), and SPEC-organization-workspace story 6 still reads 'lift the chat route's project gate' (CV-2). Both need their own bmad-spec runs on those spec folders.
- (direction by user) Owner raised mid-run: a dev run must not collide with the normal Telar folder, and believed something similar already exists. Verified against the working tree 2026-07-25 — correct on both counts.
- (note) The isolation mechanism EXISTS and works: apps/web/package.json:6 runs dev as TELAR_HOME="${TELAR_HOME:-$HOME/.telar-dev}" next dev, so a dev server defaults to ~/.telar-dev and still respects an explicit override. 'start' deliberately carries no override (production is ~/.telar), and apps/desktop/main.js:279 sets TELAR_HOME only for the --smoke gate.
- (note) Verified honoring TELAR_HOME: manifest.ts:17, looms.ts:17, session-log.ts:16, permissions.ts:45, plus doctor.ts:205 which reads the override to report which home is live. vcs.ts honors it TRANSITIVELY by importing telarDir from manifest.ts rather than duplicating the expression (vcs.ts:22,108) — so the brownfield's 'five places' is right in effect but store.ts's fix has a choice: duplicate the expression as permissions.ts and session-log.ts do, or import core's telarDir.
- (note) CONFIRMED LEAK, unchanged: store.ts:8 const DIR = path.join(os.homedir(), '.telar') — no TELAR_HOME. chats.json, usage.ndjson and plan-usage.json therefore write to the REAL ~/.telar even under bun run dev.
- (decision) CAP-1's rationale strengthened by two consequences the spec had not named. (1) CONCURRENT COLLISION: a dev server and the packaged desktop app running at the same time both append to the same usage.ndjson and both rewrite the same chats.json — not merely dev spend polluting production totals, but two processes contending for one non-concurrent store. (2) THE SMOKE GATE LEAKS: apps/desktop/main.js sets TELAR_HOME to a throwaway home for --smoke precisely to be hermetic, and store.ts defeats it, so the fail-closed release gate touches the user's real chat history and ledger.
- (event) Story 1 dispatch note extended with the two verified consequences (concurrent dev/desktop contention on chats.json + usage.ndjson; the --smoke hermetic gate defeated by store.ts) and with the fix-shape choice (duplicate the expression like permissions.ts/session-log.ts, or import telarDir like vcs.ts). No capability or contract change — CAP-1's success criterion already covered the behaviour.
