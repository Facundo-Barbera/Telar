---
topic: Finish Ultra (ultracode workflows) in telar — completion wake, real session UI, authoring skill, polish, prove-run
updated: 2026-07-24T11:29
---

- (direction) Sources: brainstorm-ultra-workflows-2026-07-24/brainstorm-intent.md (decision-of-record), archived plan .cleanup-archives/docs-legacy-2026-07-17/plans/ultra-harness.md (U1-U6 job plan), regenerated docs/ + code as as-built ground truth
- (direction) Scope is the finish work only — U1-U5 engine/tools are built and out of rebuild scope; done = run, watch, and get woken by an Ultra workflow from a real telar session
- (decision) Wake mechanism (settled #2): synthetic completion event triggers a fresh assistant turn summarizing the terminal outcome; mid-conversation it lands in context next turn; ultra_status polling stays as fallback, never replaced
- (decision) Sandbox stays as-is (settled #1): determinism bans are load-bearing for ordinal resume; script vm is hygiene/reference-parity, not a security layer — no rework
- (decision) Keep schema-less agent() returning final text (settled #3) — reference parity with the CC harness, not up for redesign
- (decision) Ship Claude-first (settled #4): Codex lights up later via the codex-driver seam; out of scope for done
- (decision) Prove-run required (settled #5): one real end-to-end Ultra workflow on a sandbox project through the actual non-demo UI before calling the feature done
- (decision) As-built terminal states done|failed|stopped supersede the plan draft name completed (storage.ts, api-contracts-web.md)
- (capability) CAP-1 Completion wake: terminal run wakes the main agent via synthetic completion event -> fresh summarizing assistant turn; mid-conversation lands next turn; polling fallback intact
- (capability) CAP-2 Real session UI (U6): transcript anchor + rail Workflows section + Stop/Resume in the real session surface, live over existing /api/ultra SSE, per the frozen ui-contract.md
- (capability) CAP-3 Authoring-reference skill file: ships with the tool, injected for Claude sessions; covers injected surface API, explicit-model rule, quality patterns (adversarial-verify, loop-until-dry), worked example
- (capability) CAP-4 Composer Ultra chip: arms Ultra for one message via the ultra:true annotation; arming only, no ceiling editor or submenu
- (capability) CAP-5 Session-cost rollup: run spend attributes to the owning chat message and folds into per-turn usage display; readout only, session cost language (USD Claude / tokens Codex)
- (constraint) Opt-in is a request: agent may call ultra only on explicit user ask (keyword or chip annotation); never inferred; no mode or flag
- (constraint) Non-blocking contract fixed: ultra returns {runId} immediately; several runs live per session; completion is an event
- (constraint) Child posture fixed: non-interactive under ULTRA_CHILD_TOOLS (Read,Grep,Glob,Write,Edit,Bash) + restrictTools; approval-needing action fails the agent() call; no per-agent permission knob
- (constraint) Explicit model per agent() call: static lint rejects before runId; runtime MissingModel is a control signal ending the run failed
- (constraint) No budgets anywhere: no ceilings/meters/budget UI; brakes are per-run cap 3, engine gate 4, 1000-agent backstop, human Stop, ultra_stop
- (constraint) Ultra never writes loom state; state lives in ~/.telar/ultra/ under TELAR_HOME, invisible to loom listing/reaping
- (assumption) Wake fires on every terminal state (done|failed|stopped) carrying {state, result|error} — plan §4 says outcome reaches the agent on terminal; brainstorm only says finishes
- (assumption) Demo-gallery session-ultra.tsx is the as-built rendering of the frozen UI contract; the real UI implements the same contract; replay controls are mockup-only
- (assumption) Session-cost rollup means run spend folds into the owning chat message per-turn usage display (plan §4 spend visibility), not a new cost surface
- (question) Headless wake: if no client is attached when a run finishes, does the summarizing turn run immediately (unattended token spend) or defer until the session next opens?
- (question) Dock signal (UI contract §9, run state in the session dock bubble) is absent from the finish punch list — required for done, or deferred polish?
- (decision) Companions: ui-contract.md + brownfield.md spec-authored; ../../project-context.md adopted; brainstorm intent + archived plan listed as absorbed sources
- (event) Self-validate pass 1 (coherence, Spec Law 1-6+8): PASS — 5 capabilities all intent+success; constraints all rule something out; 7 non-goals; success signal demonstrable (prove-run); CAP-1..5 fresh and unique; 3 assumptions and 2 open questions recorded
- (event) Self-validate pass 2 (preservation): PASS — brainstorm intent fully landed (why, built/gaps, settled 1-5, punch list -> CAP order); plan §3-§6 landed in constraints/brownfield/ui-contract. Wrapper-only content dropped: exec-summary+doctrine rhetoric, U1-U5 build-cut narration (as-built now), plan detailed prove-run assertion list (superseded by settled #5 one-run form), worker_threads future note (covered by non-goal), plan open questions 1-3 (all settled by brainstorm)
- (decision) OQ1 resolved by owner: runs happen in the background while Telar is open; a run finishing with no client attached is rare and accepted — wake fires immediately at terminal, no deferral/queueing machinery
- (decision) OQ2 resolved by owner: dock bubble is in — its point is tracking what is happening without the complete chat UI; implementation shape left open (owner indifferent); scoped as polish tier alongside chip and cost rollup
- (capability) CAP-6 Dock run signal: session with live runs shows run status (name/state/spend) in its dock bubble from anywhere in the app; tap re-focuses the run; one signal per session, concurrent runs summarize
- (event) Update self-validate: PASS — CAP-6 added with intent+success; deferred-wake non-goal added; Open Questions now empty and section omitted; ui-contract §9 rebound to CAP-6; both owner resolutions preserved in memlog
- (decision) Story 1 agreed: Completion wake (CAP-1), spec_checkpoint=true
- (decision) Story 2 agreed: Real session UI (CAP-2 + ui-contract.md), spec_checkpoint=true, done_checkpoint=true
- (decision) Story 3 agreed: Authoring-reference skill file (CAP-3), no checkpoints
- (decision) Story 4 agreed: Composer Ultra chip (CAP-4), no checkpoints
- (decision) Story 5 agreed: Session-cost rollup (CAP-5), no checkpoints
- (decision) Story 6 agreed: Dock run signal (CAP-6), no checkpoints
- (decision) Story 7 agreed: Prove-run (success signal), done_checkpoint=true; owner chose three separate polish stories, prove-run as own final story, no invoke_dev_with notes anywhere
- (event) stories.yaml schema check: PASS — 7 entries, all required fields, ids "1"-"7" unique/quoted/prefix-free, no status field, checkpoints only where owner set them, no invoke_dev_with, list order = execution order; no stories/ files yet so ids unpinned
