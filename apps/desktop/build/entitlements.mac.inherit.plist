<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<!--
	  The child processes this app forks — the engine, the Next standalone
	  server — inherit the parent's sandbox and, crucially, its Apple-events
	  permission: the engine is what actually sends the computer-use events,
	  so without inheriting this the grant given to Telar would not reach the
	  process that uses it.
	-->
	<key>com.apple.security.cs.allow-jit</key>
	<true/>
	<key>com.apple.security.cs.allow-unsigned-executable-memory</key>
	<true/>
	<key>com.apple.security.cs.disable-library-validation</key>
	<true/>
	<key>com.apple.security.inherit</key>
	<true/>
	<key>com.apple.security.automation.apple-events</key>
	<true/>
	<!--
	  THE RENDERER IS WHERE THE CAPTURE ACTUALLY HAPPENS. A page's camera and
	  microphone are opened by the Chromium helper processes this bundle signs
	  with the inherit plist, not by the main process that asked for consent —
	  so the device entitlements have to come down here too, exactly as the
	  Apple-events one does for the engine child. The grant is still the app's:
	  macOS attributes a helper's use to Telar.
	-->
	<key>com.apple.security.device.camera</key>
	<true/>
	<key>com.apple.security.device.audio-input</key>
	<true/>
</dict>
</plist>
