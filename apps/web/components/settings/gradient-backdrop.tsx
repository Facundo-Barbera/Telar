"use client";

/** Placeholder — the gradient preset gallery and custom gradient editor land
 *  here (writing the store via lib/backdrop.ts setBackdrop with resolved
 *  layers). Rendered by backdrop-section.tsx when the Scene control says
 *  Gradient. */
export function GradientBackdrop() {
  return null;
}
