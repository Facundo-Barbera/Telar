"use client";

/** Placeholder — the image picker (compressed to a data URL under
 *  BACKDROP_IMAGE_KEY), fit/blur/dim controls, and "make a theme from this
 *  image" land here. Rendered by backdrop-section.tsx when the Scene control
 *  says Image. */
export function ImageBackdrop() {
  return null;
}
