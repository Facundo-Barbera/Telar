# Throwaway — probe 1 for #755

This file exists so a pull request touching NO iOS path can be run against the
new simulator leg. It must take `ubuntu-latest`, skip both test steps, and
still report `Verify passed: SUCCESS`. Delete with the branch.
