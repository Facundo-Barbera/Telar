# Away run — goal and decision log (started 2026-08-07)

The owner is away; this run is authorized to finish the roadmap head
end-to-end. Scope and authority, as answered before leaving:

- **Scope:** the full roadmap head — message-lifecycle build (STEPS 0–6 of
  docs/plans/message-lifecycle.md), the creative run's verified defects
  (sidebar background liveness + approval indicator, crash-interrupt
  surfacing, feed observability), and the S-tier quick wins (clickable
  markers, draft persistence, transcript export).
- **Git authority:** merge branches when fully green; cut
  v0.1.0-nightly.20260807.3 after the server-layer branch lands. New work on
  fresh branches, merged when green.
- **Judgment calls:** decided against the feel contract and the lifecycle
  design as law, each one recorded below for review/veto.

## Sequence

1. Merge `refactor/session-server-layer` (PR → CI → merge), tag the nightly.
2. `fix/creative-run-defects` — sidebar liveness bundle, crash-interrupt
   surfacing, feed observability; merge when green.
3. `feat/quick-wins` — clickable coalesced markers, composer draft
   persistence, transcript export; merge when green.
4. `feat/message-lifecycle` — STEP 0 probe first (measure, don't assume),
   then F1 → F2 → anchors → remove-exchange → rollback → Codex parity.
   Steps land as reviewable commits; merge at stable boundaries.

## Decision log

- **Esc-Esc-idle staging deferred to a polish pass** (STEP 5 commit). The
  design's keyboard grammar stages the previous user message on Esc-Esc while
  idle with an empty composer; the gutter pencil (the primary affordance)
  shipped first. The existing Esc arming only listens while busy — extending
  it is a contained follow-up. Veto by asking for it now.
- **Pencils are mount-time truth** (STEP 5 commit). The anchor map comes from
  InitialChat, so a turn run since mount gains its pencil on reload rather
  than live — the same no-false-promise degradation legacy chats get, chosen
  over threading anchor updates through the done payload.

- **The refusal ladder's rung (a) heals on the next send, not in-flight**
  (STEP 4 commit). The design sketched an invisible same-POST re-run; wrapping
  the route's whole acquire-and-consume block in a retry loop was judged too
  risky a restructure. Instead a refusal re-arms the fork unguarded and closes
  the runtime, so the user's next send lands the documented unguarded
  truncation — visible as one failed send in a rare race, never silently
  wrong. Veto by asking for the in-flight retry.
- **"Remove this exchange" applies by re-seeding from the persisted record**
  rather than surgically editing local state — one source of truth, and the
  identical path a second tab takes through the feed's rolled_back line.

(Product-feel calls made without the owner. Newest first. Veto anything here
and the code moves — each entry names its commit.)

- **The walk previews; commit is typing or sending** (STEP 1 commit). The
  design left open when a walked-to line actually leaves the strip. Chosen:
  browsing (ArrowUp/ArrowDown/Escape) never removes anything — the line
  leaves only when the user types into it or sends it. Rationale: "nothing
  is destroyed before you send" is the family's own grammar sentence, and a
  browse that consumed lines would need re-queue churn to undo.
- **"Needs your approval" outranks "Working…" everywhere it renders** (task
  #20 commit). An unanswered card means work is paused ON the user, so the
  sidebar row's dot, meta line, and hover card all show it first — steady
  amber (the Marker attention pair), in words, no icon-only state. Rationale:
  feel-contract "background presence in words"; the one sidebar state whose
  job is to interrupt.

## Status notes

- 2026-08-07 (night): THE ROADMAP HEAD IS BUILT. Lifecycle STEPS 0–6 all
  implemented: probe (merged #57), F1 recall-whole (#57), F2
  stop-and-reclaim (#57), anchors (#58), remove-this-exchange (#59), full
  rollback staging + one-request truncate-then-send + Codex thread/rollback
  (PR #60, CI pending at time of writing). Remaining after #60 merges:
  live E2E passes over the new features on the dev server, the two logged
  deferrals (Esc-Esc-idle staging, live pencil updates), and task #8 (CLI
  bundling) which was explicitly out of the head's scope.

- 2026-08-07: run started; branch at 24 commits, all gates green.
- 2026-08-07 (later): stage 1 DONE — PR #54 merged, nightly
  v0.1.0-nightly.20260807.3 built and published (3m31s). Stage 2 DONE on
  branch fix/creative-run-defects (PR #55): sidebar attention bundle,
  feed-failure diagnostics, crash truth-teller. Next: merge #55, then
  feat/quick-wins.
- 2026-08-07 (evening): stage 2 merged (#55), stage 3 merged (#56 — quick
  wins: clickable markers, draft persistence, transcript export). Stage 4
  underway on feat/message-lifecycle: STEPS 0–2 done (probe findings in
  deferred-work.md; F1 recall-whole; F2 stop-and-reclaim) — PR #57 open at
  this stable boundary. Remaining: STEP 3 anchors → STEP 4 remove-exchange
  → STEP 5 full rollback → STEP 6 Codex parity.
