{
  "name": "telar",
  "private": true,
  "description": "Project-centric agentic dev tool on the Claude Agent SDK — self-verifying looms where a human always accepts.",
  "license": "UNLICENSED",
  "author": "Facundo Barbera <facundo.barbera@gmail.com>",
  "repository": {
    "type": "git",
    "url": "git+ssh://git@github.com/Facundo-Barbera/Telar.git"
  },
  "workspaces": ["apps/*", "packages/*"],
  "packageManager": "bun@1.3.11",
  "scripts": {
    "dev": "turbo dev",
    "dev:desktop": "bun run --cwd apps/desktop dev",
    "desktop:package": "bun run --cwd apps/desktop package:local",
    "desktop:install": "bun run --cwd apps/desktop install:local",
    "verify": "bun run typecheck && bun run lint && bun run test",
    "typecheck": "bun run --cwd packages/core typecheck && bun run --cwd apps/web typecheck && bun run typecheck:core-tests",
    "typecheck:core-tests": "bun scripts/typecheck-ceiling.mjs",
    "lint": "bun scripts/lint-ceiling.mjs",
    "test": "bun run test:core && bun run test:web && bun run test:desktop",
    "test:core": "bun run --cwd packages/core test",
    "test:web": "bun run --cwd apps/web test",
    "test:desktop": "bun run --cwd apps/desktop test:desktop:unit",
    "test:desktop:smoke": "bun run --cwd apps/desktop test:desktop:smoke",
    "test:desktop:e2e": "bun run --cwd apps/desktop test:desktop:e2e"
  },
  "devDependencies": {
    "turbo": "^2.10.8"
  }
}
